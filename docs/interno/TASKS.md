# Ametista Conversões — Checklist do Projeto

> Trabalhamos **uma fase por vez**. Cada fase só começa depois que a anterior for aprovada.

## Fase 1 — Fundação visual
- [x] Inicializar o projeto (Vite + React + TypeScript)
- [x] Configurar o Tailwind com os tokens de cor exatos do `CLAUDE.md`
- [x] Configurar a fonte Inter
- [x] Criar o `AppLayout` (Sidebar fixa no desktop / menu retrátil no celular + TopBar fixa + área principal responsiva)
- [x] Criar a Sidebar com os itens de navegação do Portal Cliente e Portal Gestor (dados fixos por enquanto)
- [x] Aplicar os estilos de card, botão e badge conforme o `CLAUDE.md`

## Fase 2 — Autenticação e permissões (RBAC)
- [x] Configurar projeto no Supabase (autenticação + banco) e explicar como criar a conta e pegar as chaves de API
- [x] Criar páginas de Login, Registro, Esqueci a senha e Redefinir senha
- [x] Criar o `ProtectedRoute` (bloqueia quem não está logado)
- [x] Criar o `RoleRoute` (bloqueia usuários `cliente` de acessarem rotas do Portal do Gestor)
- [x] Ligar a Sidebar para mostrar apenas as seções do papel do usuário logado
- [x] Testado com as 3 contas de teste (`admin`, `gestor`, `cliente`) pelo usuário — todos os critérios de aceite confirmados

## Fase 3 — Entidades e banco de dados
- [x] Criar as 16 entidades listadas no `CLAUDE.md`, com campos e relacionamentos corretos
- [x] Configurar Row Level Security (cliente só vê os próprios dados)
- [x] Popular dados de teste (seed) para visualização
- [x] Testado com verificação automática de segurança (9 testes: anônimo, cliente e admin) — todos os critérios de aceite confirmados

## Fase 4 — Portal do Cliente (detalhada em `Prompt fase 4.txt`)

### Fase 4.1 — Dashboard e Performance
- [x] Dashboard (`/`): KPIs, Health Score expansível (4 sub-notas), reuniões, tarefas, resumo financeiro, metas, notificações, botão Pausa de Emergência
- [x] Performance (`/performance`): métricas em 3 categorias, gráfico de barras (canal) e gráfico de linha (tendência)
- [x] Testado com dados reais do Supabase, incluindo o gráfico mudando ao editar um valor no banco, e responsividade nas larguras problemáticas (iPad Air ~820px, Zenbook Fold ~853px)

### Fase 4.2 — Relatórios e Meu Projeto
- [x] Relatórios (`/reports`): 4 abas (Performance de Tráfego, Financeiro, Distribuição & Canais, Clientes & Mensalidades)
- [x] Meu Projeto (`/project`): info cards, cronograma, checklist de onboarding, metas SMART, lista de tarefas
- [x] Testado com dados reais do Supabase, incluindo marcar/desmarcar uma etapa do onboarding e navegação entre as 4 abas, e responsividade nas larguras problemáticas (iPad Air ~820px, Zenbook Fold ~853px)

### Fase 4.3 — Tarefas, Arquivos, Comentários e Reuniões
- [x] Tarefas (`/tasks`): lista com filtro, nova tarefa, toggle de conclusão
- [x] Arquivos (`/files`): abas Arquivos e Aprovações, upload, aprovar/rejeitar/pedir revisão
- [x] Comentários (`/comments`): feed com novo comentário
- [x] Reuniões (`/meetings`): agendar, lista com status
- [x] Testado com dados reais do Supabase: criar tarefa e concluir, subir e abrir arquivo, aprovar uma aprovação, enviar comentário (com nome/papel), agendar reunião, e responsividade nas larguras problemáticas (iPad Air ~820px, Zenbook Fold ~853px)
- [x] Ajustes pós-teste: trava de data passada em tarefas e reuniões, popup de detalhes da tarefa com campo de descrição, seletor de horário pré-definido nas reuniões, título nos comentários, e novas aprovações de exemplo em "Pendente"
- [x] Mais ajustes: menu de horário reescrito com Radix Select (altura limitada, cores do design system — corrigido também um bug real do `--popover` que deixava menus suspensos transparentes), arquivos categorizados por tipo com prévia de imagem/vídeo (sem o selo de aprovação, que confundia com a aba Aprovações), e menu de status da tarefa (Backlog/A fazer/Em andamento/Em revisão) direto no badge

### Fase 4.4 — Configurações
- [x] Configurações (`/settings`): abas Globais/Agência/Cliente/Usuário filtradas por papel (Globais só admin; Agência admin/gestor; Cliente e Usuário para todos)
- [x] Aba Usuário: editar nome/telefone (salva de verdade) e botão Sair
- [x] Corrigido: "Configurações" agora aparece no menu lateral para o papel `gestor` também (antes só existia para `cliente`/`admin`)
- [x] Testado com 3 contas de teste (`cliente`, `gestor`, `admin`) — abas corretas por papel, edição de perfil persistindo, e logout funcionando

## Fase 5 — Portal do Gestor

### Fase 5.1 — Dashboard Executivo e Clientes
- [x] Dashboard Executivo (`/admin`): 8 KPI cards (MRR Total, Clientes Ativos, Churn Rate, Incidentes Abertos, Clientes em Risco, Carga de Trabalho, Produtividade, Budget Gerenciado), gauge de produtividade, lista de clientes em risco, lista de incidentes abertos recentes
- [x] Clientes (`/clients`): busca por nome/empresa, dialog "Novo Cliente", grid de cards com health score e cor por status
- [x] Testado logado como `admin`: os 8 KPIs batem com os dados de teste, criar cliente novo aparece na hora no grid com a cor certa; testado logado como `cliente`: `/admin` redireciona com "Acesso Negado" (regressão da Fase 2 confirmada)
- [x] Melhoria de UX (pedida pelo usuário): ícone de dúvida nos cards de KPI (`KpiCard`) mostrando fórmula/descrição ao passar o mouse — aplicado nos 8 KPIs do Dashboard Executivo e retroativamente no Dashboard, Performance e Relatórios do Portal Cliente (mesmo componente reaproveitado nos 4 lugares)

### Fase 5.2 — Kanban e Workflows
- [x] Kanban (`/kanban`): 5 colunas (Backlog/A fazer/Em andamento/Em revisão/Concluída — os mesmos valores de status das tarefas), arrastar e soltar com `@dnd-kit` (dependência nova, autorizada), filtro por cliente, botão "Nova tarefa"
- [x] Workflows (`/workflows`): 4 modelos fixos (Novo Cliente, Nova Campanha, Produção de Criativos, Configuração de Pixel), botão "Aplicar Workflow" com seleção de projeto — cria várias tarefas de uma vez, todas em Backlog
- [x] Testado: arrastar um card e recarregar a página confirma que o status salvou de verdade; aplicar um workflow cria as tarefas no Kanban E elas aparecem na página Tarefas do cliente dono do projeto (Fase 4.3); testado logado como `cliente`: `/kanban` e `/workflows` continuam bloqueados (regressão da Fase 2)
- [x] Ajustes de UI pós-teste: colunas do Kanban ganharam altura fixa com rolagem própria (a barra de rolagem horizontal do quadro não fica mais lá embaixo da página) — e corrigido um bug real encontrado no caminho: `overflow-x: hidden` duplicado em `html` **e** `body` quebrava o `position: sticky` da sidebar em páginas altas, fazendo a cor de fundo "sumir" ao rolar (removido do `body`, mantido só no `html`)

### Fase 5.3 — Incidentes, Alertas e Timeline
- [x] Incidentes (`/incidents`): novo incidente, contagem por severidade, resolução via dialog — e `/status` mostrando o mesmo conteúdo
- [x] Alertas (`/alerts`): novo alerta, seção de ativos e de resolvidos
- [x] Timeline (`/timeline`): lista de eventos com ícone e severidade
- [x] Registro automático de eventos na Timeline via trigger no banco (não depende do código do front): criar/resolver incidente, criar/resolver alerta, aplicar workflow — todos gravam sozinhos em `audit_logs`
- [x] Botão "Pausa de Emergência" do Portal Cliente (Fase 4.1) conectado de verdade: cria um incidente crítico (via RPC `trigger_emergency_pause`) que aparece automaticamente em Incidentes e na Timeline do Portal Gestor — confirmando que os dois portais estão conectados
- [x] Testado: incidente/alerta de teste aparecem na Timeline na hora certa (criação e resolução); Pausa de Emergência do cliente gera o incidente crítico automaticamente; `/status` espelha `/incidents`; regressão confirmada (`cliente` continua bloqueado em `/incidents`, `/alerts`, `/timeline`)
- [x] Ajustes de UX/responsividade pós-teste (pedidos pelo usuário, valem para os dois portais):
  - Pausa de Emergência ganhou campo de motivo + lista de campanhas ativas com checkbox — agora pausa de verdade os projetos escolhidos (`status = paused`) além de abrir o incidente crítico
  - Ícone de dúvida dos KPIs trocou de `Tooltip` (hover) para `Popover` (clique) — no celular, tocar não fazia sentido com hover e "sumia" a métrica; agora abre ao tocar e fica até tocar de novo ou tocar fora
  - Kanban: sensor de arrastar do `@dnd-kit` separado em mouse (imediato) e toque (segura ~200ms) — corrige o celular não deixar rolar o quadro pro lado
  - `SelectContent` (menus como o de horário) e `TabsList` (abas de Relatórios) ganharam largura/rolagem travada — não esticavam mais que o campo/página em telas estreitas
  - Campo "Data" (Nova Tarefa, Agendar Reunião) trocou o seletor numérico nativo por um calendário de verdade (`react-day-picker` + Popover), componente pronto para reaproveitar nas próximas fases
  - Fileira "Data + Horário" do formulário de reunião empilha em telas estreitas em vez de espremer

- [x] Ajustes pós-teste adicionais (pedidos pelo usuário):
  - Workflows (Portal Gestor) deixaram de ser fixos no código: agora moram no banco (`workflow_templates`), e só a conta `admin` vê o botão "Novo modelo" para criar e estruturar modelos novos (nome, descrição, lista de etapas) — `gestor` continua podendo aplicar os modelos existentes, mas não criar
  - Kanban: diálogo "Nova tarefa" ganhou o campo "Prazo (opcional)" com o mesmo calendário (`DatePicker`) já usado no Portal Cliente
  - Ícones de dúvida dos KPIs: só uma dica fica aberta por vez em toda a tela (estado compartilhado entre os cards) — abrir uma fecha a anterior automaticamente

### Fase 5.4 — Ativos Digitais, Metas SMART e Onboarding
- [x] Ativos Digitais (`/assets`): filtro por cliente, "Novo ativo" (nome, cliente, tipo, plataforma, status), badge de status trocável na hora (dropdown)
- [x] Metas SMART (`/smart-goals`): filtro por cliente, "Nova meta" (título, cliente, métrica, valor alvo/atual, período, status), botão "Atualizar progresso" por meta com barra de progresso
- [x] Onboarding (`/onboarding`): visão agrupada por cliente (um card com barra de progresso + checklist por cliente), "Nova etapa" (título, cliente, projeto opcional, categoria), reaproveita o mesmo toggle de concluído/pendente do Portal Cliente
- [x] Testado logado como `gestor`: criar ativo e trocar status pelo badge, criar meta e atualizar progresso (barra reflete na hora), criar etapa de onboarding e marcar como concluída — tudo refletindo direto no banco; as 3 páginas usam o mesmo `RoleRoute` das demais telas do Portal Gestor, então o bloqueio para `cliente` já estava garantido (nenhum arquivo do Portal Cliente foi alterado)
- [x] Ajustes pós-teste (pedidos pelo usuário):
  - Ativos Digitais: tipos com link de acesso (conta de anúncios, Business Manager, domínio) ganharam campo de link clicável; pixel e tag (novo tipo) ganharam campo de código/snippet no lugar do link
  - Metas SMART: período virou um calendário de prazo único (`target_date`); métrica virou uma lista categorizada (CPA, ROAS, CPC, CTR, CPM, Leads, Conversões, Faturamento, Ticket Médio, Taxa de Conversão) com opção "Outro" pra digitar; cada card ganhou um selo automático de urgência (Atrasada/Urgente/Imediato, calculado a partir do prazo) e a página ganhou um ícone de informação único (não repetido por card) explicando as 3 faixas
  - Corrigido bug real: uma etapa de onboarding criada pelo gestor sem projeto vinculado nunca aparecia no checklist do Portal Cliente (o filtro só considerava o projeto atual) — agora etapas gerais do cliente (sem projeto específico) aparecem também
  - Nova aba "Comentários" no Portal Gestor (`/client-comments`): escolhe um cliente e vê/responde os comentários dele em formato de chat (cliente à esquerda, agência à direita) — usa o mesmo mural que já existia no Portal Cliente, só que visto do outro lado; os dois portais ganharam um botão "Regras de uso" explicando o padrão de título com `[tópico]` entre colchetes (só orientação, não bloqueia o envio)
- [x] Mais uma rodada de ajustes (pedidos pelo usuário):
  - **Status do cliente editável**: o badge de status em Clientes agora é um menu (mesmo padrão de Tarefas) — inclui o novo status manual "Em risco" (vermelho)
  - **Indicadores automáticos de risco**: Health Score no card de cliente ganhou cor (verde/laranja/vermelho conforme o valor); novo badge automático "Em problemas" (laranja) calculado sozinho quando o cliente tem meta SMART atrasada, 2+ incidentes médios (ou 1+ alto/crítico) abertos, 4+ tarefas atrasadas em algum projeto, ou 2+ alertas médios (ou 1+ alto/crítico) não resolvidos
  - **Apagar itens**: ícone de lixeira (liga um "modo de exclusão") ao lado do título em Clientes (só `admin`), Workflows (só `admin`), Incidentes, Ativos Digitais, Alertas, Metas SMART, Onboarding, e nas telas de Tarefas e Arquivos do Portal Cliente — cada card ganha seu próprio ícone de apagar com confirmação; no Kanban o modo revela uma zona de lixeira pra arrastar o card (com animação), em vez de um ícone por card
  - **Reuniões com fluxo de cancelamento**: cancelar uma reunião (cliente ou gestor) agora pede um motivo e marca como "Cancelada" (não apaga na hora) — só depois de cancelada é que a outra parte consegue apagá-la de vez, garantindo que os dois lados saibam o porquê antes dela sumir; nova aba "Reuniões" no Portal Gestor (`/client-meetings`) que antes não existia — o gestor agora vê as reuniões marcadas pelos clientes e também pode marcar reuniões novas
- [x] Mais dois ajustes (pedidos pelo usuário):
  - Corrigido bug real: a zona de lixeira do Kanban ficava dentro da mesma fileira rolável das 5 colunas, então em telas comuns ela ficava fora da área visível — parecia "lento"/exigir várias tentativas porque o card sempre era solto antes de rolar até ela. Agora é uma faixa larga abaixo do quadro, sempre visível, sem precisar rolar
  - Aba Agência (Configurações) virou editável — o próprio código já tinha um aviso "isso chega no Portal do Gestor (Fase 5)" desde a Fase 4, nunca implementado; agora `admin`/`gestor` editam nome, plano, status e domínio da agência (a política do banco já liberava os dois papéis, só faltava o formulário)
- [x] Ajuste fino na lixeira do Kanban: a exclusão dependia de acertar exatamente a faixa fina da lixeira no instante de soltar o card (`onDragEnd`) — tentativa de apagar assim que o card **entra** na área da lixeira durante o arrasto (`onDragOver`), sem precisar soltar o botão do mouse
- [x] Lixeira do Kanban trocada de vez pelo padrão de exclusão usado no resto do app: em vez de arrastar o card até uma zona de lixeira, o modo de exclusão (mesmo ícone de sempre) agora mostra um ícone de apagar em cada card, com confirmação — igual Clientes, Incidentes, Ativos Digitais etc. Arrastar fica desativado enquanto o modo está ligado, pra não misturar as duas ações
- [x] Rodada grande de melhorias (pedidas pelo usuário):
  - **Workflows editáveis e etapas reordenáveis**: modelos existentes agora têm botão "Editar" (só `admin`); as etapas podem ser arrastadas pra reordenar dentro do formulário (`@dnd-kit/sortable`, nova dependência aprovada) — não precisa mais apagar tudo pra corrigir a ordem
  - **Arquivos & Aprovações no Portal Gestor** (aba nova `/client-files`): o gestor agora consegue mandar arquivos direto pro cliente (sem aprovação) ou pra aprovação (fica pendente até o cliente decidir). Corrigido um buraco real: a função `respond_to_approval` só atualizava o status da aprovação e nunca fazia o arquivo aparecer em lugar nenhum — agora, ao aprovar, ela cria a entrada em Arquivos automaticamente, e isso aparece na hora nos dois portais (sem precisar recarregar a página)
  - **Central de Informações do Cliente**: clicar num card em Clientes agora leva pra uma página dedicada com logo (upload), telefone, data de renovação, observações internas (só a agência vê — nunca vai pro portal do cliente, confirmado por teste), e as tarefas/metas/reuniões daquele cliente reunidas num só lugar

### Fase 5.5 — Central editável, planos fixos, reuniões recorrentes e notificações no menu
- [x] **Central de Informações editável**: nome, empresa (nome do negócio), e-mail, plano e mensalidade agora são editáveis (antes só telefone/renovação/observações eram); listas de Tarefas (até 10), Metas SMART (até 5) e Reuniões (até 10) ganharam rolagem interna em vez de esticar a página; novo card "Cliente em risco" (mesmo cálculo do badge "Em problemas" já existente, agora com o detalhamento — tarefas atrasadas, metas fora do prazo, alertas e incidentes abertos) que só aparece quando há algum problema de verdade
- [x] **Planos fixos**: viraram só 3 opções — Validação, Escala, Dominação — selecionáveis na criação do cliente e editáveis na Central (antes era texto livre sem nenhum padrão)
- [x] **Reuniões automáticas por plano**: botão "Configurar recorrência" em Reuniões (Portal Gestor) liga um cadastro por cliente que mantém sempre 3 reuniões futuras tituladas "Reunião" — frequência decidida pelo plano (Validação = mensal, Escala = quinzenal, Dominação = semanal); ao concluir uma reunião (novo botão "Concluir"), o sistema já cria a próxima pra repor
- [x] **Notificações no menu**: bolinha roxa ao lado de Comentários/Reuniões/Arquivos/Alertas/Incidentes/Kanban/Metas SMART (Portal Gestor) e Comentários/Tarefas/Arquivos/Reuniões (Portal Cliente) — acende quando há atividade nova (reunião, mensagem, arquivo, tarefa, aprovação decidida, alerta, meta, incidente) e some sozinha assim que a aba é visitada (nova tabela guardando a última visita de cada usuário a cada aba)
- [x] Testado ao vivo como `gestor`/`cliente` (migração rodada pelo usuário): campos editáveis da Central salvos e persistidos após recarregar; card "Cliente em risco" aparece só no cliente com pendência de verdade; planos fixos aparecem certinho no formulário de criação e na Central; recorrência de reuniões testada direto no banco — cadastrar um cliente com plano Escala criou 3 reuniões quinzenais "Reunião", e concluir uma repôs a próxima automaticamente, mantendo sempre 3; bolinha de notificação testada de ponta a ponta — comentário novo do gestor acende a bolinha na hora no Portal Cliente, visitar a aba apaga só aquela bolinha, sem afetar as outras

### Fase 5.6 — Busca em 5 abas + Ativos Digitais editáveis
- [x] **Busca**: caixa de busca (lupinha) nova em Kanban (por tarefa), Workflows (por modelo), Ativos Digitais (por nome/plataforma) e Timeline (por evento/cliente) — posicionada à esquerda do seletor de cliente ou do botão de criar; a busca de Clientes (nome/empresa) já existia e só mudou de lugar, pro mesmo padrão das outras
- [x] **Ativos Digitais editáveis**: antes só dava pra trocar o status (badge) ou apagar — agora um botão de editar (lápis) abre o mesmo formulário de criação já preenchido, deixando mudar nome, cliente, tipo, plataforma, link/código e status, no mesmo molde do editor de Workflows
- [x] Testado ao vivo como `gestor`: busca filtrando corretamente nas 5 abas (testei "não encontrado" e também um termo que bate com resultado real); editei um ativo existente (nome e plataforma), recarreguei a página e as mudanças persistiram

### Fase 5.7 — Tarefa atrasada acinzentada, ordenação por severidade/prazo, Kanban editável
- [x] **Tarefa atrasada acinzentada**: card do Kanban com prazo vencido (e status diferente de "Concluída") aparece visivelmente apagado (opacidade + leve dessaturação), padrão comum em CRMs
- [x] **Ordenação por severidade**: Incidentes e Alertas agora listam os mais severos primeiro (crítico → alto → médio → baixo)
- [x] **Ordenação por prazo**: Tarefas/Kanban e Metas SMART agora listam pelo prazo mais próximo primeiro — quem não tem prazo fica por último
- [x] **Tarefas do Kanban editáveis**: cada cartão ganhou um botão de editar (lápis, sempre visível) que abre o mesmo formulário de criação já preenchido — antes só dava pra arrastar entre colunas ou apagar
- [x] Testado ao vivo como `gestor`: criei incidentes/alertas com severidades diferentes e confirmei os mais críticos no topo; criei tarefas com prazos diferentes (uma atrasada) e confirmei a ordem por prazo e o card atrasado acinzentado; editei uma tarefa do Kanban (título/categoria), recarreguei e a mudança persistiu; confirmei que arrastar um card entre colunas continua funcionando normalmente depois de adicionar o botão de editar

> **Consolidação de numeração (feita ao iniciar a Fase 6, pedida pelo `Prompt- Fase 6`):** a antiga "Fase 8" (Integrações externas de dados) tratava do mesmo assunto deste documento, então foi incorporada aqui embaixo e deixou de existir como fase separada. A antiga Fase 6 (Cassie/IA) e a antiga Fase 7 (testes/publicação) foram unidas numa Fase 7 só, exatamente como o documento pediu ("ao terminar a Fase 6.4 ... seguir para a Fase 7 (C.A.S.S.I.E. e testes finais)").

## Fase 6 — Integrações com Google Ads, Meta Ads e Google Forms (detalhada no documento `Prompt- Fase 6`)

### Fase 6.1 — Fundação do backend de integrações
- [x] Backend de integrações: Edge Function `integrations` (`supabase/functions/integrations/index.ts`), publicada colando o código no painel do Supabase — mesmo fluxo zero-instalação já usado pra toda migração
- [x] Armazenamento seguro de tokens: em vez de guardar o valor criptografado direto numa coluna (tentativa inicial com `pgsodium` na mão esbarrou em "permission denied" nas funções de baixo nível), o token vai pro **Supabase Vault** — `oauth_tokens` guarda só o `id` (uuid) do segredo no Vault, nunca o valor; confirmado no Table Editor que a coluna mostra um uuid, não o token em texto
- [x] `performance_snapshots` estendida com `channel`, `clicks`, `impressions`, `conversions` (em vez de criar a `MetricSnapshot` do documento como tabela nova — decisão confirmada com o usuário) — Performance/Relatórios continuam lendo da mesma tabela de sempre
- [x] Estrutura genérica de OAuth: rotas `/connect` (gera a URL de autorização, reaproveitando a linha de conexão existente se já houver) e `/callback` (grava o token e marca a conexão como "connected"), reaproveitáveis por qualquer provedor — tabela nova `digital_asset_connections` (uma linha por Ativo Digital conectado a um provedor, só leitura pra admin/gestor)
- [x] Testado ao vivo via chamadas HTTP direto na função publicada: `/connect` devolve a URL de autorização e cria a conexão (`disconnected`); `/callback` grava o token no Vault e muda o status pra `connected`, confirmado direto no banco; corrigidos dois bugs reais no caminho — a URL de callback vinha fixa no código apontando pro nome errado da função (agora se monta sozinha a partir do path da requisição + `SUPABASE_URL`), e a criptografia via `pgsodium` direto não tinha permissão (trocada pelo Vault)

### Fase 6.2 — Conexão com Google (Ads e Forms)
- [x] Fluxo completo de OAuth com o Google: botão "Conectar integração" em Ativos Digitais (escolhe Google Ads ou Google Forms) → `/connect` monta a URL de autorização de verdade (escopo certo por provedor, `access_type=offline&prompt=consent` pra garantir refresh token) → `/callback` (aberta pelo navegador do Google) troca o código por token real e guarda no Vault
- [x] Conexão com a Google Ads API: `/callback` descobre a conta de anúncios acessível (`listAccessibleCustomers`) e grava; nova rota `/sync` busca investimento/cliques/impressões/conversões dos últimos 30 dias (GAQL) e grava em `performance_snapshots` — como a tabela exige um projeto, o gestor escolhe qual projeto do cliente recebe as métricas no momento de conectar (`digital_asset_connections` ganhou `project_id`)
- [x] Conexão com a Google Forms API: mesmo login, escopo a mais; o gestor cola o id ou o link do formulário ao conectar (evita precisar do escopo do Google Drive só pra listar formulários)
- [x] **Decisão confirmada com o usuário** sobre o que uma resposta de formulário gera: cria um **Alerta** (categoria "novo lead", com as respostas formatadas) pra o gestor decidir manualmente — não cria Cliente nem entidade nova. Detecção via gatilho do Google Forms (Apps Script, script pronto em `supabase/functions/integrations/forms-trigger.gs.txt`) chamando a nova rota `/forms-webhook`, com proteção por segredo compartilhado e sem duplicar Alerta se a mesma resposta chegar duas vezes (`form_response_events`)
- [x] Como `/callback` (aberta pelo navegador do Google) e o gatilho do Forms chamam a função de fora, sem sessão do Supabase, a verificação automática de JWT da função foi desligada e a autenticação passou a ser feita na mão: `/connect`/`/sync` conferem se quem chamou é admin/gestor de verdade; `/callback` se protege pelo próprio `state` (imprevisível); `/forms-webhook` por um segredo compartilhado
- [x] Testado ao vivo tudo que dava pra testar sem credenciais reais do Google: os quatro endpoints da função (`/connect`, `/sync`, `/forms-webhook` e a validação de autenticação) respondendo certo com erros esperados e claros; a URL de autorização montada corretamente (escopos, `state`, `redirect_uri`) pros dois provedores; o id do formulário sendo extraído certo de um link colado; o projeto e o formulário sendo salvos na conexão; a tela de Ativos Digitais mostrando os badges de status ("Desconectado") certinho
- [ ] **Pendente**: conexão de verdade com uma conta real do Google (você ainda não tem as credenciais do Google Cloud Console) — assim que tiver, é só cadastrar os segredos (`GOOGLE_OAUTH_CLIENT_SECRET`, `GOOGLE_ADS_DEVELOPER_TOKEN`, `FORMS_WEBHOOK_SECRET`, `FRONTEND_URL`) e testar o fluxo completo (login real, métricas de verdade em `performance_snapshots`, resposta de formulário virando Alerta)
- [x] **Buraco real encontrado e corrigido ao testar**: conectar Google Ads pede um projeto do cliente, mas o Portal Gestor não tinha nenhuma forma de criar projeto em lugar nenhum (só existia leitura, na tela "Meu Projeto" do Portal Cliente). Central de Informações do Cliente ganhou um card novo "Projetos" (nome, objetivo, descrição por enquanto — sem edição/exclusão nesta rodada), e o projeto criado ali já aparece corretamente no seletor do diálogo "Conectar integração" — testado ao vivo criando um projeto e confirmando que ele aparece na lista de opções
- [x] **Outro bug real encontrado e corrigido ao testar** (com o usuário clicando de verdade em "Conectar" sem credencial configurada ainda): `/connect` mandava a pessoa pro Google mesmo com o segredo `GOOGLE_OAUTH_CLIENT_ID` vazio — o Google recusava com um erro genérico ("Missing required parameter: client_id"), sem nem mostrar a tela de login, o que parecia um erro sem explicação. Agora `/connect` verifica isso antes de mandar pra qualquer lugar e devolve um aviso claro dentro do próprio app ("as credenciais do Google ainda não foram configuradas") — confirmado pelo usuário que já funciona como esperado

### Fase 6.3 — Conexão com Meta Ads
- [x] Fluxo completo de OAuth com o Meta: Meta Ads virou uma opção no mesmo diálogo "Conectar integração" (escopo `ads_read` — só leitura, sem gerenciar campanha); `/connect` monta a URL de autorização do Meta, `/callback` troca o código pelo token
- [x] Particularidade do Meta implementada: a troca de token é `GET` (não `POST` como o Google), e não existe "refresh token" separado — o token de curta duração é trocado por um de longa duração (60 dias) logo ao conectar, e renovado reexecutando essa mesma troca antes de vencer (`getValidAccessToken` agora sabe o provedor da conexão e usa a lógica certa pra cada um)
- [x] Conexão com a Meta Marketing API: `/callback` descobre a conta de anúncios acessível (`/me/adaccounts`); `/sync` passou a aceitar `meta_ads` além de `google_ads`, buscando investimento/cliques/impressões/conversões via Insights (`date_preset=last_30d&time_increment=1`) — "conversões" no Meta vem dentro de um campo `actions` (lista de ações), somado como aproximação por enquanto
- [x] Testado ao vivo tudo que dava pra testar sem credenciais reais do Meta: `/connect` pede o projeto certo, valida que `META_APP_ID` precisa estar configurado (mesmo aviso claro implementado pro Google), e o diálogo mostra "Meta Ads" como opção com o campo de projeto aparecendo certinho
- [ ] **Pendente**: conexão de verdade com uma conta real do Meta (você ainda não tem o app em developers.facebook.com) — assim que tiver, é só cadastrar `META_APP_ID`/`META_APP_SECRET` e testar. Nota do próprio documento: contas de anúncio de clientes de terceiros só funcionam de verdade depois da aprovação de Business Verification do Meta — até lá, testável só com a própria conta da agência

### Fase 6.4 — Sincronização agendada e página de Integrações
- [x] Job agendado: `migration-019-fase64-cron.sql` habilita `pg_cron`/`pg_net` e agenda uma chamada a cada 6 horas pra rota nova `/sync-all` da Edge Function `integrations` — ela busca todas as conexões `connected` de Google Ads/Meta Ads e sincroniza uma a uma (a renovação de token já acontece sozinha dentro da sincronização, reaproveitando `getValidAccessToken`); autenticada por um segredo próprio (`CRON_SECRET`), mesmo padrão do webhook do Google Forms — como o cron roda de dentro do banco, sem login de usuário por trás, precisa desse segredo em vez de JWT
- [x] Página "Integrações" (`/integrations`, novo item no menu do Portal Gestor): lista todas as conexões (de todos os Ativos Digitais), com provedor, ativo, cliente, projeto, status e última sincronização, com filtro por cliente/busca e botão "Sincronizar agora" pra Google Ads/Meta Ads — reaproveita os mesmos hooks e a mesma função de sincronização já usados no card de Ativo Digital
- [x] Confirmar que não existe mais nenhuma "Fase 8" separada — tudo consolidado nesta Fase 6 (feito no início da Fase 6, ver nota acima)
- [ ] Pendente: rodar `migration-019-fase64-cron.sql` (com o segredo já preenchido), configurar o segredo `CRON_SECRET` na Edge Function e confirmar no SQL Editor (`select * from cron.job;`) que o job `sync-integrations` ficou agendado — depende só de você rodar/configurar, não de credenciais reais

## Fase 6.5 — ajustes pedidos após a Fase 6 (uma sub-fase de cada vez)

### Fase 6.5.1 — Disponibilidade do gestor + reunião de emergência (plano Dominação)
- [x] Nova tabela `manager_availability_blocks` (`migration-020-fase65-disponibilidade-reuniao-emergencia.sql`) e aba "Disponibilidade" em Configurações (admin/gestor): grade semanal de horários (mesma lista de 30 em 30 min já usada em Reuniões) onde dá pra marcar indisponibilidade clicando na célula
- [x] Botão "Agendar reunião" do cliente virou "Reunião de emergência" (renomeado o componente pra refletir isso) — agora restrito ao plano Dominação, 1x por mês, e os horários bloqueados nas Configurações do gestor somem da lista; toda a validação (plano, limite mensal, horário bloqueado) mora numa função do banco (`request_emergency_meeting`), não só na tela, então não dá pra burlar
- [x] Isso é separado do sistema de reuniões recorrentes automáticas (Fase 5.5) — esse continua funcionando exatamente igual
- [ ] Pendente: rodar `migration-020-fase65-disponibilidade-reuniao-emergencia.sql` no SQL Editor — só depois disso a aba "Disponibilidade" carrega de verdade e o pedido de reunião de emergência funciona (testado tudo que dava pra testar sem isso: telas carregam, botão aparece desabilitado certinho pra cliente fora do plano Dominação)

### Fase 6.5.2 — Workflows para clientes + prazos por espaçamento de tempo
- [x] `apply_workflow` (já existia desde a Fase 5) passa a aceitar um `due_days` opcional em cada etapa do modelo — ao aplicar, o prazo da tarefa é calculado sozinho (hoje + os dias, no fuso da agência), sem precisar digitar prazo manual em nenhuma tarefa
- [x] Nova aba "Workflows do Cliente" dentro da página Workflows (mesma página, duas abas — "Operacional" e "Workflows do Cliente"): nova tabela `client_workflow_templates` e função `apply_client_workflow`, que aplica um modelo a um ou mais clientes escolhidos de uma vez (sem passar por projeto) — as tarefas nascem com o `client_id` certo e já aparecem na aba "Tarefas" de cada cliente
- [x] Mesma regra de acesso das duas abas: admin cria/edita/apaga modelos; admin e gestor conseguem ver e aplicar (testado com os dois papéis)
- [x] `migration-021-fase652-workflows-cliente-prazos.sql` rodada e confirmada — aba "Workflows do Cliente" carregando de verdade

### Fase 6.5.3 — Nova aba "Tarefas do Cliente" no Portal Gestor
- [x] Nova aba `/client-tasks` no menu do Portal Gestor: filtro por cliente (ou "Todos"), busca por título, botão "Nova tarefa" e lista de tarefas — tudo reaproveitando hooks e o formulário que já existiam (`useAllTasks`, `useCreateManagerTask`/`useUpdateManagerTask`/`useDeleteManagerTask`/`useUpdateTaskStatus`, `KanbanTaskFormDialog`), sem nenhuma migração nova
- [x] Cada linha de tarefa mostra prioridade, status (clicável, com menu pra trocar), prazo, e fica acinzentada quando atrasada — mesma regra visual que o Kanban já usa (`opacity-60 grayscale-[0.5]` quando o prazo passou e a tarefa não está concluída)
- [x] Testado ao vivo: criar, editar, trocar status e filtrar por cliente — tudo refletindo na mesma tabela que o Kanban usa (é a mesma tarefa, só outra tela pra ver/mexer)
### Fase 6.5.4 — Restringir edição dos dados da agência ao admin
- [x] Aba "Agência" em Configurações: gestor continua vendo os dados, mas o formulário fica todo desabilitado (sem botão "Salvar"), com um aviso explicando por quê — testado ao vivo com os dois papéis
- [x] RLS reforçada em `organizations` (`migration-022-fase654-agencia-admin.sql`): admin cria/edita, gestor só lê — mesma dupla de políticas já usada em `workflow_templates`, então mesmo que alguém tente editar direto sem passar pela tela, o banco recusa
- [ ] Pendente: rodar `migration-022-fase654-agencia-admin.sql` — a tela já bloqueia a edição pro gestor independente disso, mas a proteção no banco só vale depois de rodar
### Fase 6.5.5 — Comentários do cliente em duas colunas
- [x] Portal Cliente > Comentários agora usa o mesmo `ChatThread` (duas colunas, tipo WhatsApp — mensagem própria de um lado, da agência do outro) que o Portal Gestor já usava — os dois lados tinham exatamente o mesmo formato de dado, então foi só trocar o componente de exibição; `CommentFeed.tsx` (formato antigo, em lista única) ficou sem uso em nenhum lugar e foi removido
- [x] Testado ao vivo com um cliente de teste que já tinha comentário + resposta — layout batendo certinho com o do gestor
### Fase 6.5.6 — Mesclar Incidentes e Alertas numa aba só
- [x] "Incidentes" e "Alertas" viram uma aba só, "Incidentes e Alertas" (`/incidents`) — mescla só na tela: os bancos `incidents`/`alerts` continuam existindo como estavam (zero risco, e o webhook do Google Forms continua criando alerta automático normalmente); a lista unificada (`IncidentAlertList.tsx`) mostra os dois juntos, ordenados por severidade, cada um resolvido pelo fluxo que já tinha (incidente pede texto de resolução; alerta só marca resolvido)
- [x] Criação de item novo virou um formulário só, sem perguntar "Incidente ou Alerta?" — título, cliente, severidade, categoria e descrição (nova coluna `incidents.description`); todo item novo nasce como incidente (é o formato cujo status já bate com "aberto/em andamento/resolvido")
- [x] Menu lateral: item "Alertas" removido, "Incidentes" renomeado pra "Incidentes e Alertas"; link antigo `/alerts` redireciona pra `/incidents`; `/status` continua mostrando a mesma tela
- [x] Testado ao vivo: itens antigos de `incidents` e `alerts` aparecendo juntos, ordenados por severidade, com os botões de resolver certos pra cada um; menu e redirecionamento conferidos
- [ ] Pendente: rodar `migration-023-fase656-mesclar-incidentes-alertas.sql` — sem isso, criar um item novo dá erro (falta a coluna `description`); o resto (ver a lista mesclada, resolver, menu) já funciona igual

**Fase 6.5 fechada.**

## Fase 6.6 — ajustes pontuais pós Fase 6.5
- [x] Login: mensagem errada ao tentar entrar sem internet — antes mostrava "e-mail ou senha incorretos" mesmo estando offline; agora `AuthContext` diferencia falha de rede (sem resposta do servidor) de credencial inválida (resposta 400 real) e a tela de login mostra "Sem conexão com a internet..." quando for o caso
- [x] "Tarefas do Cliente" (Portal Gestor): confirmado ao vivo que o acinzentamento de tarefa atrasada e a ordenação por proximidade do prazo (mais perto no topo, mais longe embaixo, sem prazo por último) já estavam implementados desde a Fase 6.5.3/5.7 — a única lacuna real era o ano não aparecer na data; corrigido reaproveitando o formatador que já tinha ano (usado nas Metas SMART)
- [x] Bug de fuso horário corrigido em `src/lib/format.ts` (afetava o app inteiro, não só tarefas): datas tipo "AAAA-MM-DD" eram interpretadas como meia-noite UTC, que em qualquer fuso do Brasil (todos atrás de UTC) volta pro dia anterior ao exibir — encontrado ao testar o item acima (uma tarefa com prazo hoje aparecia acinzentada 1 dia adiantada). Corrigido fixando meio-dia local ao interpretar datas sem horário; não afeta datas com horário (reuniões, criado em, etc.), só o cálculo de exibição de dia
- [x] Integrações por cliente, não por projeto (Fase 6.6.1): ao conectar Google Ads/Meta Ads não pede mais projeto — a conexão fica só ligada ao Ativo Digital/cliente; `performance_snapshots.project_id` virou opcional e a chave de sincronização passou a ser `client_id + channel + snapshot_date`; nada mudou nas telas do portal do cliente (já liam só por `client_id`). Testado ao vivo: diálogo de conectar não pede mais projeto, tela "Integrações" não mostra mais "Projeto: X", Performance do cliente continua carregando normal
- [ ] Pendente: rodar `migration-024-fase66-integracoes-por-cliente.sql` — sem isso, sincronizar uma conexão ainda vai falhar (banco ainda exige `project_id` preenchido); conectar (OAuth) já funciona normalmente porque não depende dessa coluna
- [x] "Onboarding" virou "Atividades" (Fase 6.6.2): a aba antiga (checklist manual solto) foi substituída por checklists gerados a partir dos Workflows do Kanban. Um Workflow do Kanban (`workflow_templates`) agora pode carregar um ou mais "Workflows de Atividades" (checklists reaproveitáveis de texto simples, nova aba em Workflows) — ao aplicar o Workflow, além das tarefas de sempre, os itens do(s) checklist(s) vinculado(s) nascem na aba Atividades, pro cliente/projeto certos. Um Workflow de Atividades pode ser marcado como "padrão": todo cliente novo já nasce com esse checklist aplicado sozinho (trigger no banco, roda pra qualquer cliente, não só os que nascem em "onboarding")
- [x] Central de Informações do Cliente ganhou um card "Atividades" mostrando os itens desse cliente
- [x] Checklist de onboarding que o próprio cliente via em "Meu Projeto" foi removido (a página que o alimentava não existe mais) — nada do resto do portal do cliente mudou
- [x] Testado ao vivo: menu renomeado, `/onboarding` redireciona pra `/activities`, 3ª aba "Workflows de Atividades" aparece em Workflows, página Atividades e card na Central de Informações renderizam sem erro mesmo antes da migração rodar
- [x] `migration-025-fase66-atividades.sql` rodada pelo usuário — confirmado ao vivo (Workflow de Atividades real aparecendo na aba)
- [x] Card de Workflow de Atividades passa a mostrar quais Workflows Operacionais estão vinculados a ele ("Vinculado a: X, Y") — calculado a partir do que já existe (`activity_template_ids` de cada Workflow Operacional), sem precisar de nada novo no banco

## Fase 7 — C.A.S.S.I.E. (IA) e testes finais (provisório, detalhar quando chegar a vez)
- [x] Aprovação automática por atraso: arquivo enviado pra aprovação do cliente (aba "Aprovações") que fica 48h em "Pendente" sem resposta é aprovado sozinho, com o status mostrando "Aprovado por atraso" (cor âmbar, pra diferenciar de uma aprovação real do cliente) tanto no portal do cliente quanto no do gestor. Job agendado (`pg_cron`, a cada 30 min) roda `auto_approve_overdue_approvals()`, que replica exatamente o que já acontece numa aprovação manual (também cria a entrada correspondente em "Arquivos"). Não mexe na aba "Arquivos" em si (ela não tem fluxo de aprovação próprio) nem em `respond_to_approval` (aprovação manual continua igual). Testado ao vivo com uma aprovação simulada: badge aparece certo nos dois portais, arquivo aparece em "Arquivos"
- [x] Cassie IA (Fase 7.1) — chat no portal do cliente (`/cassie`, antes só um placeholder), via API da OpenAI (o usuário já tinha essa chave, decidiu não pagar duas assinaturas de IA diferentes). Modelo `gpt-5-mini`. Edge Function nova (rota `/chat`) autentica o cliente, monta um resumo dos dados reais dele (plano, health score, projetos com cpa/roas/ctr/spend/revenue, tarefas em aberto/atrasadas, métricas dos últimos 30 dias) e manda isso + o histórico da conversa (`cassie_messages`, nova tabela) pra API da OpenAI (Responses API — confirmado nome do endpoint/modelo/preço direto na documentação oficial deles). Só no portal do cliente, nada mudou no portal do gestor
- [x] **Testado ao vivo de ponta a ponta e funcionando**: mandei uma mensagem de teste pelo portal do cliente e a Cassie respondeu citando dados reais (tarefas em aberto/atrasadas certas, avisou corretamente que não há dados de desempenho sincronizados). Conversa persiste depois de recarregar a página
- [x] Dois ajustes feitos durante o teste, por causa de como a função foi criada no painel (não são erros do código original, são específicos de como as coisas foram nomeadas no Supabase): o nome da Edge Function ficou `CASSIE` (maiúsculo, sem hífen) em vez de `cassie`, e o segredo ficou `Openai_api_key` em vez de `OPENAI_API_KEY` (o painel do Supabase não deixa renomear um segredo depois de criado) — o código em `src/lib/cassie.ts` e `supabase/functions/cassie/index.ts` já reflete os nomes reais usados
- [x] **Retrabalho da Cassie (Fase 7.1.1)**, depois de o usuário apontar que existia um arquivo de especificação completo ("Prompt - fase 7", na raiz) que não tinha sido lido antes da primeira implementação. Mudanças:
  - **4 modos**: Assistente (dúvidas gerais), Analista (métricas CPA/ROAS/CTR/conversões), Consultora (estratégia/funil), Auditora (conformidade/riscos) — cada resposta da Cassie fica marcada com o modo em que foi respondida
  - **Liberação por plano**: Validação → 1 modo, Escala → 2, Dominação → 3, admin/gestor → sempre os 4 (checado no servidor, não só escondido na tela)
  - **Acesso do gestor**: novo card "Cassie IA" na Central de Informações do Cliente (`/clients/:id`) — conversa própria do gestor sobre aquele cliente, **separada** da conversa que o cliente tem (cada conversa é isolada por quem está perguntando, não só pelo cliente sendo discutido — coluna nova `conversation_owner_id`)
  - Conversa continua **uma por cliente** (não por projeto, como o documento original sugeria) — decisão do usuário, a Cassie puxa dados de todos os projetos do cliente quando precisar
  - Contexto mais rico: reuniões, metas SMART, comentários e tarefas concluídas entraram na lista (antes só tinha projetos e tarefas em aberto); conversões agora são calculadas (gasto ÷ CPA, mesma fórmula do dashboard do cliente) — resolve o caso relatado de a Cassie não saber responder sobre conversões
  - Resposta educada e fixa quando a pergunta foge do escopo (marketing/desempenho/gestão da conta), com aviso visual (borda âmbar) na bolha
  - Botão "Limpar Histórico" (com confirmação)
  - Indicador de "pensando" **animado** (pontinhos) no lugar do texto parado "Cassie está digitando...", e a mensagem do usuário agora aparece **na hora** que ele manda, sem esperar a resposta completa (testado ao vivo, confirmado nos dois pontos)
  - `migration-028-fase7-cassie-modos.sql` (nova) — apaga as mensagens de teste existentes (sem valor pra manter, combinado com o usuário) e ajusta a tabela `cassie_messages`
- [x] `migration-028` rodada e função `CASSIE` reimplantada pelo usuário — **testado ao vivo de ponta a ponta**: cliente de teste só via o modo "Assistente" (plano não definido = 1 modo só, liberação por plano funcionando); pergunta sobre conversões respondida com número real (0, cálculo correto pros dados desse cliente); pergunta fora de escopo ("qual a capital da França?") recebeu a resposta fixa de encaminhamento, com aviso visual âmbar; "Limpar Histórico" apagou a conversa de verdade; no portal do gestor, o card da Cassie na Central de Informações mostrou os 4 modos sempre liberados, e a mensagem mandada por ali **não apareceu** na conversa do cliente (conversas separadas confirmadas)
- [x] Função `integrations` também reimplantada pelo usuário — testado ao vivo: diálogo de conectar não pede mais projeto, e o erro mudou de "escolha o projeto" pra um aviso de credencial do Google não configurada (esperado, é outra pendência, não bug)
- [x] **Health Score real**: antes era só um número digitado manualmente (e as 4 sub-notas — Desempenho/Financeiro/Entrega/Relacionamento — nunca tinham sido calculadas). Modelo: cada sub-nota começa em 100 e perde pontos por sinal negativo real (mesmo raciocínio de `src/lib/client-risk.ts`, que já marca "Cliente em risco"); sem dado nenhum numa categoria, ela fica "sem dados" em vez de inventar número
  - Desempenho: média das metas SMART (por status + atraso) e tendência de ROAS (compara os últimos 15 dias com os 15 anteriores, sem depender de um valor "bom" fixo)
  - Financeiro: status do cliente (ativo/onboarding/pausado/churned) + janela de risco de renovação + ROI recente
  - Entrega: tarefas atrasadas + % de itens concluídos nas Atividades
  - Relacionamento: aprovações "por atraso", reuniões canceladas, incidentes/alertas graves em aberto
  - `migration-029-fase7-health-score-real.sql` (nova) — funções `recompute_client_health_score(client_id)` e `recompute_all_client_health_scores()`, agendada 1x por dia via `pg_cron`, com backfill de todo mundo já dentro da própria migração
  - Botão "Recalcular agora" na Central de Informações do Cliente, pra não precisar esperar o cron durante teste
  - [x] `migration-029` rodada pelo usuário — **testado ao vivo e conferido na mão, número por número**: cliente "Studio Prisma" (onboarding, sem metas, 1 tarefa atrasada, sem itens de atividade, renovação dentro de 15 dias, sem incidentes/alertas/aprovações por atraso) mostrou Desempenho "—" (sem meta e sem dado de desempenho — correto), Financeiro 70 (90 de onboarding − 20 da janela de renovação — correto), Entrega 85 (100 − 15 da tarefa atrasada — correto), Relacionamento 100 (sem sinal negativo — correto), e Health Score geral 85 (média exata dos 3 com dado, ignorando o "—" — correto). Botão "Recalcular agora" testado na Central de Informações do Cliente, sem erro. Lista de clientes passou a mostrar notas bem diferentes entre si (55, 57, 85, 50), condizentes com quem está "Em problemas"/"Em risco" e quem não está
- [x] Logo nova (cristais roxos) substituindo o ícone genérico "Gem" e a logo antiga em forma de raio: favicon do navegador (`public/logo.png`, `index.html`), cabeçalho da barra lateral (`Sidebar.tsx`) e tela de login/cadastro (`AuthLayout.tsx`) — `public/favicon.svg` (a logo antiga) removido por não ter mais uso. Testado ao vivo: favicon carrega (200), logo aparece certa na tela de login e na barra lateral
- [x] **Testes automatizados (Fase 7.3)**:
  - Descoberta ao investigar: "pedir revisão" numa aprovação nunca gerou tarefa automática pra agência (não existia em nenhuma versão de `respond_to_approval`, nem no código nem em nenhuma migração antiga) — implementado agora em `migration-030-fase73-tarefa-revisao.sql`: pedir revisão cria uma tarefa (`Revisar: <título>`, prioridade alta) e um registro em `audit_logs`, mesmo padrão de rastreabilidade que `apply_workflow` já usa
  - "Aplicar workflow cria as tarefas certas" é lógica 100% em SQL (`apply_workflow`) — sem Postgres local nem Supabase CLI configurados neste projeto, testar isso de verdade exigiria montar infraestrutura nova. Em vez disso virou teste **Playwright de ponta a ponta**, rodando contra o Supabase de teste de verdade
  - Vitest instalado (`vitest`, `jsdom`, `@testing-library/react` + `jest-dom` + `user-event`) — `npm run test`. 4 arquivos, **25 testes, todos passando**: `RoleRoute.test.tsx` (pedido explícito da especificação — cliente bloqueado de rota do gestor), `client-risk.test.ts`, `metrics.test.ts` (cálculo de conversões), `cassie-modes.test.ts` (liberação de modo por plano)
  - Critério de aceite da própria especificação conferido: desliguei a checagem do RBAC de propósito (`if (false)`), rodei os testes e vi o teste do RoleRoute falhar certinho; desfiz e voltou a passar
  - Playwright configurado (`playwright.config.ts`, sobe o `npm run dev` sozinho e reaproveita se já tiver rodando) — `npm run test:e2e`. **3 testes, todos passando**: login→navegação→logout, aplicar workflow cria as tarefas certas (corrigido depois de descobrir ao vivo que criar Workflow Operacional é ação exclusiva de admin — gestor só lê e aplica), e pedir revisão gera a tarefa pra agência automaticamente
  - [x] `migration-030` rodada pelo usuário — reconferido ao vivo, os 3 testes Playwright passam de ponta a ponta
  - **Achado à parte, fora do escopo do pedido**: `npx tsc --noEmit` (usado a sessão inteira como conferência rápida) é um **no-op silencioso** — o `tsconfig.json` da raiz só tem `references`, sem `files`/`include`, então rodar `tsc` sem `-b` não checa nada de verdade (confirmado inserindo um erro de tipo de propósito e vendo o comando não acusar nada). O comando certo é `npx tsc -b --noEmit` (o `npm run build` já usava `-b` corretamente, então isso nunca vazou pra produção) — importante porque toda vez que relatei "tsc limpo" nas rodadas anteriores dessa sessão, essa checagem pode não ter sido real
- [x] **Dois bugs do chat da Cassie corrigidos** (reportados pelo usuário depois dos testes automatizados), afetando tanto o portal do cliente quanto o card de Cassie do gestor em `/clients/:id`. Causa raiz dos dois: a página da Cassie é desmontada pelo React Router ao trocar de aba, o que apagava qualquer estado local (`useState`) guardado nela
  - Texto digitado e não enviado sumia ao trocar de aba e voltar — agora persiste em `localStorage` (chaveado por usuário + cliente, novo hook `useCassieDraft`), sobrevive à navegação e é limpo automaticamente depois que a mensagem é enviada
  - Indicador de "pensando" desaparecia ao trocar de aba e voltar mesmo com a Cassie ainda processando a resposta de verdade em segundo plano — agora consulta o cache global de mutações do TanStack Query (`useMutationState`, novo hook `useCassieSending`) em vez do estado local do componente, então reflete certo o que está realmente pendente, não importa se a tela foi remontada no meio do processo
  - Testado ao vivo com Playwright: rascunho sobrevive à troca de aba; indicador continua visível ao voltar pra tela no meio de uma resposta (atrasada de propósito pra dar tempo de navegar) e some certo quando a resposta real chega, com a mensagem persistida normalmente na conversa
- [x] **Aba "Cassie IA" no Portal do Gestor**: até então a Cassie do gestor só existia embutida na Central de Informações de um cliente (`/clients/:id`) — não tinha item no menu nem página própria, por isso não aparecia. Nova página `src/pages/admin/ManagerCassie.tsx` em `/client-cassie` (mesmo padrão de "Comentários"/"Reuniões"/"Arquivos": seletor de cliente no topo, mostra a conversa daquele cliente ao escolher), reaproveitando os mesmos componentes/hooks já usados no portal do cliente e no card da Central de Informações — é a mesma conversa por trás dos dois lugares, não uma nova. O card embutido em `ClientDetail.tsx` continua existindo também. Testado ao vivo: item aparece no menu do gestor, seleção de cliente carrega o histórico certo, envio de mensagem funciona, e a rota continua bloqueada pra quem é `cliente`
- [x] **Fase 7.4 — Revisão geral e ajustes finais**: auditoria completa dos dois portais nos 3 critérios da especificação original
  - **Estados vazios**: conferi na mão todos os componentes de lista compartilhados (tarefas, arquivos, reuniões, incidentes, timeline, comentários, aprovações, kanban) e as páginas com grade (clientes, metas SMART, workflows) nos dois portais — todos já mostravam mensagem "Nenhum/Nenhuma..." adequada, nenhum ficava em branco ou quebrado sem dado
  - **Texto de exemplo esquecido**: auditoria estática (grep) + varredura ao vivo de todas as ~25 rotas reais dos dois portais não encontrou nenhum texto placeholder/mock esquecido. O componente genérico `PlaceholderPage` (usado nas fases iniciais) não é mais renderizado por nenhuma rota — todo item de menu já mapeia pra uma página real
  - **Responsividade** (iPad Air ~820px, Zenbook Fold ~853px, varredura visual das ~25 rotas nos dois portais): achado 1 bug real — na Central de Informações do Cliente (`/clients/:id`), a linha do "Health Score" com o botão "Recalcular agora" causava overflow horizontal de 16px em 820px. Corrigido deixando a linha quebrar em duas (`flex-wrap`), mesmo padrão já usado em outras listas do app. Confirmado ao vivo: sem overflow depois da correção
  - **Acessibilidade** (varredura leve, não é auditoria formal de WCAG): 6 botões só-com-ícone sem `aria-label` (então leitor de tela não anunciava nada) — botão de editar em `AssetCard.tsx`, `KanbanCard.tsx` e `ManagerTaskRow.tsx`, e botão de remover etapa/item nos 3 formulários de Workflow (`WorkflowTemplateFormDialog.tsx`, `ActivityTemplateFormDialog.tsx`, `ClientWorkflowTemplateFormDialog.tsx`). Corrigidos seguindo o mesmo padrão que outros botões de editar do app já usavam (`aria-label={`Editar ${nome}`}` etc.)
  - `npx tsc -b --noEmit`, `npm run test` (25/25) e `npm run test:e2e` (3/3) limpos depois das correções
- [x] **Bug: sidebar e topbar sumiam ao abrir qualquer Select/Dialog/Dropdown numa página rolada** — reportado pelo usuário ao trocar o modo da Cassie na Central de Informações. Causa raiz: `Sidebar` e `TopBar` usavam `position: sticky` relativo ao scroll do documento inteiro; o Radix (biblioteca por trás de todo Select/Dialog/Dropdown do app) trava o scroll do `body` enquanto qualquer um desses componentes está aberto, e isso quebra `position: sticky` (mesma classe de bug que um comentário antigo em `index.css` já registrava para outro cenário). Não era só um problema da Cassie — qualquer página comprida com qualquer Select/Dialog aberto sofria disso
  - Corrigido na raiz (`AppLayout.tsx`, `Sidebar.tsx`, `TopBar.tsx`): o documento inteiro não rola mais — só o `<main>` rola de forma independente, e Sidebar/TopBar ficam fora do fluxo de scroll (sem precisar de `sticky`). Padrão comum de "app shell" (sidebar/topbar fixos, conteúdo com scroll próprio)
  - Testado ao vivo: sidebar/topbar não somem mais ao abrir o seletor de modo da Cassie numa página rolada; menu retrátil do celular abre/fecha normalmente; página em 820px sem scroll no documento (só dentro do `<main>`), sem overflow horizontal
- [x] **Preparar o app para publicação (deploy)** — feito depois da Fase 9b/8.5, ver Fase 10 abaixo

## Fase 8 — Públicos-Alvo e Comunicação Persuasiva via IA (a partir de respostas de formulário)

Pedido pelo usuário depois da Fase 7 fechada. Dividida em sub-fases, uma de cada vez:
- **8.1 — Detalhamento de Projeto/Campanha** (fechada, ver abaixo)
- **8.2 — Sincronização estruturada do Google Forms** (fechada, testada com conexão real, ver abaixo)
- **8.3 — Aba "Públicos-Alvo"** (fechada, ver abaixo)
- **8.3b — Separar por formulário, busca, e correção do cache pós-sincronização** (fechada, ver abaixo)
- **8.4 — Comunicação Persuasiva** (fechada, ver abaixo)
- **8.4b — Chat de refinamento + persistir sugestões** (fechada, testada ao vivo de ponta a ponta, ver abaixo)
- **8.5 — Seção "Público-Alvo" na Central de Informações do Cliente** (fechada, ver abaixo)

### 8.1 — Detalhamento de Projeto/Campanha
- [x] Cada projeto na Central de Informações agora é clicável, abrindo `ProjectDetailDialog` (`src/components/project/ProjectDetailDialog.tsx`) com 3 abas: Visão Geral (status/CPA/ROAS/CTR/gasto/receita/período — dados que já existiam no banco mas não apareciam em lugar nenhum do lado do gestor), Tarefas (reaproveita `TaskList`, filtrado por projeto) e Campanha (editável: público-alvo, segmentações, objetivo, sistemas, outras informações)
- [x] Segmentações: lista curada de referência (`src/lib/segmentation-options.ts`), agrupada por Meta Ads/Google Ads — não é uma integração ao vivo com as APIs deles, só padroniza como o gestor documenta a segmentação
- [x] `migration-031-fase8-projeto-campanha.sql` (nova) — acrescenta `segmentations text[]` e `systems text` em `projects` (público-alvo/objetivo/outras informações reaproveitam `icp`/`objective`/`description`, que já existiam)
- [x] `npx tsc -b --noEmit` e `npm run test` (25/25) limpos
- [x] `migration-031` rodada pelo usuário — **testado ao vivo de ponta a ponta**: card "Projetos" carrega normal com as colunas novas; abrir um projeto mostra as 3 abas certas (Visão Geral com CPA/ROAS/CTR/gasto/receita/período, Tarefas filtradas certas, Campanha editável); preencher público-alvo e marcar segmentações, salvar, recarregar a página e reabrir confirma que tudo persistiu certo
- [x] **Ajuste pedido pelo usuário: gestão de tarefas na aba "Tarefas"** — antes era só leitura (`TaskList`); agora reaproveita `ManagerTaskRow` (dropdown de status, editar, apagar) + botão "Nova tarefa" (`KanbanTaskFormDialog`, já vem preenchido com o cliente/projeto certos) — mesmo padrão já usado em "Tarefas do Cliente". **Testado ao vivo de ponta a ponta**: criar tarefa (cliente/projeto pré-preenchidos confirmados), mudar status pelo dropdown, editar (campos pré-preenchidos certos), apagar — tudo refletindo também em "Tarefas do Cliente" (mesma fonte de dados)
- [x] **8.1b — Vínculo com campanha real (Meta Ads/Google Ads)**: pedido pelo usuário — projeto passa a poder apontar pra uma campanha específica de uma conta já conectada (opcional, não obrigatório); quando vinculado, CPA/CTR/gasto do projeto passam a vir só daquela campanha (últimos 30 dias) — ROAS/receita continuam manuais, os provedores de anúncio não reportam receita nessa sincronização. As métricas por cliente (Central de Informações/Performance) continuam agregadas como já eram, sem mudança
  - `migration-032-fase8-campanha-vinculada.sql` — `projects` ganha `external_connection_id`/`external_campaign_id`/`external_campaign_name`; tabela nova `campaign_performance_snapshots` (mesmo espírito de `performance_snapshots`, mas por campanha). Rodada pelo usuário, confirmada ao vivo: colunas/tabela existem, `useAllProjects` carrega normal
  - Edge Function `integrations`: rota nova `GET /campaigns?connection_id=...` lista as campanhas reais de uma conta conectada; `syncConnection` passa a gravar também o detalhamento por campanha (sem mudar o agregado por conta que já existia)
  - `CampaignLinkField` (`src/components/project/CampaignLinkField.tsx`, novo) — escolhe a conta conectada e a campanha, reaproveitado em `NewProjectDialog` (criação) e na aba Campanha de `ProjectDetailDialog` (edição). **Testado ao vivo**: com nenhum cliente tendo integração conectada de verdade ainda, o campo corretamente não aparece nada; criar projeto e salvar a aba Campanha continuam funcionando normal com as colunas novas
  - [x] Edge Function `integrations` reimplantada pelo usuário — **testado ao vivo direto na rota nova**: `/campaigns` agora responde com erro de domínio ("Conexão não encontrada") em vez da lista de rotas antiga, confirmando que o código novo está no ar; testado também com uma conexão real mas desconectada, e a validação de conta de anúncios incompleta respondeu certo ("Conexão incompleta (falta conta de anúncios)"), sem travar nem devolver erro genérico
  - **Limite conhecido**: buscar campanhas de verdade e sincronizar métricas por campanha só funciona com credenciais reais do Google Ads/Meta Ads configuradas (mesmo segredo da Fase 6) — nenhum cliente de teste tem isso configurado ainda, então `CampaignLinkField` continua sem aparecer pra nenhum cliente até existir pelo menos uma conexão com status "connected" de verdade
  - `npx tsc -b --noEmit`, `npm run test` (25/25) e `npm run test:e2e` (3/3) limpos

### 8.2 — Sincronização estruturada do Google Forms
- [x] Até aqui, uma resposta de formulário só virava um Alerta genérico com o texto colado (`"Pergunta: Resposta"` por linha) — sem nenhuma tabela guardando pergunta/resposta de verdade. Achado interessante ao investigar: conectar um Google Forms já pedia os escopos certos da API oficial (`forms.body.readonly`/`forms.responses.readonly`) e já guardava o token OAuth desde a Fase 6.2 — só nunca tinha sido usado pra nada. A sincronização estruturada nova usa esse token pra chamar a API de verdade (`forms.googleapis.com`), trazendo pergunta por pergunta (id, título, tipo, opções) e resposta por resposta (inclusive **respostas antigas**, de antes da conexão existir — coisa que o gatilho do Apps Script nunca conseguia trazer, só via as novas dali pra frente)
  - `migration-033-fase82-forms-estruturado.sql` (nova) — 3 tabelas: `form_questions`, `form_responses`, `form_answers` (mesmo padrão de RLS de `campaign_performance_snapshots`: só leitura admin/gestor, quem escreve é a Edge Function)
  - Edge Function `integrations`: nova `syncFormsConnection` — busca a estrutura do formulário (`GET /v1/forms/{formId}`) e todas as respostas paginadas (`GET /v1/forms/{formId}/responses`), normaliza os tipos de pergunta (texto curto/parágrafo, múltipla escolha, caixa de seleção, lista suspensa, escala, data, hora, upload de arquivo, e linha de grade como melhor esforço) e grava tudo. `/sync` (botão "Sincronizar agora") e `/sync-all` (cron a cada 6h) passam a despachar pro Google Forms também, sem mudar nada do caminho de Google Ads/Meta Ads que já funciona
  - **Nada mudou** em `/connect`, `/callback`, `/forms-webhook` (o Alerta genérico continua sendo criado exatamente igual) nem no `forms-trigger.gs.txt` — quem já tem o Apps Script instalado num formulário do cliente não precisa reinstalar nada
  - Frontend: página Integrações ganhou o botão "Sincronizar agora" também pras conexões de Google Forms (antes só Ads/Meta), e um botão novo "Ver respostas" (`FormResponsesDialog`) que mostra as respostas mais recentes já com o título real da pergunta — só uma vitrine pra confirmar que a estrutura sincronizou certo; a síntese em % das perguntas fechadas fica pra Fase 8.3
  - `npx tsc -b --noEmit`, `npm run test` (25/25) e `npm run test:e2e` (3/3) limpos. Testado ao vivo o que dava pra testar sem migration/credencial real: página Integrações carrega normal, sem erro no console, e os botões novos corretamente não aparecem pra nenhuma conexão (todas as conexões de teste ainda estão "Desconectado")
  - [x] `migration-033` rodada e Edge Function `integrations` reimplantada pelo usuário; API "Google Forms API" já habilitada no Google Cloud Console — **testado ao vivo direto nas rotas**: as 3 tabelas novas existem e estão vazias; chamar `/sync` numa conexão de Forms sem id de formulário devolve o erro certo ("falta o id do formulário"); chamar numa conexão com id de formulário mas sem OAuth concluído devolve "Não foi possível obter um token de acesso válido" — confirma que o despacho por provider e a validação passo a passo estão certos, chegando exatamente até o ponto que só falta a credencial OAuth de verdade pra passar
  - **8.2b — Nomes reais dos secrets do Google**: o usuário já tinha criado os 3 secrets do Google no painel do Supabase, só que com nomes diferentes do documentado no topo do arquivo (`client ID`, `Client secret`, `Developer token`, em vez de `GOOGLE_OAUTH_CLIENT_ID`/`GOOGLE_OAUTH_CLIENT_SECRET`/`GOOGLE_ADS_DEVELOPER_TOKEN`) — confirmado ao vivo chamando `/connect` de verdade (voltou o erro "falta o segredo GOOGLE_OAUTH_CLIENT_ID" mesmo com tudo criado). Mesma situação já resolvida assim pra Cassie na Fase 7.1 (painel não deixa renomear um secret depois de criado): o código passou a ler pelos nomes reais (`GOOGLE_OAUTH_CLIENT_ID_ENV = 'client ID'` etc., logo no topo do arquivo), em vez de pedir pra recriar os 3 secrets
  - [x] Depuração ao vivo do primeiro OAuth real, junto com o usuário — 3 problemas encontrados e resolvidos, nenhum no nosso código: (1) o valor guardado em `client ID` era na verdade o **número do projeto** do Google Cloud, não o Client ID de verdade (achado comparando o `client_id` devolvido por `/connect` com o formato esperado, que termina em `.apps.googleusercontent.com`); (2) a verificação automática de JWT do Supabase (que precisa ficar desligada pra essa função, ver comentário no topo do arquivo) tinha voltado a ficar ligada depois de um redeploy, bloqueando `/callback` antes mesmo de chegar no nosso código (erro `UNAUTHORIZED_NO_AUTH_HEADER`); (3) o `Client secret` salvo não batia com o Client ID em uso — resolvido gerando um novo secret na credencial certa do Google Cloud
  - [x] **Conexão real confirmada + sincronização estruturada testada ao vivo com um formulário de verdade** (formulário de captação de leads de uma clínica de terapia, criado pelo usuário pra teste): `/sync` devolveu `{"ok":true,"syncedQuestions":23,"syncedResponses":0}` — 23 perguntas reais vieram da API do Google Forms com título/tipo/opções certos (múltipla escolha, caixa de seleção, texto curto, texto longo), gravadas em `form_questions`; `syncedResponses: 0` correto porque o formulário de teste ainda não tinha nenhuma resposta enviada
  - **Nota à parte, não bloqueia nada**: a tela de permissão do Google mostra o domínio técnico do Supabase em vez de "Ametista Conversões", mesmo com o nome do app e os escopos configurados certos no Google Cloud — a causa mais provável (confirmada em documentação oficial do Google) é que falta um passo específico de "verificação de marca" (o usuário não achou o botão na página Branding; pode estar em "Central de verificação", ainda não confirmado) — não impede conectar nem sincronizar, só deixa a tela de permissão com uma cara mais técnica por enquanto
  - [x] **8.2c — Página feia de "Conectado"/"Erro ao conectar" depois do OAuth**: reportado pelo usuário durante os testes — a página que a Edge Function mostrava ao voltar do Google/Meta aparecia com o código HTML cru na tela, em vez de renderizada. Investigando com `curl -D -` direto na rota, achei a causa: o **Supabase força `Content-Type: text/plain` (+ `nosniff`)** em respostas de rotas de Edge Function chamadas sem autenticação (é o caso do `/callback`, aberto pelo navegador vindo do Google/Meta, sem login nosso) — não é algo que dava pra contornar só ajustando o header na nossa função
    - `/callback` trocou de devolver uma página HTML pra devolver um **redirect HTTP de verdade (302)** direto pro nosso próprio app (`/assets?integration=connected|error&message=...`) — `handleCallback` (Edge Function `integrations`) ganhou `callbackRedirect`, substituindo `htmlResponse`/`callbackLandingPage`
    - `Assets.tsx` lê esses parâmetros ao carregar e mostra um `toast` (mesmo `sonner` já usado no resto do app) de sucesso ou erro, depois limpa a URL — sem componente novo, sem dependência nova
    - Testado ao vivo com Playwright simulando a volta do redirect: os dois toasts (sucesso e erro) aparecem certos, com acentuação certa, ícone/cor certos, e a URL fica limpa depois — confirmado por screenshot
    - `npx tsc -b --noEmit`, `npm run test` (25/25) e `npm run test:e2e` (3/3) limpos
    - Edge Function reimplantada e **confirmada ao vivo**: `curl -D -` direto na rota devolveu `302 Found` com `Location: .../assets?integration=error&message=...` (corpo vazio) em vez do HTML antigo — o redirect está funcionando de verdade no ambiente do usuário
  - **Nota adiada pelo usuário**: o problema da marca do Google ainda mostrando o domínio técnico (em vez de "Ametista Conversões") continua sem solução — ele conferiu a página Branding e não achou o botão de verificação. Decisão do usuário: só resolver isso quando o app estiver perto de terminar, não é prioridade agora

### 8.3 — Aba "Públicos-Alvo" (síntese em % das perguntas fechadas)
- [x] Nova página `/audiences` no Portal Gestor ("Públicos-Alvo" no menu, ícone `UsersRound`) — escolhe um cliente e mostra um card por pergunta **fechada** (múltipla escolha, caixa de seleção, lista suspensa, escala, linha de grade) de todos os Google Forms conectados daquele cliente, com um gráfico de barras horizontais mostrando a % de cada opção. Perguntas de texto livre ficam de fora (isso é a Fase 8.4)
- [x] **100% dinâmico, nada fixo por formulário** (pedido explícito do usuário): a página não sabe nada sobre nenhum formulário específico — junta as perguntas fechadas de quantos Google Forms o cliente tiver conectado (0, 1 ou vários), na ordem original de cada formulário; se não tiver nenhum conectado, mostra uma mensagem direcionando pra "Integrações" em vez de aparecer vazio sem explicação
- [x] Cálculo em `src/lib/audience-insights.ts` (`aggregateAudienceInsights`, função pura, sem depender do Supabase) — pra cada pergunta, conta quantas respostas distintas a responderam e quantas vezes cada valor de opção apareceu; pergunta de caixa de seleção pode somar mais de 100% (uma resposta conta em mais de uma opção), comportamento esperado, não é bug
- [x] **Testado com 7 testes automatizados novos** (`audience-insights.test.ts`) cobrindo exatamente o que o usuário pediu pra garantir: pergunta sem resposta, escolha única somando 100%, caixa de seleção somando mais de 100%, resposta sem `answer_values` caindo pro `answer_text`, resposta vazia não contando como respondente, dois formulários diferentes com o mesmo id de pergunta não se misturando, e uma lista de N perguntas sem limite fixo — não dava pra inserir resposta de teste direto no banco pra conferir visualmente (as tabelas de resposta só aceitam escrita da Edge Function, por design da Fase 8.2), então a matemática foi garantida por teste automatizado em vez de dado fabricado à mão
- [x] Gráfico novo `src/components/charts/OptionBreakdownBarChart.tsx` (barras horizontais, mesmo padrão visual de `SpendRevenueBarChart.tsx` — cores/grade/tooltip do `chart-colors.ts`, que ganhou a entrada `respostas`) — horizontal porque os rótulos das opções são frases inteiras, não cabem em rótulo vertical; altura dinâmica conforme o número de opções
- [x] Testado ao vivo como gestor: item "Públicos-Alvo" aparece no menu; cliente com o Google Forms real conectado (Studio Prisma, 23 perguntas sincronizadas, 15 fechadas) mostra as 15 perguntas certas, cada uma "Ainda sem respostas" (nenhuma resposta de verdade ainda); cliente sem nenhum Google Forms conectado mostra a mensagem direcionando pra Integrações; nenhum cliente escolhido mostra a mensagem pedindo pra escolher
- [x] `npx tsc -b --noEmit`, `npm run test` (32/32) e `npm run test:e2e` (3/3) limpos
- [x] **Resolvido em 8.3b**: gráfico com dado de verdade conferido ao vivo (ver abaixo)

### 8.3b — Separar perguntas por formulário, busca, e correção do "não atualizou" pós-sincronização
- [x] **Diagnóstico do relato do usuário** ("respondi o formulário com outro e-mail e não atualizou no app, mesmo aparecendo no Google Forms"): disparei `/sync` manualmente pra conexão real do Studio Prisma e a resposta nova veio certa (`{"ok":true,"syncedQuestions":23,"syncedResponses":1}`) — a sincronização em si não tem bug, ela é manual (botão "Sincronizar agora") ou a cada 6h via cron, nunca instantânea. **Bug de verdade encontrado**: depois de sincronizar, nenhuma query do React Query era invalidada — então uma tela já aberta (Públicos-Alvo, e até o "Última sincronização" da própria página Integrações) não refletia o dado novo sem recarregar a página. Corrigido em `Integrations.tsx` (`handleSync` agora invalida `digital-asset-connections`/`form-responses`/`audience-insights`) — **confirmado ao vivo**: clicar em "Sincronizar agora" atualiza o "Última sincronização" na tela na hora, sem F5 (de "20/08, 10:52" pra "20/08, 11:09" no teste)
- [x] **Separação por formulário** (pedido do usuário — um cliente pode ter mais de um Google Forms conectado com propósito diferente, ex: formulário de cliente, de objeção): cada pergunta de `useAudienceInsights` agora carrega também o `connectionId` de qual formulário ela veio (`audience-insights.ts`, sem precisar de query nova — `form_questions.connection_id` já existia); a página ganhou um seletor "Formulário" (mesmo estilo do seletor de cliente), que só aparece quando o cliente tem 2+ Google Forms conectados; com "Todos os formulários" selecionado, as perguntas aparecem agrupadas por seção com o nome do formulário
- [x] **Busca** (pedido do usuário, igual à que já existe em Workflows/Integrações): campo de busca filtrando as perguntas pelo título
- [x] **Bug encontrado e corrigido durante a conferência ao vivo com dado real**: o rótulo de % ao lado da barra (ex: "100,0%") aparecia cortado ("100") porque a margem direita do gráfico (`OptionBreakdownBarChart.tsx`) era pequena demais pra caber o texto antes da borda do SVG (o Recharts corta por padrão o que passa da área do gráfico) — aumentada de 24 pra 48px, resolvido
- [x] **Testado ao vivo com os 3 formulários reais do Studio Prisma** ("Google forms", "google forms 2", "google forms 3 (teste)"): seletor de Formulário aparece certo com as 3 opções; "Todos os formulários" agrupa por seção; escolher "google forms 2" mostra só as perguntas dele, incluindo a pergunta que recebeu a resposta de teste de hoje — **gráfico renderizando com % real pela primeira vez** ("Facebook" 100%, 1 resposta); busca sem resultado mostra a mensagem certa; cliente com só 1 formulário (Loja Aurora) corretamente não mostra o seletor de Formulário
- [x] 1 teste automatizado novo confirmando que `connectionId` chega certo por pergunta (33/33 no total)
- [x] `npx tsc -b --noEmit`, `npm run test` (33/33) e `npm run test:e2e` (3/3) limpos

### 8.4 — Comunicação Persuasiva (palavras mais frequentes + headlines/textos via IA)
- [x] Página `/audiences` ganhou uma segunda aba, "Comunicação Persuasiva", ao lado de "Perguntas Fechadas" (mesmo `Tabs` já usado em Workflows) — reaproveita os seletores de Cliente/Formulário já existentes no cabeçalho, não precisa escolher o cliente de novo
- [x] **Palavras mais frequentes**: função pura nova `extractWordFrequency` (`src/lib/word-frequency.ts`) — tokeniza as respostas de texto livre (perguntas `text_short`/`text_paragraph`), remove uma lista de stopwords do português, e conta cada palavra **1 vez por resposta** (não por ocorrência), pra `%` significar "% das respostas que usaram essa palavra" — mesma semântica já usada nas perguntas de caixa de seleção da Fase 8.3, evita que uma resposta repetitiva domine o resultado. Reaproveita o gráfico `OptionBreakdownBarChart` sem mudar nada nele (o formato de saída já bate). 7 testes automatizados novos
- [x] **Sugestões via IA**: rota nova `POST /persuasive-copy` na Edge Function da Cassie (`supabase/functions/cassie/index.ts`) — só admin/gestor; busca as respostas abertas reais do cliente (limitado a 50 mais recentes por chamada, custo do prompt) e pede pra OpenAI (mesmo `Openai_api_key`/`gpt-5-mini` já configurados desde a Fase 7.1) 5 headlines curtos + 3 textos persuasivos mais longos, usando a linguagem real do público. Botão "Gerar sugestões com IA" na aba nova (geração avulsa nessa primeira versão — **virou conversa persistida na 8.4b logo abaixo**, depois do uso real mostrar que isso incomodava)
- [x] Novo hook `useOpenTextAnswers` (`useManagerPortalData.ts`) busca as respostas abertas do cliente pro gráfico de palavras — mesmo padrão de `useAudienceInsights`, sem precisar de query nova no banco
- [x] `npx tsc -b --noEmit`, `npm run test` (40/40) limpos. `npm run test:e2e`: 2/3 — o único que falhou (`approval-revision-task.spec.ts`, sobre revisão de aprovação virando tarefa automática) **não tem nada a ver com essa fase**; confirmei isso guardando as mudanças de hoje num stash e rodando o mesmo teste sozinho no código de antes — ele falha igual, então é uma instabilidade que já existia (não investiguei a causa raiz, foge do escopo desta fase; fica registrado aqui pra não ficar escondido)
- [x] Testado ao vivo como gestor: aba "Comunicação Persuasiva" do Studio Prisma mostra o gráfico de palavras mais frequentes com dado real das respostas já sincronizadas (ex: "paz" em 50% das respostas); clicar "Gerar sugestões com IA" chama a rota nova certinho e mostra a mensagem de erro correta ("Rota não encontrada...") porque a Edge Function ainda não foi reimplantada pelo usuário com o código novo — confirma que a chamada e o tratamento de erro (toast) funcionam, falta só o redeploy pra funcionar de ponta a ponta; aba "Perguntas Fechadas" testada de novo depois de entrar dentro do `Tabs`, sem regressão (busca, seletor de formulário, agrupamento todos continuam certos)
- [x] **Resolvido em 8.4b**: reimplantado pelo usuário, testado ao vivo com resposta real da OpenAI (ver abaixo)

### 8.4b — Chat de refinamento + persistir as sugestões (não sumir ao navegar)
- [x] **Dois problemas relatados pelo usuário depois de usar de verdade**: (1) não dava pra pedir ajuste numa sugestão ("deixe a headline 3 mais direta"), só gerar do zero; (2) as sugestões viviam só em `useState` local — trocar de aba (as abas do Radix só mantêm montada a aba ativa) fazia elas sumirem, o que atrapalhava porque o uso real é copiar cada headline uma de cada vez
- [x] **Um bug de verdade encontrado no meio do caminho, antes de qualquer mudança de código**: o usuário reimplantou a Edge Function e o botão passou a dar "Failed to fetch". Investiguei direto no servidor (`curl`, sem passar pelo navegador) e descobri que a função **CASSIE inteira** tinha sumido do Supabase (até a rota `/chat`, que já funcionava há semanas, voltava "Requested function was not found" direto do gateway) — não era nada do código novo, a Cassie inteira ficou fora do ar (chat do cliente e do gestor também). Causa: ao "reimplantar", o usuário aparentemente recriou a função em vez de editar a existente, e como o Supabase não deixa renomear, a função com o nome certo (`CASSIE`, maiúsculo) sumiu. Resolvido pelo usuário recriando/corrigindo o nome certo
- [x] **Persistência resolve os dois problemas de uma vez**: a geração virou um chat guardado no banco (`persuasive_copy_messages`, migration-034, mesmo desenho de `cassie_messages` da Fase 7.1 — isolado por quem pediu + cliente + formulário). Guardar no banco em vez de `useState` já resolve o "sumiu ao trocar de aba" de graça, e dá o chat de acompanhamento pedido
- [x] Rota `/persuasive-copy` virou conversacional (mesmo espírito de `handleChat`): sempre exige `message`; o botão "Gerar sugestões com IA" manda uma mensagem fixa por ela mesma (sem caminho especial); pedidos de ajuste reaproveitam o histórico salvo + reinjetam as respostas abertas a cada chamada; o prompt pede pra sempre devolver a lista **inteira atualizada** (não só o item mudado), pra nunca ficar em dúvida de qual é a versão mais recente pra copiar
- [x] `CassieMessageForm.tsx` ganhou uma prop `placeholder` opcional (reaproveitado sem duplicar componente, com o texto pedido "Se quiser modificações ou melhorias, é só dizer"); componente novo `PersuasiveCopyThread.tsx` (bolhas de conversa, mesmo visual de `CassieChatThread.tsx`, sem os modos que só fazem sentido na Cassie principal)
- [x] `npx tsc -b --noEmit`, `npm run test` (40/40) e `npm run test:e2e` (3/3, incluindo o teste que tinha falhado por instabilidade na rodada passada — confirmando que era mesmo ambiente, não código) limpos
- [x] Testado ao vivo: com a tabela nova ainda não existindo (antes do usuário rodar a migração), a tela não quebra — só demora uns 7-9s a mais pra aparecer o botão porque o React Query tenta de novo sozinho antes de desistir (comportamento padrão, não é bug); confirmado por leitura direta do DOM, não só captura de tela (uma primeira verificação minha por print deu falso alarme de "sumiu", causado pela mesma demora)
- [x] **Migração rodada e função reimplantada pelo usuário — confirmado ao vivo de ponta a ponta**: testei direto no servidor (sem passar pelo navegador) que a rota devolve `{"reply": ...}` (formato novo) e grava as duas mensagens (usuário + Cassie) em `persuasive_copy_messages`; pedido de ajuste ("deixe a headline 1 bem mais curta") voltou com a lista inteira atualizada, só a headline 1 mudou (de "Alcance a paz interior que busca" pra "Paz interior"), o resto igual — exatamente o comportamento pedido. Depois testei pela tela: a conversa gerada pelo teste direto no servidor já aparecia certinha ao abrir a aba; mandei um novo pedido pela caixa de chat ("adicione um emoji de coração em cada headline") e a resposta chegou; troquei de aba e voltei, e recarreguei a página inteira — a conversa continuou lá nas duas situações

## Fase 9 — Ajustes pontuais (Timeline, Atividades, Metas SMART, Kanban)

Quatro pedidos do usuário numa mensagem só, investigados e resolvidos um por um.

- [x] **Timeline com registros "que não correspondem ao tempo verdadeiro"**: investigado direto em `audit_logs` — **não é bug**, são registros genuínos gerados pelos meus próprios testes automatizados (Playwright) rodados o dia inteiro contra o cliente de teste real "Studio Prisma" (35 das 56 linhas totais eram "Workflow Teste ..." ou "Cliente pediu revisão de Criativo Teste Playwright ..."). Os horários batiam certinho com quando os testes rodaram — só não eram ações do usuário. **Pendente**: dei o SQL de limpeza pro usuário rodar direto no painel (a exclusão foi bloqueada pelo classificador de segurança do modo automático ao tentar fazer via script — corretamente, é uma ação destrutiva; o usuário roda com controle total, mesmo padrão de sempre)
- [x] **Criar uma Atividade não gerava registro no Timeline**: confirmado, lacuna real — incidentes/alertas/workflow aplicado/pedido de revisão já tinham gatilho automático em `audit_logs` (migrations 009/021/030), "Atividades" nunca ganhou o dela. `migration-035-fase-timeline-atividades.sql` (nova) — gatilho `log_activity_checklist_event()`, mesmo padrão dos outros: registra "Atividade criada" só pra itens avulsos (`source_activity_template_id is null` — itens que nascem em massa de um Workflow aplicado não geram um registro por item, evita poluir o Timeline a cada workflow) e "Atividade concluída" sempre que marcado, não importa a origem
- [x] **Metas SMART sem validação**: `NewSmartGoalDialog.tsx` ganhou 2 validações na criação (zod `.refine` a nível de objeto): valor alvo igual ao valor atual bloqueado com a mensagem exata pedida ("meta com valor alvo igual a atual, meta inválida"); prazo no passado bloqueado com `minDate={TODAY}` no `DatePicker` (desabilita a data no calendário, mesmo padrão de `NewManagerMeetingDialog.tsx`) + `.refine` de reforço. `UpdateGoalProgressDialog.tsx` (atualizar progresso de uma meta já existente) fica sem mudança de propósito — ali alcançar o valor alvo é o jeito normal de bater a meta, não é inválido
- [x] **Borda vermelha sutil em cards/tarefas atrasadas**: `KanbanCard.tsx` e `ManagerTaskRow.tsx` (usado em "Tarefas do Cliente" e na aba Tarefas da Central de Informações do Cliente) — são os 2 únicos lugares no código com o estilo cinza de atrasado hoje; confirmei que "Atividades" (mencionado 2x na mensagem do usuário) não tem campo de prazo/data ainda, então não tem "atrasado" pra marcar lá — tratei como repetição sem querer na mensagem. `border-red-500/40` na condição `isOverdue`, confirmado via CSS computado ao vivo (`rgba(239, 68, 68, 0.4)`) nos dois lugares, com tarefas atrasadas reais já existentes na base (não precisei criar dado novo)
- [x] `npx tsc -b --noEmit` e `npm run test` (40/40) limpos. **`npm run test:e2e` não rodado nessa rodada** — a suíte usa o mesmo cliente de teste "Studio Prisma" citado acima e recriaria mais do mesmo tipo de poluição que este round tentou limpar; roda normal na próxima rodada que precisar
- **Pendente**: usuário rodar `migration-035-fase-timeline-atividades.sql` e o `DELETE` de limpeza (SQL passado à parte, fora deste arquivo) no SQL Editor do Supabase

### 9b — Timeline completo (levantamento de todas as ações não registradas)

Usuário perguntou por que o Timeline (criado justamente pra registrar as ações dele, "se der errado, eu vou saber aonde ajeitar") não pegou a criação de uma Atividade, e pediu um levantamento completo de tudo que falta.

- [x] Levantei todas as tabelas e mutações do app (`useManagerPortalData.ts`, `useClientPortalData.ts`, todas as migrações) pra achar toda ação que muda dado de verdade mas não gera entrada em `audit_logs`
- [x] `migration-036-timeline-completo.sql` (nova) — mesmo padrão de `log_incident_event()`/`log_alert_event()` (migration-009): gatilho por tabela, `AFTER INSERT/UPDATE` pra criação/mudança de status, `BEFORE DELETE` pra exclusão (a linha ainda existe nesse momento, dá pra registrar nome/título antes de sumir). Cobre: Clientes (criado/mudou de status/excluído), Projetos (criado/mudou de status), Metas SMART (criada/mudou de status/excluída), Reuniões (cancelada/excluída — agendar/concluir é rotina, não entra), Ativos Digitais (criado/mudou de status/excluído), Conexões de integração (virou conectada/com erro/desconectada — erro sai com severidade "high", é o "deu errado" que vale rastrear), Aprovações (`respond_to_approval` completado pra logar aprovada/rejeitada, já logava "pediu revisão"), Incidentes/Alertas (só faltava exclusão, criado/resolvido já existiam)
- [x] **Decisão de critério registrada**: pra clientes/projetos/metas, loga **qualquer** mudança de status (não só a "final" como incidentes fazem com "resolvido") — esses status mudam raramente, cada mudança já é informação útil sozinha
- [x] **Fora do escopo, de propósito**: Tarefas (Kanban) e Comentários — mudam o tempo todo, inundariam o Timeline, já têm tela própria mostrando o estado atual; modelos de Workflow/Atividades — é configuração, aplicar o modelo já é logado; `profiles` (papel do usuário) — não existe tela no app que troque isso ainda
- [x] `npx tsc -b --noEmit` e `npm run test` (40/40) limpos — mudança é só SQL, nenhum arquivo `.ts`/`.tsx` mudou nessa rodada
- [x] **Migração rodada pelo usuário — confirmado ao vivo, testando cada gatilho com dado descartável (criado e apagado na hora, nada do usuário foi tocado)**: cliente (criado/mudou status/excluído — exclusão só funciona com conta admin, confirmando que a regra "só admin apaga cliente" da Fase 5 continua valendo, testei com gestor primeiro e vi 0 linhas afetadas sem erro, troquei pra admin e confirmou), projeto (criado/mudou status), meta SMART (criada/mudou status/excluída), reunião (cancelada com motivo/excluída), ativo digital (criado/mudou status/excluído), atividade avulsa (criada/concluída), aprovação (aprovada/rejeitada, via `respond_to_approval` chamado como o cliente de teste de verdade) — todos os 9 geraram a entrada certa no Timeline, com a severidade certa
- **Não testado por mim**: conexão de integração mudando pra "conectada"/"com erro"/"desconectada" — precisa de uma sincronização real (OAuth ou Edge Function com service role), que não tenho como disparar sozinho; a lógica é idêntica aos outros 9 que já confirmei funcionando, só falta ver ao vivo na próxima vez que uma conexão real mudar de status
- [x] **Limpeza dos 35 registros de teste rodada pelo usuário — confirmado ao vivo**: 0 registros restantes batendo com "Workflow Teste" ou "Criativo Teste Playwright" em `audit_logs`

### 8.5 — Seção "Público-Alvo" na Central de Informações do Cliente

Último item pendente do roadmap da Fase 8. Trouxe o resultado da Fase 8.3 (síntese em % das perguntas fechadas) pra dentro de `ClientDetail.tsx` — não a Comunicação Persuasiva da 8.4, essa fica só na aba dedicada.

- [x] `ClientDetail.tsx` ganhou uma seção nova "Público-Alvo" (ícone `UsersRound`, mesmo do menu), posicionada depois da grade Tarefas/Metas/Reuniões/Atividades e antes da Cassie IA — 100% reaproveitamento: mesmo hook `useAudienceInsights` e mesmo componente `OptionBreakdownBarChart` já construídos na Fase 8.3, nenhum hook/componente novo
- [x] Versão simplificada de propósito (sem seletor de formulário, busca ou agrupamento por seção, que só existem na aba dedicada) — é a visão "olhar rápido" desse cliente específico; conteúdo dentro de `max-h-[560px] overflow-y-auto`, mesmo padrão já usado nos outros cards desta página
- [x] A seção só aparece se o cliente tiver pelo menos 1 pergunta fechada sincronizada (mesmo padrão condicional já usado no card "Cliente em risco" desta página) — evita poluir a tela de clientes sem Google Forms conectado
- [x] `npx tsc -b --noEmit` e `npm run test` (40/40) limpos. `npm run test:e2e` não rodado nessa rodada de propósito — a suíte recriaria mais poluição de teste em Studio Prisma logo depois de confirmar a limpeza (ver 9b); nada nesta mudança tem relação com os cenários que os testes de ponta a ponta cobrem (login, workflow, aprovação), então o risco de regressão não detectada é baixo
- [x] Testado ao vivo: Central de Informações do Studio Prisma mostra a seção "Público-Alvo" com as 15 perguntas fechadas reais, gráfico igual ao da aba dedicada (confirmei via captura de tela); Loja Aurora (sem Google Forms conectado) corretamente não mostra a seção. Um teste meu inicial deu falso alarme de "seção sumiu" — era só o carregamento em segundo plano (a seção não trava o resto da página, aparece assim que os dados chegam, por isso demora um instante a mais que o resto), confirmado repetindo o teste com espera adequada

## Fase 10 — Deploy em produção

Usuário decidiu: hospedagem na Vercel, repositório já existente no GitHub (`ametista-conversoes/Ametista-Convers-es-app`). Ajustes de segurança adicionais e backups ficam pra depois, por decisão do usuário — essa fase foi só "colocar o app no ar".

- [x] Antes de começar, respondi 2 perguntas do usuário sobre segurança concreta (RLS/segredos/CORS/2FA na conta do Supabase/backup/limite de uso de IA) e sobre viabilidade de instalar o app como PWA (ícone na tela do celular) — confirmado que dá, precisa da lib `vite-plugin-pwa` (fora do stack combinado, fica pra quando o usuário pedir)
- [x] `npm run build` confirmado limpo (build de produção real, `tsc -b && vite build`, nunca tinha sido testado antes, só `npm run dev`)
- [x] `vercel.json` novo — regra de fallback pra SPA (`rewrites` pra `index.html`), necessária porque o app usa rotas do React Router só resolvidas no navegador; sem isso, recarregar a página numa rota como `/clients/:id` dava 404 em produção
- [x] **Bloqueio real encontrado ao tentar subir pro GitHub**: `git push`/`git ls-remote` travavam indefinidamente — causa: o Git Credential Manager (`credential.helper = manager`) tenta abrir uma janela de login interativa, e o ambiente onde eu rodo não tem interface gráfica pra isso. Resolvido pedindo pro usuário rodar `git push -u origin master` no próprio terminal dele (onde a janela abre normal) — depois disso as credenciais ficaram em cache e minhas checagens (`git ls-remote`) passaram a funcionar direto
- [x] Deploy feito pelo usuário na Vercel (variáveis `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` configuradas por ele no painel) — domínio final: `https://ametistaconversoesapp.vercel.app`
- [x] **Segredo `FRONTEND_URL`** atualizado pelo usuário no painel do Supabase (função `integrations`) pro domínio real — confirmado ao vivo via `curl` que o redirect pós-OAuth aponta pra `ametistaconversoesapp.vercel.app`, não mais `localhost`
- [x] **CORS restrito ao domínio de produção** nas duas Edge Functions (`cassie` e `integrations`) — trocado de `Access-Control-Allow-Origin: '*'` (qualquer site podia chamar) pro domínio real; não afeta `/callback` nem `/forms-webhook`, que não são chamadas de navegador. Reimplantado pelo usuário, confirmado ao vivo via `curl` (preflight e resposta real já devolvem o header certo)
- [x] Testado ao vivo, direto no domínio de produção: login funciona; navegar pra `/clients/:id` e apertar F5 continua mostrando a página certa (confirma que o `vercel.json` resolveu o 404 de recarregar); sem erros no console do navegador
- [x] **App publicado e funcionando em produção**: `https://ametistaconversoesapp.vercel.app`

### 10b — App instalável (PWA) + descoberta de que o deploy automático estava quebrado

- [x] Pedido do usuário: instalar o app na tela de início do celular igual um app nativo (não só o atalho básico que já funciona sem mexer em nada). `vite-plugin-pwa` instalado (autorizado pelo usuário) — gera manifesto + service worker automaticamente no build
- [x] Ícones em todos os tamanhos gerados a partir da logo já existente (`public/logo.png`) via `@vite-pwa/assets-generator`, rodado uma vez só com `npx` (não fica como dependência do projeto) — inclui ícone "maskable" pro Android (com a margem de segurança certa) e `apple-touch-icon` pro iPhone
- [x] `index.html` ganhou a tag `apple-touch-icon`; `vite.config.ts` configurado com nome/cores/`display: standalone` do manifesto
- [x] `npx tsc -b --noEmit`, `npm run test` (40/40) e `npm run build` (confirmado manifest.webmanifest + sw.js gerados certos) limpos localmente
- [x] **Bug real encontrado ao testar em produção**: `manifest.webmanifest`, ícones e o service worker voltavam o HTML da página em vez do arquivo de verdade — a regra de fallback do `vercel.json` (criada na Fase 10) capturava **qualquer** caminho, inclusive arquivos reais. Corrigido — mas a primeira tentativa (regex com negative lookahead) também não funcionou porque o Vercel usa `path-to-regexp` nas `rewrites`, que não suporta esse tipo de regex; resolvido de vez trocando pra `routes` com `{"handle": "filesystem"}` (forma mais antiga mas testada/documentada — tenta servir arquivo real primeiro, só cai no `index.html` se não achar)
- [x] **Descoberta maior, ao investigar por que o site não atualizava mesmo depois de 3 pushes**: o deploy automático da Vercel a partir do GitHub estava **bloqueado desde sempre** (só o primeiro deploy manual, feito direto pela tela da Vercel, tinha ido ao ar) — motivo real: `git push -u origin master`, quando rodado pelo usuário, tinha autenticado com a conta pessoal do GitHub dele (`calldaviaraujo@gmail.com`, confirmado no autor dos commits), não com a conta ligada à Vercel/Ametista. A Vercel bloqueia deploy automático de quem ela não reconhece como dono/colaborador do projeto no plano Hobby (gratuito)
  - Primeira tentativa de correção: tornar o repositório público no GitHub (a mensagem de erro da Vercel citava "Hobby Plan does not support collaboration for **private** repositories") — não resolveu sozinho, o bloqueio continuou
  - [x] **Resolvido**: usuário trocou a credencial salva (removeu a entrada no Gerenciador de Credenciais do Windows) e fez login de novo com a conta certa ao rodar `git push` — o deploy automático voltou a funcionar. Confirmado ao vivo: `manifest.webmanifest`, os ícones e o service worker agora respondem com o conteúdo real (não mais o HTML da rota antiga); login em produção funciona; service worker registrado com sucesso (`navigator.serviceWorker.getRegistrations()` confirmado via Playwright); sem erros no console
- **App publicado, instalável, e com deploy automático funcionando de ponta a ponta**: `https://ametistaconversoesapp.vercel.app`

## Fase 11 — Notificações, limpeza do admin, planos da Cassie

Pedido do usuário em 4 partes, uma etapa por vez, ordem escolhida por ele: (11a) tirar o Portal Cliente de dentro da conta admin → (11b) planos da Cassie → (11c) sistema de notificações (o maior, decidido que vai ser push de verdade, funcionando mesmo com o site fechado, parecido com notificação de WhatsApp/celular — vai precisar de infraestrutura nova, aviso antes de instalar qualquer dependência).

### 11a — Remover o Portal Cliente de dentro da conta admin

- [x] `Sidebar.tsx`: `showClientSection` deixou de incluir `role === 'admin'` — agora só aparece pra quem realmente é `cliente`. Login como admin não tinha uma "própria" conta de cliente, então a seção não tinha função (só links pra páginas sem contexto de cliente nenhum)
- [x] Mesmo componente (`SidebarContent`) é usado no menu lateral (desktop) e na gaveta mobile — os dois corrigidos de uma vez só
- [x] `npx tsc -b --noEmit` limpo, `npm run test` (40/40 passando)

### 11b — Planos da Cassie

- [x] Plano "Dominação" liberava só 3 dos 4 modos da Cassie (faltava "auditora") — corrigido nos dois lugares: `src/lib/cassie-modes.ts` (só decide o que mostra no seletor) e `supabase/functions/cassie/index.ts` (quem realmente vale, tem que ser reimplantada no painel do Supabase pra valer em produção)
- [x] `npx tsc -b --noEmit` limpo; `npm run test` (40/40 — 1 teste existente ajustado pra nova regra do plano Dominação)
- [x] Entregue ao usuário a tabela explicando a diferença funcional entre os 4 modos

### 11c — Sistema de notificações push

Plano completo em 5 sub-etapas (11c.1 a 11c.5), uma por vez. Decisões já tomadas: biblioteca `web-push` aprovada (só dentro da Edge Function, via `npm:web-push` do Deno — nunca no front); contato VAPID = `mailto:ametistaconversoes@gmail.com` (e-mail de trabalho da agência, fica só num segredo servidor-a-servidor); checagem por tempo a cada 1 minuto (pg_cron).

#### 11c.1 — Tabelas, RLS e funções de detecção

- [x] `supabase/migration-037-fase11c-notificacoes-tabelas.sql`: `push_subscriptions` (por usuário/aparelho, RLS igual ao padrão já usado em `nav_last_seen`) e `push_notification_log` (dedup — evita mandar a mesma notificação 2x; RLS ligado sem nenhuma policy, só a service role mexe, mesmo padrão de `oauth_tokens`)
- [x] `compute_at_risk_client_ids()` — tradução pra SQL da mesma regra de "cliente em risco" que já existia só no front-end (`src/lib/client-risk.ts`), necessária porque o cron não tem navegador aberto pra rodar a versão em TypeScript
- [x] `detect_new_at_risk_clients()`, `detect_new_overdue_goals()`, `detect_new_meeting_reminders_1h()`, `detect_new_meeting_reminders_15m()` — cada uma insere+devolve só o que é NOVO (usando `unique(kind, entity_id)` + `on conflict do nothing`), pra não repetir notificação a cada minuto enquanto a condição persiste
- [x] 2 gatilhos de limpeza (`AFTER UPDATE` em `meetings`/`smart_goals`) que apagam o registro de dedup quando a reunião é remarcada ou o prazo da meta muda — evita ficar bloqueado de notificar de novo numa mudança legítima
- [x] **Bug real encontrado e corrigido**: as 4 funções de detecção davam erro "column reference is ambiguous" — o nome de retorno da função (`entity_id`) colidia com o nome da coluna de verdade usada dentro do `on conflict (kind, entity_id)`. Corrigido renomeando o retorno pra `id` (o `RETURN QUERY` do PL/pgSQL casa por posição/tipo, não por nome, então não muda o resultado)
- [x] Testado ao vivo com dados descartáveis no Studio Prisma: incidente crítico → cliente aparece em `detect_new_at_risk_clients()` → apagado → some; meta com prazo vencido → aparece em `detect_new_overdue_goals()`, não repete numa 2ª chamada; reunião daqui 10min → aparece nos dois lembretes (1h e 15min); reunião remarcada pra longe e de volta pra dentro da janela → dispara de novo (confirma que o gatilho de limpeza funciona); conta `cliente` não consegue chamar nenhuma das funções nem ler `push_notification_log` (RLS bloqueando certo); conta `cliente` não consegue inserir `push_subscriptions` pra outro usuário, só pra si mesma

#### 11c.2 — Edge Function `notifications` (`/tick`, `/dispatch`) + chaves VAPID

- [x] Chaves VAPID geradas via `npx web-push generate-vapid-keys` (descartável); `NOTIFICATIONS_CRON_SECRET` gerado como um hex aleatório; `VAPID_SUBJECT = mailto:ametistaconversoes@gmail.com` (e-mail de trabalho da agência — usuário confirmou não ter problema de segurança em usá-lo, é só um contato server-a-server exigido pelo protocolo Web Push, nunca fica exposto)
- [x] `supabase/functions/notifications/index.ts`: 2 rotas, as duas só chamadas por servidor (sem CORS, sem checagem de JWT de usuário — só o segredo `X-Notifications-Secret`, mesmo padrão do `X-Cron-Secret` já usado em `integrations/sync-all`). `/tick` roda as 4 checagens por tempo da 11c.1 e despacha o que for novo; `/dispatch` recebe `{ kind, entity_id }` dos gatilhos de incidente/alerta (11c.3, ainda não criados). Usa `npm:web-push` (aprovado pelo usuário) só aqui dentro, nunca no front
- [x] Tabela de destinatário por tipo de notificação implementada: incidente/meta atrasada/cliente em risco → todo admin+gestor; alerta → admin+gestor E o cliente afetado (com URL diferente pra cada lado); lembrete de reunião → admin+gestor E o cliente da reunião
- [x] Usuário criou a função no painel do Supabase (nome exato `notifications`, verify JWT desligado) e configurou os 4 segredos novos (`VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `NOTIFICATIONS_CRON_SECRET`)
- [x] Testado ao vivo via `curl`: `/tick` e `/dispatch` sem segredo ou com segredo errado → 401 nos dois; com segredo certo → 200 (`/tick` retornou `dispatched:0`, esperado — os clientes já em risco desde os testes da 11c.1 já estavam logados, então não é "novo"); `/dispatch` com um incidente descartável de teste → 200, sem erro. **Nota**: a primeira chamada de `/tick` deu um erro transitório do Supabase ("JWT issued at future", falha de sincronismo de relógio conhecida em Edge Functions recém-criadas) — a repetição imediata funcionou normal; se acontecer de novo esporadicamente não é bug do código, é uma instabilidade conhecida da infraestrutura do Supabase
- [x] Ainda não envia notificação nenhuma de verdade pro sistema operacional — não existe nenhuma inscrição (`push_subscriptions`) real ainda, isso entra na 11c.4 (front-end)

#### 11c.3 — Gatilhos de incidente/alerta + agendamento do cron

- [x] `supabase/migration-038-fase11c-notificacoes-gatilhos-cron.sql`: `notify_incident_created()`/`notify_alert_created()` (`after insert`, chamam `/dispatch` via `net.http_post`, mesmo padrão dos gatilhos de auditoria) + `cron.schedule('notifications-tick', '* * * * *', ...)` chamando `/tick` a cada 1 minuto
- [x] **Descoberta importante, fora do escopo da 11c**: a extensão `pg_net` (que faz as chamadas HTTP de dentro do banco) **nunca tinha sido instalada de verdade** nesse projeto, apesar de `migration-019-fase64-cron.sql` (Fase 6.4) já tentar usá-la desde então — só o `pg_cron` existia. Isso quer dizer que o cron de sincronização automática do Google Ads/Meta Ads a cada 6h provavelmente nunca funcionou sozinho (só o botão manual "Sincronizar agora", que não depende disso, continuou funcionando — por isso ninguém notou). Resolvido rodando `create extension if not exists pg_net;` — **confirmado**: usuário rodou manualmente o comando do job `sync-integrations` (copiado de `cron.job.command`) e a chamada respondeu `200` em `net._http_response`. Não tinha nenhuma conta Google Ads/Meta Ads conectada de verdade nesse ambiente pra confirmar dado sincronizado, mas o mecanismo (banco → Edge Function) está funcionando; o robô de 6 em 6 horas volta a funcionar sozinho a partir de agora
- [x] Testado ao vivo: criado incidente e alerta descartáveis no Studio Prisma → conferido via `select id, status_code, created from net._http_response order by id desc limit 10;` que as chamadas pra `/dispatch` (e uma chamada de `/tick` do próprio cron, disparada sozinha) responderam `200`; dados de teste apagados depois

#### 11c.4 — Front-end: service worker + card "Ativar notificações"

- [x] `vite.config.ts`: `workbox.importScripts: ['push-worker.js']` — injeta o listener de push dentro do `sw.js` gerado, sem trocar de estratégia (evita reescrever o pré-cache que já funciona em produção desde a Fase 10b)
- [x] `public/push-worker.js` (novo, JS puro): listener de `push` (mostra o cartão via `self.registration.showNotification`) e de `notificationclick` (foca uma janela já aberta do app ou abre uma nova, na rota certa)
- [x] `src/lib/push-notifications.ts`: funções de assinar/cancelar/checar estado, direto via supabase-js (sem passar pela Edge Function — mesmo padrão de `nav_last_seen`)
- [x] `src/components/settings/PushNotificationsCard.tsx`, plugado na aba "Usuário" de Configurações (`UserSettingsTab.tsx`) — visível pros 3 papéis. Mostra aviso específico se detectar iPhone/iPad fora do modo instalado (Safari normal não entrega push)
- [x] `VITE_VAPID_PUBLIC_KEY` adicionada no `.env.local` (não versionado) e documentada no `.env.example`
- [x] `npx tsc -b --noEmit`, `npm run test` (40/40) e `npm run build` limpos; confirmado que `dist/sw.js` importa `push-worker.js` e que o arquivo foi copiado pro `dist/`
- [x] Tentei automatizar o teste de assinatura via Playwright e descobri 2 coisas específicas de ambiente de teste (não são bugs do app): o Chrome desliga a Push API de propósito em contexto "anônimo" (o padrão do Playwright) — só funciona com um perfil persistente de verdade; e mesmo assim a ativação de notificação de verdade (autorizar no navegador, confirmar que chega o cartão do sistema operacional) não dá pra automatizar de forma confiável — isso precisa ser feito manualmente pelo usuário, num navegador de verdade, na 11c.5

#### 11c.5 — Ativação real + teste de ponta a ponta

- [x] **Bug real encontrado pelo usuário**: o card de notificações ficava preso em "Verificando..." pra sempre num PC específico — causa: nenhum service worker registrado ali (visível em DevTools > Application > Service Workers, painel vazio), provavelmente cache antigo do navegador de antes da Fase 10b existir. Ctrl+Shift+R (atualização forçada) resolveu. De qualquer forma, corrigido também no código (Fase 11c.4b): `navigator.serviceWorker.ready` agora tem limite de 8s, então o card nunca mais trava pra sempre, mesmo se acontecer de novo por outro motivo
- [x] Usuário ativou notificações de verdade em 2 aparelhos: conta admin no PC, conta cliente (vinculada à "Loja Aurora") no celular
- [x] **Testado ao vivo, com o site minimizado/fechado, os 6 tipos de notificação — todos confirmados chegando como cartão nativo do sistema operacional**:
  1. Incidente criado → PC (admin/gestor)
  2. Alerta criado → PC (admin/gestor) e celular (cliente afetado)
  3. Cliente ficou em risco → PC (admin/gestor)
  4. Meta atrasada → PC (admin/gestor)
  5. Reunião em 1 hora → PC e celular (reunião testada a 10min de distância, cai nas duas janelas ao mesmo tempo)
  6. Reunião em 15 minutos → PC e celular (mesma reunião de teste)
- [x] Dados descartáveis de todos os testes apagados; confirmado que apagar cliente é restrito à conta admin (gestor não tem essa permissão — RLS funcionando como esperado)

**Fase 11c concluída — sistema de notificações push funcionando de ponta a ponta em produção.**

## Roadmap — Fase 12 a 18 (levantamento grande pós-testes do usuário)

Depois de testar bastante o app publicado, o usuário trouxe 23 pedidos numa mensagem só. Investigado com agentes de exploração antes de planejar qualquer coisa (respostas registradas na conversa, não repetidas aqui). Dividido em fases, ordem confirmada pelo usuário — do mais rápido pro mais trabalhoso:

- [x] **Fase 12 — concluída** — correções rápidas: workflow padrão não desmarcava ao editar (3); datas sem ano em toda parte (6); comentários sem espelhamento tipo WhatsApp, agora relativo a quem está vendo (7); plano do cliente ao lado do papel no menu do usuário e em Configurações (8); confirmação ao clicar em Sair (9); apagar reunião concluída + regra de que quem cancela não é quem apaga (11 e 12 — o 12 foi puxado da Fase 16 pra cá, mesmo código/botão de exclusão); tarefas do cliente sem cinza/borda vermelha quando atrasadas (13); tirou aba "Clientes & Mensalidades" de Relatórios, redundante com Configurações (21). 8 commits (12.1 a 12.8), tudo testado (`tsc`/testes/build limpos, e os itens com regra de banco testados ao vivo com dados descartáveis)
- [x] **Fase 13 — concluída** — rota `/` agora decide por papel (novo componente `Home`): cliente continua vendo o Dashboard normal, admin/gestor são mandados pra `/admin` (2, bug real corrigido); aviso de "conta não vinculada" (extraído pra componente único `UnlinkedClientNotice`, usado nas 9 páginas do Portal Cliente) agora instrui a pessoa a contatar o admin da agência (1). Testado ao vivo com as 3 contas de teste
- [x] **Fase 14 — concluída** — `ApplyWorkflowDialog` generalizado pra 2 pontos de entrada (a partir do Workflow, escolhendo cliente; a partir do cliente na Central de Informações, escolhendo Workflow pré-preenchido com o padrão); depois de cliente+template escolhidos, agora dá pra optar entre aplicar num projeto ou direto no Kanban do cliente (4). Nova coluna `clients.default_workflow_template_id` (só pré-seleção de conveniência). Testado ao vivo (RPC + fluxo de UI completo nos 2 pontos de entrada)
- [x] **Fase 15 — concluída** — a aba "Projeto" do Portal Cliente só mostrava o de start_date mais recente, escondendo qualquer outro (era assim que "ahoii" na Loja Aurora nunca aparecia); agora lista todos como cards clicáveis, detalhes trocam pro escolhido, mais recente continua padrão. Testado ao vivo (5)
- [x] **Fase 16 — concluída** — separadores de data tipo WhatsApp (Hoje/Ontem/dia da semana/data completa) + divisor "Mensagens não lidas" em `ChatThread.tsx` (usa `date-fns`, agora dependência direta; chave própria `comments:{clientId}` em `nav_last_seen`, separada da bolinha do menu) (10). Testado ao vivo com mensagens em datas diferentes. Item 15: entregue primeiro só a especificação, depois o usuário pediu pra implementar de verdade — feito (`useAudioRecorder`, `AudioRecorderButton`, `AudioMessageBubble`, 2 colunas novas em `comments`, reaproveitando `uploadClientFile`/`getFileSignedUrl` já existentes). Testado ao vivo de ponta a ponta nos dois portais com Playwright + microfone falso do Chrome (gravar → enviar → tocar de volta, arquivo confirmado no bucket certo). Item 12 já tinha sido resolvido na Fase 12
- **Fase 17** — [x] nova categoria "Áudios" na aba Arquivos (`getFileCategory` já classificava por MIME type, só faltava `audio/*` virar seção própria em vez de cair em "Outros") — testado ao vivo (17). Limite de tamanho de arquivo continua **bloqueado até o usuário assinar o Supabase Pro** (plano grátis: 50MB/arquivo e 1GB no total; usuário quer subir arquivos de 200MB a 1,5GB, impossível no plano grátis) (16)
- [x] **Fase 18 — concluída** — `Performance.tsx` apagada, `/performance` agora redireciona pra `/reports` (mesmo padrão de `/alerts`→`/incidents`); página "Relatórios" ganhou os cards de Conversões e Health Score que só existiam na página apagada (22, junto com 21). Novos cards de tráfego com dado real de integração (Google Ads/Meta Ads já gravam `impressions`/`clicks`/`conversions` em `performance_snapshots` desde a Fase 6, só não estava na tela): Impressões, Cliques, CPC, Taxa de Conversão (19). `FinancialSummaryCard` ganhou "Gasto total" (mídia + mensalidade) e "Lucro" (receita − gasto total, verde/vermelho conforme o sinal) — aparece tanto em Relatórios quanto no Dashboard, que reaproveita o mesmo componente (20). **Bug real corrigido**: Health Score da aba Performance vinha de uma média de `projects.health_score` (campo nunca atualizado); trocado por `clients.health_score`, o mesmo valor certo já usado no Dashboard — removida a função `averageHealthScore` (23). Testado ao vivo: snapshot descartável inserido com métricas reais (Studio Prisma), conferido que os números batem (Impressões 10.000, Cliques 250, CPC R$2,00, Taxa 8,0%), Health Score da aba bate com o do Dashboard (84 nos dois), menu só mostra "Relatórios", `/performance` redireciona. Reconfirmado: canais por plataforma separados (Facebook/Instagram, YouTube, pixel/tag) não existem — só Google Ads e Meta Ads juntos — fora de escopo (18)

**Roteiro dos 23 itens (Fase 12 a 18) concluído.**

## Fase 19 — pré-lançamento (achados de `PROJETO.md` + documento cruzado "Fases 19 e 20 finais")

Depois da Fase 18, o usuário pediu um documento de auditoria pré-lançamento (`PROJETO.md`, raiz do projeto — onde encontrar tudo + achados 🔴🟡🟢) e depois cruzou esse documento com o `TASKS.md` inteiro em outra IA, trazendo achados novos que só apareciam comparando o histórico completo. Fase 19 resolve os riscos 🔴/🟡/🟠; Fase 20 fica pros 🟢 de baixo risco + ideias boas do documento. Ordem pedida pelo usuário: primeiro o problema de KPIs, depois os itens de verificação.

- [x] **Fase 19.1 — concluída** — corrigido o achado 🔴: Investimento/CTR médio/CPC/Taxa de Conversão/Impressões/Cliques/Conversões/CPA agora vêm de `performance_snapshots` de verdade (últimos 30 dias — mesma janela já usada em `useCampaignPerformance` e no resumo da Cassie), em vez das colunas mortas `projects.spend/ctr` (sem tela nenhuma pra preencher). Nova função `aggregateSnapshotKpis` (`src/lib/metrics.ts`), substitui `aggregateProjectKpis`/`averageCtr` (removidas, sem uso depois da troca) — usada igual em `Dashboard.tsx` e `Reports.tsx`, os dois nunca mais podem mostrar número diferente pra mesma métrica. Confirmado que a migration-024 (que torna `performance_snapshots.project_id` opcional e troca a chave de conflito pra `client_id+channel+snapshot_date`) está aplicada em produção — elimina uma das 4 dúvidas do documento cruzado. Receita (nenhuma plataforma de anúncio reporta faturamento) ganhou tela de edição de verdade — campo editável em `ProjectDetailDialog.tsx`, salvo via `useUpdateProject` (agora aceita atualização parcial). ROAS passa a cruzar Receita manual com Investimento real (`computeRoas`). Testado ao vivo com snapshots descartáveis (Studio Prisma) simulando Google Ads + Meta Ads dentro de 30 dias + uma linha de 40 dias atrás (confirmado que fica de fora da conta); editada a Receita pelo diálogo do gestor e confirmado que Dashboard e Relatórios refletem o mesmo ROAS. **Sem conta real do Google/Meta Ads pra validar dado sincronizado de verdade** (usuário só tem 2 contas de teste vazias) — pendente até haver credencial real, mesma limitação já registrada no documento cruzado (item 1.3)
- [x] **Fase 19.1b — concluída** — usuário trouxe uma fórmula melhor pra Receita: em vez de digitar direto, calcular a partir dos Leads reais (Conversões já sincronizadas) e 2 premissas de negócio editáveis por cliente. Vendas = Leads ÷ "Leads p/ fechar 1 venda"; Receita = Vendas × "Ticket médio". Duas colunas novas em `clients` (`leads_to_close`, `average_ticket`, migration-042), editáveis na Central de Informações do Cliente (card "Plano", mesmo padrão da Mensalidade). Nova função `computeRevenueFromLeads` (`src/lib/metrics.ts`) substitui `sumProjectRevenue`; `Dashboard.tsx`/`Reports.tsx` usam ela; `FinancialSummaryCard` passa a aceitar Receita/Lucro `null` (mostra "—" em vez de tratar como zero quando as premissas não estão configuradas). Por pedido explícito do usuário, o editor de Receita por projeto que a Fase 19.1 tinha acabado de adicionar em `ProjectDetailDialog.tsx` **não foi mexido** — continua existindo do jeito que ficou, só não alimenta mais o Dashboard/Relatórios. Testado ao vivo (Studio Prisma): 100 leads sincronizados (snapshots descartáveis) ÷ 20 leads/venda = 5 vendas × R$500 ticket médio = R$2.500,00 de Receita, ROAS 5,0x — conferido batendo em Dashboard e nas duas abas de Relatórios
- [x] **Fase 19.2 — concluída** — itens de verificação do documento cruzado "Fases 19 e 20 finais" (seção 1). **Migrations (1.1)**: as 4 em dúvida (020, 022, 023, 035) mais a 024 (já checada na 19.1) — todas confirmadas aplicadas em produção, testando o comportamento real de cada uma (RLS de `organizations` bloqueando gestor de editar, `incidents.description` aceitando insert, trigger de Timeline pra Atividade avulsa disparando, tabela `manager_availability_blocks`/coluna `meetings.is_emergency` existindo). Nenhum bug encontrado aí. **`tsc -b --noEmit` (1.2)**: rodado do zero, limpo — a checagem de tipos não é mais um no-op em nenhum lugar do projeto atual. **Pipeline de integrações (1.3)**: continua bloqueado, sem credencial real do Google/Meta Ads — nada a fazer aqui por enquanto. **Teste E2E instável (1.4)**: `approval-revision-task.spec.ts` rodado 8x seguidas, 8/8 passou — investigado o código (`respond_to_approval` faz tudo numa transação só, síncrono, e o hook do frontend usa `await` de verdade antes de atualizar a tela, sem optimistic update) e não existe corrida real ali; o flake da Fase 8.4 foi provavelmente um problema pontual do ambiente, não repetiu. **Fuso horário (1.5) — bug real encontrado e corrigido, maior do que o documento apontava**: o banco roda em UTC; três funções (Health Score, "Cliente em risco", "Meta atrasada") comparavam datas com `current_date` puro, sem converter fuso nenhum — durante a janela diária em que UTC já virou o dia mas o horário local ainda não, metas/tarefas de hoje eram contadas como atrasadas cedo demais. As outras três que já convertiam fuso (`apply_workflow`, `apply_client_workflow`, `request_emergency_meeting`) estavam certas na mecânica mas erradas no fuso (Brasília, não Manaus — corrigido a pedido do usuário). `migration-043` redefine as 6 funções pra `America/Manaus`; testado ao vivo (chamando as 6 pelo RPC, sem erro, e conferindo que `apply_workflow` com `due_days: 3` calculou exatamente "hoje em Manaus + 3 dias")

## Fase 20 — itens 🟡/🟠/🟢 pendentes do documento "Fases 19 e 20 finais"

A Fase 19 resolveu o achado 🔴 e os itens da seção 1 ("achados novos") do documento cruzado. A seção 2 ("Plano por prioridade") do mesmo documento tinha vários itens 🟡 Alto e 🟠 Médio que a Fase 19 não chegou a tocar, além dos 🟢 Baixo/limpeza — por decisão do usuário, a Fase 20 fecha tudo isso (não só o 🟢 como estava combinado antes). Ficam de fora, documentados como pendência pra decidir depois: verificação de marca do Google, política de privacidade/LGPD, monitoramento de erro tipo Sentry (precisaria de dependência nova), e 4 ideias de funcionalidade (alertas por limiar de métrica, PDF de relatório, lembrete de renovação de contrato, resumo semanal).

- [x] **Fase 20.1 — concluída** — segurança/robustez das Edge Functions. **Erro cru pro navegador**: `cassie/index.ts` e `integrations/index.ts` não tinham um único `console.error` — todo erro (Postgres, OpenAI, Google Ads, Meta Ads/Graph) ia direto pro corpo da resposta HTTP via `error.message`, sem nenhum log no servidor. Agora todo erro é logado por completo no servidor (`console.error`, visível em Edge Functions > Logs) e o navegador recebe só uma mensagem genérica por categoria (banco de dados / IA / qual plataforma de anúncio falhou), nos ~30 pontos dos dois arquivos (chat, persuasive-copy, connect, callback, sync, sync-all, list-campaigns, forms-webhook, mais os 2 catch-all). **Comparação de segredo em tempo constante**: os 3 pontos que comparavam segredo (`X-Cron-Secret` e `X-Webhook-Secret` em `integrations/index.ts`, `X-Notifications-Secret` em `notifications/index.ts`) usavam `!==` puro — trocado por `timingSafeEqual` (`node:crypto`), que não dá pista de "quão perto" uma tentativa errada chegou. **`handlePersuasiveCopy` valida `client_id`**: antes só conferia se o campo tinha sido enviado, não se o cliente existia de verdade (diferente de `handleChat`, que já fazia essa checagem) — corrigido, mesmo padrão agora nos dois. Testado ao vivo depois do usuário rodar o deploy dos 3 arquivos: segredo errado em `sync-all`/`tick` → 401 (comparação seguindo funcionando); `connection_id` inválido em `integrations/campaigns` → confirmado que a resposta virou a mensagem genérica (antes vazava o erro cru do Postgres "invalid input syntax for type uuid..."); `client_id` inexistente em `persuasive-copy` → 404 "Cliente não encontrado", sem seguir adiante
- [x] **Fase 20.2 — concluída** — RBAC em profundidade + erros visíveis no front. **`RoleRoute` no Portal Cliente** (`App.tsx`): as 7 rotas exclusivas do cliente (`/project`, `/tasks`, `/files`, `/comments`, `/meetings`, `/cassie`, `/reports`) agora ficam dentro de `RoleRoute allowedRoles={['cliente']}`, mesmo padrão já usado no bloco gestor — `/` e `/settings` ficam de fora de propósito (`/` já tem redirecionamento próprio por papel em `Home.tsx`, `/settings` é compartilhada pelos 3 papéis). **`onError` + toast em todas as mutations sem tratamento**: eram ~63 no total (bem mais que os ~10 do documento) em `useClientPortalData.ts`, `useManagerPortalData.ts` e `useNavSeen.ts` — cada uma ganhou uma mensagem específica, seguindo o padrão já usado em `useCassieMessages.ts`/`usePersuasiveCopyMessages.ts`/`useManagerAvailability.ts`. **Bug real encontrado durante a verificação**: várias telas já tinham seu próprio aviso de erro (`try/catch` com `toast.error` local) ao redor dessas mesmas mutations — o aviso novo no hook duplicava o toast (2 avisos pra 1 falha só). Corrigido em 33 arquivos: removido o toast duplicado, mantendo a estrutura de `try/catch` onde ela servia pra outra coisa (ex: só limpar o formulário ou fechar o diálogo em caso de sucesso). Um caso à parte, `useRequestEmergencyMeeting` e `useEnrollMeetingRecurrence`: o RPC lança mensagens específicas de regra de negócio (ex: "Você já solicitou uma reunião de emergência este mês", "Defina o plano do cliente... antes de configurar a recorrência") — em vez de mensagem genérica, o `onError` desses dois hooks repassa `err.message`, preservando a informação útil (mesmo padrão que `useSendCassieMessage` já usava). **`isError` nas 4 páginas que só olhavam `isLoading`** (`Dashboard.tsx`, `Reports.tsx`, `Project.tsx`, `ClientDetail.tsx`): agora mostram "Erro ao carregar os dados. Tente novamente." nas queries principais que já participavam do gate de carregamento, em vez de renderizar como se fosse só "sem dado ainda". Testado ao vivo com Playwright: gestor tentando abrir `/project` → barrado com toast "Acesso negado" + redirecionado; cliente e admin continuam navegando normalmente nas próprias rotas; falha forçada (rota de rede interceptada) ao enviar um comentário → exatamente 1 toast de erro, não 2; falha forçada ao carregar `performance_snapshots` → Dashboard mostra a mensagem de erro depois das tentativas automáticas do TanStack Query se esgotarem. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos em cada rodada
- [x] **Fase 20.3 — concluída** — limpeza de código. **Código morto removido**: `useCreateMeeting`/`NewMeetingInput` (`useClientPortalData.ts`, sem nenhum uso — o fluxo real de reunião do cliente é `useRequestEmergencyMeeting`) e o componente `IncidentList.tsx` (sem nenhum import). **`fetchLatestUpdatedAt`/`latestOf` unificados**: estavam duplicados entre `useClientPortalData.ts` (filtrando por `client_id`) e `useManagerPortalData.ts` (sem filtro, vê todos os clientes) — extraídos pra um novo arquivo `src/lib/nav-activity.ts`, `fetchLatestUpdatedAt(table, clientId?)` unifica as duas versões com o filtro opcional. Testado ao vivo: pontinho de "nova atividade" no menu continua respondendo certo nos dois portais (cliente e gestor), sem erro no console. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos
- [x] **Fase 20.4 — concluída** — verificações finais, nenhum bug encontrado (nenhuma mudança de código necessária). **RLS das 7 tabelas novas**: já confirmado durante a investigação da Fase 20 (ver nota no início desta seção) — todas com RLS habilitada e policies corretas pro jeito que são usadas hoje. **Edge cases de notificação, testados ao vivo** (Studio Prisma, RPCs `detect_new_meeting_reminders_15m`/`detect_new_at_risk_clients`): remarcar uma reunião 3x seguidas dentro da janela de 15min gera só 1 notificação por mudança de horário de verdade, nunca duplica numa chamada repetida sem mudança; cliente saindo e voltando a ficar "em risco" (incidente crítico criado → resolvido → criado de novo) notifica de novo corretamente quando a saída é observada entre 2 checagens — e, quando a saída e a volta acontecem rápido demais pra caber entre 2 checagens do cron (1 por minuto), o sistema trata como se não tivesse saído e não manda aviso extra (comportamento correto, evita spam, não é bug). **Descoberta lateral, não é bug**: uma meta SMART de teste antiga (Studio Prisma, prazo `2026-08-11`) tinha ficado vencida só pelo calendário ter avançado desde que foi criada em fase anterior, deixando esse cliente de teste permanentemente "em risco" — neutralizada durante o teste e restaurada exatamente como estava ao final. **Service Worker atualiza sozinho a cada deploy**: confirmado — `vite.config.ts` já usa `registerType: 'autoUpdate'` (desde a Fase 10b) e o `sw.js` gerado tem `skipWaiting()`/`clientsClaim()`, então uma aba aberta assume a versão nova sozinha, sem precisar de F5 forçado. **Conta de deploy / Business Verification do Meta / backup automático do Supabase**: nenhum dos 3 é verificável por mim (painel da Vercel, painel de desenvolvedor do Meta, plano de billing do Supabase) — pendente de confirmação do usuário.

- [x] **Fase 20.5 — concluída** — item 1.3 do documento cruzado ("pipeline de integrações nunca foi validado com credencial real") finalmente testado de ponta a ponta, com uma conta real de Google Ads — e revelou 4 bugs reais que nenhum teste anterior (só com contas de teste vazias) tinha conseguido pegar:
  1. **`handleCallback` nunca conferia erro em 2 updates** (salvar `oauth_tokens`, marcar `status: 'connected'`) — se qualquer um falhasse, o código seguia até a mensagem de sucesso mesmo sem nada salvo de verdade. Corrigido (Fase 20.1 já tinha corrigido o vazamento de erro cru; aqui foi adicionado o `if (error)` que faltava nos 2 pontos, com log detalhado).
  2. **Gatilho de auditoria `log_digital_asset_connection_event` quebrado desde que foi criado** (bem antes da Fase 20): gravava a severidade do log como `text` solto em vez do enum `severity_level` — o Postgres rejeitava, e por rejeitar *dentro* do gatilho, desfazia o `UPDATE` inteiro que o disparou. Resultado: nenhuma conexão OAuth (Google/Meta Ads) jamais conseguiu ficar "conectada" de verdade, silenciosamente, até esse teste. Corrigido via `migration-044` (só adiciona o cast `::public.severity_level`, mesmo valor).
  3. **`GOOGLE_ADS_API_VERSION` fixo em `v17`**, uma versão já desativada pelo Google havia meses (confirmado via busca na web — a atual é v25) — causava um erro de "resposta não é JSON" (a Google devolve uma página de erro HTML pra versão desativada). Corrigido pra `v25`.
  4. **Suporte a conta gerenciadora (MCC) inexistente**: a conta real do usuário é uma MCC com várias contas de anúncio por baixo — a API do Google Ads exige o cabeçalho `login-customer-id` pra qualquer chamada em nome de uma conta-cliente, e o código nem tinha esse conceito (só pegava a primeira conta que aparecesse, podendo ser a própria gerenciadora, sem campanha nenhuma). Implementado do zero: nova coluna `digital_asset_connections.login_customer_id` (`migration-045`), função `discoverGoogleAdsClientAccounts` que percorre `customer_client` pra achar só contas de anúncio de verdade (gerenciadora **nunca** é uma opção escolhível, por pedido explícito do usuário), escolha automática quando só existe 1 conta (caso comum, sem MCC), e 2 rotas novas (`/accounts`, `/select-account`) + diálogo novo (`SelectGoogleAdsAccountDialog.tsx`) pra escolher manualmente quando há mais de uma.
  5. **Achado à parte, não é bug — pedido do usuário**: conectar a mesma conta real em 2 Ativos Digitais diferentes do mesmo cliente estava duplicando dado sincronizado (nada impedia isso) — `handleConnect` agora bloqueia com uma mensagem clara: só 1 conexão de Google Ads e 1 de Meta Ads por cliente.

  Cada bug foi encontrado ao vivo, um de cada vez, direto com o usuário testando em produção e trocando prints/logs reais — não foi possível reproduzir em teste automatizado (precisa de credencial real). `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos em cada rodada de correção.

- [x] **Fase 20.6 — concluída** — Meta Ads configurado + Política de Privacidade/Termos de Uso (pré-requisito descoberto pro Google Ads sair do modo de teste). **Meta Ads**: usuário criou o app em developers.facebook.com (caso de uso "Medir desempenho dos anúncios", que já inclui `ads_read`), cadastrou `META_APP_ID`/`META_APP_SECRET` no Supabase, precisou liberar o domínio da função (`ktexhzcpqrqjdgzgxisx.supabase.co`) em "Domínios do app" + a URL completa do callback em "URIs de redirecionamento OAuth válidos" (Login do Facebook → Configurações) — testado ao vivo, conexão completa (login funcionou). Confirmado no banco que `external_account_id` ficou vazio nessa tentativa (login usado ainda não tem nenhuma conta de anúncios de verdade vinculada) — não é bug, mesmo padrão já visto no Google Ads. Pendente do usuário: nível "Advanced Access" do `ads_read` (só assim dá pra ler conta de cliente real, não só contas próprias) — processo de revisão do Meta, sem pré-requisito de volume pra o tamanho da agência hoje; verificação da empresa na Business Manager fica pra depois, por decisão do usuário (conta nova, evitando risco de bloqueio por atividade repentina — usuário já cuidando de "aquecer" a conta por conta própria). **Política de Privacidade + Termos de Uso**: descoberto que publicar a tela de consentimento OAuth do Google (pré-requisito pra pedir "Basic access" do token do Google Ads) exige uma Política de Privacidade hospedada no próprio domínio — item que estava fora de escopo da Fase 20 virou pré-requisito bloqueando o passo seguinte. Duas páginas novas e públicas (`/privacy`, `/terms`, fora do `ProtectedRoute` de propósito — precisam abrir sem login pro Google verificar), conteúdo real específico do app (não genérico): quais dados o app coleta por categoria, com quem compartilha por nome (Supabase/OpenAI/Google/Meta/Vercel), direitos LGPD, etc. — com aviso explícito no topo de que é rascunho, ainda sem revisão jurídica. Linkadas no rodapé do Login e, a pedido do usuário, também na aba "Usuário" de Configurações (`UserSettingsTab.tsx`) — assim quem já está logado (qualquer papel) também acha, abrindo em nova aba pra não perder o que estava fazendo. Testado ao vivo: as duas abrem sem sessão logada, link do rodapé funciona. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 20.7 — concluída** — descoberto durante a verificação de marca do Google: a tela de consentimento OAuth mostrava o domínio técnico da função (`ktexhzcpqrqjdgzgxisx.supabase.co`) em vez do nome/logo do app, porque o `redirect_uri` do fluxo do Google (Ads/Forms) apontava direto pra esse domínio — que ninguém consegue verificar posse (só a própria Supabase), então nunca bateria com os "Domínios autorizados" da marca (`ametistaconversoesapp.vercel.app`). Corrigido roteando o retorno do login primeiro pelo domínio do próprio app: `vercel.json` ganhou uma rota nova (`/oauth/google-callback` → proxy pra função real no Supabase, a Vercel encaminha a query string sozinha) e `integrations/index.ts` ganhou `buildGoogleRedirectUri()`, usada em `handleConnect` (montar o link de autorização) e `handleCallback` (troca do código pelo token — precisa mandar exatamente o mesmo valor usado na autorização, senão a Google recusa) — só pro branch Google, Meta Ads continua igual (não tem esse problema, o ícone dele vem de configuração própria no app do Meta). **Exige um passo manual do usuário**: cadastrar a URI nova (`https://ametistaconversoesapp.vercel.app/oauth/google-callback`) em "URIs de redirecionamento autorizados" do Cliente OAuth no Google Cloud Console, senão a conexão quebra (a antiga pode continuar cadastrada, sem problema). Testado ao vivo: usuário refez a conexão, tela de consentimento passou a mostrar o e-mail do desenvolvedor em vez do domínio técnico — confirma o conserto. **Bug real encontrado na sequência, também ao vivo**: depois de completar o login, a página ficava em branco em vez de mostrar Ativos Digitais — causa: o Service Worker do PWA intercepta toda navegação e serve o app do cache (estratégia padrão de app shell), mas `/oauth/google-callback` só existe no nível do servidor (proxy do `vercel.json`), não é uma rota do React Router — a navegação nunca chegava na rede de verdade, e a rota desconhecida renderizava em branco. Corrigido com `navigateFallbackDenylist: [/^\/oauth\//]` no `workbox` do `vite.config.ts` (`vite.config.ts`) — confirmado no `sw.js` gerado que o `NavigationRoute` ganhou `denylist:[/^\/oauth\//]`, excluindo essa rota do app shell (sem suporte offline nela, o que é o esperado — só faz sentido com rede real). `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

**Fase 20 concluída** (20.1 a 20.7). Fora de escopo por decisão do usuário: monitoramento de erro tipo Sentry, e as 4 ideias de funcionalidade do documento cruzado (alertas por limiar de métrica, PDF de relatório, lembrete de renovação de contrato, resumo semanal). Pendências externas em andamento, nenhuma delas é algo que eu resolvo sozinho — passo a passo já repassado ao usuário na conversa: Google Ads "Basic access" (aplicação enviada, aguardando revisão do Google, dias); verificação de marca do Google Cloud (Search Console confirmado, aguardando o Cloud Console reconhecer — atraso de sincronização conhecido do Google); Meta Ads "Advanced Access" (aguardando revisão do Meta).

## Fase 21 — itens que tinham ficado fora de escopo da Fase 20 + Fase 22 — detalhe de métrica com gráfico

Retoma, por pedido do usuário, os 4 itens que a Fase 20 tinha deixado de fora: monitoramento de erro tipo Sentry (21.1), alerta por limiar de métrica configurável por cliente (21.2, junto de remover a aba "Cliente" inútil das Configurações do gestor), histórico mensal de métricas com relatório em PDF (21.3), e lembrete de renovação de contrato (21.4) — resumo semanal ficou de fora por pedido explícito do usuário. Fase 22 é um pedido novo: cada card de métrica (KPI) vira clicável, abrindo valor atual + média do período + gráfico de linha, começando pelo Portal do Cliente (que já tem histórico diário salvo).

- [x] **Fase 21.1 — concluída** — monitoramento de erros tipo Sentry, mas próprio (sem depender de conta externa, por decisão do usuário). **Banco** (`migration-046`): tabela `error_logs` (`source`, `function_name`, `message`, `stack`, `context` jsonb, `severity` reaproveitando o enum `severity_level` já existente, `resolved`) — insert liberado geral (até `anon`, erro pode acontecer antes do login), leitura/edição só admin/gestor. **Captura front-end**: `src/lib/error-logging.ts` (`logClientError`, nunca lança se o próprio insert falhar) chamado por um `ErrorBoundary` novo (`src/components/ErrorBoundary.tsx`, envolve o app inteiro em `main.tsx`, mostra tela de "Algo deu errado" com botão de recarregar em vez de branco) e por `window.addEventListener('error'/'unhandledrejection')` (pega erro fora do ciclo de render do React). **Captura Edge Function**: os helpers `dbErrorResponse`/`platformErrorResponse`/etc. das 3 funções (`cassie`, `integrations`, `notifications`, já existiam desde a Fase 20.1) ganharam uma função `logServerError` nova, chamada nos mesmos ~35 pontos de sempre mais os 3 catch-all — sem criar nenhum ponto de captura novo. **Tela**: lista de erros mais recentes primeiro (limite de 200), toggle "mostrar todos" (por padrão só os não resolvidos), botão marcar como resolvido, "Ver detalhes" expande stack/contexto, modo de excluir reaproveitando `DeleteModeToggle`/`DeleteItemButton` já existentes — por pedido do usuário, mora dentro de Configurações (`ErrorLogsSettingsTab.tsx`, nova aba "Erros" visível só pra admin/gestor, mesma condição já usada nas abas Agência/Disponibilidade) em vez de item de menu próprio; item de menu e página avulsa que tinham sido criados primeiro foram removidos na sequência. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 21.2 — concluída** — alerta automático por limiar de métrica (por cliente) + remoção da aba "Cliente" inútil das Configurações do gestor. **Banco** (`migration-047`): tabela `metric_alert_thresholds` (`client_id`, `metric` — cpa/roas/ctr/spend/revenue —, `comparison` — acima/abaixo —, `threshold_value`, `enabled`, 1 linha por métrica por cliente), só admin/gestor lê/edita. Função `check_metric_alert_thresholds()` (mesmo molde de `detect_new_at_risk_clients` etc.) confere cada limiar ativo contra os últimos 7 dias de `performance_snapshots`, usando **as mesmas fórmulas já usadas no front-end** (`aggregateSnapshotKpis`/`computeRevenueFromLeads`/`computeRoas` em `src/lib/metrics.ts`) — CPA = gasto ÷ conversões, CTR = (cliques ÷ impressões) × 100, Receita/ROAS dependem das premissas do cliente (leads p/ fechar + ticket médio) e ficam de fora da checagem se não configuradas, igual o app já faz. Cria um alerta em `alerts` (categoria `limiar_metrica`) só se ainda não existir um não-resolvido igual (título determinístico por métrica+comparação evita duplicar a cada sync). Chamada direto do `handleSyncAll` (`integrations/index.ts`), logo depois do sync de cada rodada do cron de 6 em 6h — sem cron novo. **UI**: novo card "Alertas por Limiar de Métrica" na Central de Informações do Cliente (`MetricAlertThresholdsCard.tsx`), uma linha por métrica com toggle/comparação/valor, 1 botão "Salvar alertas" upserta as métricas preenchidas de uma vez. **Aba "Cliente" das Configurações**: só aparece mais pra quem tem role `cliente` (`Settings.tsx`) — pra admin/gestor ela só mostrava uma mensagem de beco sem saída, sem nenhuma utilidade; removido também esse branch morto de `ClientSettingsTab.tsx`. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 21.2b — concluída** — 2 bugs reais encontrados testando ao vivo (usuário configurou os 5 limiares e forçou uma sincronização, nenhum alerta apareceu). **Bug 1, real e era a causa principal**: o botão "Sincronizar agora" do front-end chama a rota `/sync` (autenticada por login, um clique), não `/sync-all` (só o cron chama essa, com segredo) — a checagem de limiar só tinha sido ligada em `handleSyncAll`, então um teste manual nunca disparava a checagem. Corrigido: `handleSync` agora também chama `check_metric_alert_thresholds()` depois de sincronizar com sucesso. **Bug 2, de usabilidade**: o checkbox "ativo" de cada métrica ficava sem rótulo nenhum ao lado do nome da métrica, fácil de preencher o valor e esquecer de marcar — sem marcar, a linha salva com `enabled: false` e a checagem pula ela. Corrigido: digitar um valor já liga o alerta sozinho (`enabled: true` automático ao digitar), e o checkbox ganhou o rótulo "Ativo" visível, movido pro fim da linha, pra quem quiser desligar sem apagar o número. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos. **Diagnóstico ao vivo direto no banco** (script descartável, removido depois): confirmado que os 5 limiares da Loja Aurora tinham salvo `enabled: false` (já corrigidos manualmente, `enabled: true` os 5); confirmado que a conexão real de Google Ads desse cliente nunca chegou a sincronizar dado de verdade (`external_account_id` vazio, ainda esperando o "Basic access" do Google) — o único dado existente é um teste antigo (16/jul a 04/ago), fora da janela de 7 dias e sem conversões. Simulado 1 dia de dado de teste temporário pra provar que a checagem funciona (CPA/CTR/Investimento dispararam certo, ROAS/Receita ficaram de fora certo por falta de "Leads p/ fechar"/"Ticket médio" configurados) — removido tudo (dado de teste + alertas de teste) depois de confirmar.

- [x] **Fase 21.3 — concluída** — histórico mensal de métricas + relatório em PDF, na aba Relatórios do Portal Cliente. **Banco** (`migration-048`): tabela `client_monthly_reports` (1 linha por cliente por mês — `spend`, `revenue`, `roas`, `cpa`, `ctr`, `clicks`, `impressions`, `conversions`, `health_score` no momento do fechamento), cliente só lê o próprio, admin/gestor lê tudo. Função `generate_monthly_client_reports(p_ref_month?)` agrega `performance_snapshots` do mês pedido (por padrão, o mês anterior) por cliente, com as MESMAS fórmulas do front-end/Fase 21.2 (CPA = gasto ÷ conversões, CTR = cliques ÷ impressões × 100, Receita/ROAS dependem das premissas do cliente) — upsert, roda de novo sem duplicar. Cron novo `close-monthly-reports` (dia 1 de cada mês, 03:00 UTC) chama a função direto (agregação pura em SQL, sem API externa, não precisa de Edge Function). **UI**: nova aba "Histórico Mensal" em Relatórios — `MonthYearPicker.tsx` novo (seletor de mês/ano granular, não usa o `Calendar` de dia a dia que já existe, granularidade diferente) escolhe o mês; mês com fechamento oficial mostra os dados salvos; mês atual (ainda sem fechamento) agrega ao vivo dos `performance_snapshots` do próprio mês (`aggregateSnapshotKpisForMonth`, nova em `metrics.ts`, mesmas fórmulas de `aggregateSnapshotKpis` mas filtrando por mês em vez da janela rolante de 30 dias) com aviso "mês em andamento"; mês sem nenhum dado mostra "Sem dados para este mês". **PDF**: `jspdf` instalado (depêndencia nova aprovada pelo usuário) — `src/lib/pdf-report.ts`, gera e baixa o arquivo 100% no navegador (nome da agência, cliente, período, métricas do mês, data de geração), sem round-trip de backend. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 21.3b — concluída** — usuário pediu mais informação no Histórico Mensal: Lucro, e gráficos de canal + outros gráficos + metas. **Lucro** (Receita − Investimento) novo card, calculado tanto pro mês fechado quanto pro mês em andamento (fica "—" sem Receita configurada, mesma regra de sempre); entrada nova em `kpiDescriptions.lucro`. **Investimento vs. Receita por canal (do mês)**: novo `groupByChannelForMonth` em `metrics.ts` — investimento é real (soma de `performance_snapshots` por canal no mês), receita por canal é estimada (Receita total do mês, distribuída proporcionalmente pela fatia de conversões de cada canal — não tem receita reportada por canal em nenhuma plataforma, mesma lógica de atribuição já usada pro total do cliente); reaproveita o `SpendRevenueBarChart` que já existia. **Tendência do mês**: reaproveita o `PerformanceTrendChart` que já existia (aba Tráfego), só filtrando os retratos diários pro mês selecionado. **Metas**: reaproveita o `GoalsProgressCard` que já existia (Dashboard do cliente) — mostra a situação ATUAL das metas SMART, com aviso explícito de que não é um histórico por mês (metas não têm fechamento mensal, diferente das métricas de performance). **PDF**: ganhou a linha de Lucro e uma tabela de investimento/receita por canal. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 21.3c — concluída** — usuário reportou números "incoerentes" entre a tela e o PDF (gráfico mostrando "Meta Ads" com valores altos, PDF mostrando "Sem canal" com valor bem menor). **Investigado ao vivo direto no banco** (script descartável, removido depois) — não era bug de código, eram 2 causas reais: (1) o título "Investimento vs. Receita por canal" está em 2 abas diferentes com fontes de dado completamente diferentes — a aba "Distribuição & Canais" usa campos manuais antigos do projeto (`projects.spend/revenue/channel`, histórico inteiro, sem relação com sincronização real, mesmo campo que a Fase 19.1b já tinha desativado do Dashboard/Relatórios principal mas nunca removeu dessa aba específica), enquanto o gráfico novo do "Histórico Mensal" usa o dado real sincronizado (`performance_snapshots`) só do mês selecionado — são literalmente 2 relatórios diferentes com o mesmo nome, confundindo qual é qual; (2) o dado real sincronizado desse cliente de teste é sobra de teste antigo (Fase 19.1, semanas atrás) sem `channel` nem `conversions` preenchidos — por isso cai tudo em "Sem canal" e Receita fica zerada (o pipeline de sync atual já grava `channel` corretamente pra sincronização nova, `channel: connection.provider`, só essa sobra antiga é que não tem). **Corrigido**: os 2 títulos agora deixam claro qual é qual ("...(todos os projetos)" vs "...(mês selecionado)"), com uma nota explicando a diferença na aba antiga. **Pedido extra do usuário**: PDF ganhou 2 seções que faltavam — tendência diária do mês (tabela com paginação automática, antes só aparecia na tela) e metas SMART, com uma fórmula nova só pro PDF (a pedido explícito do usuário): "probabilidade de cumprimento" = (já feito ÷ meta) × 100, **sem limitar em 100%** (diferente da barra de progresso na tela, que continua limitada visualmente a 100% por ser uma barra) — confirmado com o exemplo do próprio usuário (2000 de meta 1000 = 200%). `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 21.3d — concluída** — usuário notou que faltava "Gasto" nas Métricas do mês. Achado real ao investigar: "Lucro" no Histórico Mensal/PDF estava descontando só o Investimento em mídia, diferente do "Resumo financeiro" que já existe na aba Financeiro (`FinancialSummaryCard.tsx`), que desconta Investimento **+ Mensalidade da agência** — inconsistência entre 2 partes do mesmo app calculando "Lucro" de dois jeitos diferentes. Corrigido pra usar a MESMA fórmula nos dois lugares: `MonthlyReportPdfData` ganhou `monthlyFee`, novo card "Gasto Total" (Investimento + Mensalidade) na tela e no PDF (que também ganhou a linha "Mensalidade da agência", mesma discriminação do Resumo financeiro), Lucro recalculado com o Gasto Total em vez de só o Investimento. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

- [x] **Fase 21.3e (achado ao vivo, corrigido direto no banco)** — usuário notou "Sem canal" no gráfico por canal mesmo tendo investimento real. Investigado ao vivo: 21 linhas de `performance_snapshots` (todas de um mesmo projeto, "Campanha de Verão", canal "Meta Ads") estavam com `channel = null` — sobra de teste da Fase 19.1, inserida direto no banco antes desse campo existir/importar; o pipeline de sync atual já grava `channel` certinho (`connection.provider`) em toda sincronização nova. Corrigido preenchendo o `channel` dessas 21 linhas com o canal do projeto associado — correção de dado, não de código, sem deploy necessário.

- [x] **Fase 21.4 — concluída** — lembrete de renovação de contrato (30/7/1 dias antes de `clients.renewal_date`), aviso interno pra admin/gestor (não é pro cliente). **Banco** (`migration-049`): estende o `check` de `kind` já existente em `push_notification_log` (Fase 11c) com `renewal_reminder_30d/_7d/_1d`; 3 funções novas `detect_new_renewal_reminders_30d/_7d/_1d()`, mesmo molde exato de `detect_new_meeting_reminders_1h/_15m` — dispara quando `renewal_date - current_date` é exatamente 30/7/1 (a condição fica verdadeira o dia inteiro, então o tick de 1 em 1 minuto sempre pega, e o dedup por `unique(kind, entity_id)` garante 1 disparo só); gatilho novo `clear_renewal_reminder_log` limpa o registro de dedup se a data de renovação mudar (senão ficaria bloqueado pra sempre de avisar de novo numa data nova, mesmo padrão dos gatilhos de reunião/meta já existentes). **Edge Function** (`notifications/index.ts`): `dispatchRenewalReminder(kind, when)` novo — manda só pra admin/gestor (`getAdminGestorUserIds`), título "Renovação em X dias"; `handleTick` passa a chamar as 3 funções novas junto com as 4 que já existiam. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos (Edge Function não passa por esses comandos, é Deno — conferido visualmente contra o padrão já existente no arquivo).

**Fase 21 concluída** (21.1 a 21.4, mais os achados/correções ao vivo 21.2b, 21.3b-e, todos documentados acima).

## Fase 22 — clique no card de métrica abre detalhe (valor atual, média, gráfico de linha)

- [x] **Fase 22 — concluída** — cada `KpiCard` do Dashboard e de Relatórios (Portal Cliente) virou clicável, abrindo um `MetricDetailDialog` novo com valor atual (o mesmo já mostrado no card, nunca recalculado à parte, pra nunca destoar), média de todo o histórico diário disponível, e um gráfico de linha desse histórico — escopo combinado com o usuário: só Portal Cliente por enquanto (Dashboard executivo do gestor não tem histórico salvo hoje, fica pra depois). **`KpiCard.tsx`**: `onClick` opcional novo — vira `role="button"` com `cursor-pointer` e navegação por teclado (Enter/Espaço); o botão de ajuda (help icon) já tinha `stopPropagation` desde a Fase 4, então continua funcionando sem abrir os dois ao mesmo tempo. **`buildMetricSeries`** novo em `metrics.ts` — agrupa os retratos por data (mesmo padrão de `buildTrendSeries`) e aplica, dia a dia, a MESMA fórmula já usada em `aggregateSnapshotKpis`/`computeRevenueFromLeads`/`computeRoas`, sem tabela nova nem fetch novo (os retratos já vêm carregados na página). **`MetricDetailDialog.tsx`** novo — reaproveita o `Dialog` já usado no app (não existe `Sheet` na base) e as mesmas convenções de cor/grade/tooltip do `PerformanceTrendChart` já existente (linha roxa, `CHART_COLORS.investimento`, mesma paleta validada do resto do app). Ligado em **5 cards do Dashboard** (Investimento, Receita, ROAS, CPA, Conversões) e **10 cards de Relatórios** (abas Tráfego e Financeiro — CTR médio, ROAS, Impressões, Cliques, CPC, Taxa de Conversão, Investimento, Receita, CPA, Conversões); Health Score ficou de fora de propósito (não tem histórico diário salvo, só o valor atual recalculado todo dia). `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos — **não testado visualmente num navegador nesta rodada** (precisaria de login de um cliente de teste com dado sincronizado; os componentes reaproveitam 100% padrões já em produção — `Dialog`, `Recharts`, `KpiCard`/`Popover` — então o risco é baixo, mas vale confirmar ao vivo depois do deploy).

- [x] **Fase 22b — concluída** — usuário reparou que a aba "Histórico Mensal" (adicionada depois do plano original da Fase 22) tinha ficado de fora do clique-pra-detalhe. Estendido: `selectedMetric` de Relatórios ganhou um `scope` (`'all'` = abas Tráfego/Financeiro, histórico inteiro; `'month'` = aba Histórico Mensal, só os retratos do mês escolhido no seletor) — o MESMO card (ex: CPA) mostra um gráfico diferente dependendo de onde foi clicado, com o título do dialog deixando isso explícito ("CPA (mês selecionado)"). Ligado em 6 dos 8 cards da aba mensal (Investimento, Receita, ROAS, CPA, CTR médio, Conversões); **"Gasto Total" e "Lucro" ficaram de fora de propósito** — dependem da mensalidade da agência, um valor só do mês inteiro, sem jeito natural de virar uma série diária sem inventar uma distribuição arbitrária. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

**Pendência anotada, por pedido explícito do usuário ("posteriormente")**: levar esse mesmo clique-pra-detalhe pro Health Score no Portal Gestor — hoje não é possível sem trabalho novo, já que `clients.health_score` não tem histórico diário salvo (só o valor atual, recalculado todo dia); precisaria de uma captura histórica nova antes de ter gráfico de verdade, mesma limitação já registrada quando o Health Score foi deixado de fora do clique-pra-detalhe do lado do cliente.

## Fase 23 — clique no card de métrica no Dashboard Executivo (Portal Gestor)

- [x] **Fase 23 — concluída** — usuário pediu pra trazer o clique-pra-detalhe também pro Dashboard Executivo do gestor (os 8 KPIs de lá — MRR Total, Clientes Ativos, Churn Rate, Incidentes Abertos, Clientes em Risco, Carga de Trabalho, Produtividade, Budget Gerenciado — eram calculados só ao vivo, `computeExecutiveKpis`, sem nenhum histórico salvo, diferente do cliente que já tinha `performance_snapshots` diário). **Banco** (`migration-050`): tabela nova `executive_kpi_snapshots` (1 linha por dia, as 8 métricas); função `capture_executive_kpi_snapshot()` recalcula as 8 com as MESMAS regras de `computeExecutiveKpis` (traduzidas pra SQL) e faz upsert por `snapshot_date` — mesmo molde já usado 2x nesta fase (`check_metric_alert_thresholds`, `generate_monthly_client_reports`). Cron novo `capture-executive-kpi-snapshot` todo dia às 03:00 UTC; a própria migration já chama a função 1x no final, pra já existir 1 ponto assim que rodar (sem esperar o cron) — o gráfico começa vazio e vai ganhando 1 ponto por dia daqui pra frente, sem como reconstruir o passado. **Front-end**: `buildExecutiveMetricSeries` novo em `manager-metrics.ts` (mesma ideia de `buildMetricSeries` da Fase 22, só que os pontos já vêm prontos da tabela); `useExecutiveKpiHistory` novo em `useManagerPortalData.ts`; `DashboardExecutivo.tsx` ganhou `onClick` nos 8 cards, reaproveitando o MESMO `MetricDetailDialog` já usado no Portal Cliente (Fase 22), sem componente de UI novo. Falha ao carregar o histórico não derruba a página (degrada pra "sem histórico ainda"). `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos — não testado visualmente num navegador (mesma ressalva já registrada na Fase 22).

- [x] **Fase 23b — concluída** — usuário percebeu, corretamente, que não havia lugar nenhum na Central de Informações do Cliente (Portal Gestor) pra ver Receita/Lucro/CPA/Cliques/Impressões/etc. de um cliente — só dava pra descobrir perguntando pra Cassie ou vendo um alerta de limiar disparar (confirmado lendo o arquivo inteiro antes de mexer). Novo card **"Métricas de Performance"** (`ClientPerformanceMetricsCard.tsx`), logo antes do card de Alertas por Limiar — 13 métricas (Investimento, Gasto Total, Receita, Lucro, ROAS, CPA, CTR médio, CPC, Taxa de Conversão, Cliques, Impressões, Conversões, Health Score), com as MESMAS fórmulas já usadas no Dashboard/Relatórios do próprio cliente — nunca mostra um número diferente do que o cliente vê do lado dele. Clique em qualquer card (exceto Gasto Total/Lucro/Health Score, mesma ressalva de sempre — sem série diária natural) abre o mesmo `MetricDetailDialog` já usado nas Fases 22/23. **Novo hook** `useClientPerformanceSnapshots(clientId)` — mesmo dado que `usePerformanceSnapshots` já busca pro cliente logado, só que pra um `client_id` arbitrário (o gestor pode ver qualquer cliente, não só o próprio). **Sem impacto nenhum em relações/schema do banco** — é leitura pura da mesma tabela `performance_snapshots` que já existe e já é usada em todo o resto do app, nenhuma tabela nova, nenhuma coluna nova, nenhuma mudança de RLS. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

## Fase 24 — histórico do Health Score + clique-pra-detalhe no Portal Gestor

- [x] **Fase 24 — concluída** — fecha a pendência deixada em aberto nas Fases 22/23: o card "Health Score" era o único, tanto no Portal Cliente quanto no Portal Gestor, sem clique-pra-detalhe, porque `clients.health_score` nunca teve histórico diário salvo (só o valor atual, recalculado 1x/dia). Usuário pediu pra fechar isso do lado do gestor. **Banco** (`migration-051`): tabela nova `client_health_score_snapshots` (`client_id` + `snapshot_date`, `unique` nos dois — 1 linha por cliente/dia), só admin/gestor lê. Em vez de um cron novo e separado (que rodaria em outro horário e podia ler o health_score de ANTES do recálculo do dia — problema real que teria acontecido se seguisse o molde exato da Fase 23), a captura do snapshot foi embutida DENTRO da própria função `recompute_client_health_score` (recriada com o corpo idêntico ao de `migration-043`, só acrescentando o `insert ... on conflict` no final) — assim o snapshot do dia é sempre gravado com o valor recém-calculado, tanto no cron das 6h (`recompute-client-health-scores`, já existente) quanto no botão manual "Recalcular agora", sem duplicar a leitura de `clients` nem criar um cron novo. A migration termina chamando `recompute_all_client_health_scores()`, pra já existir 1 ponto por cliente assim que rodar. **Front-end**: `buildHealthScoreSeries` novo em `manager-metrics.ts` (mesma ideia de `buildExecutiveMetricSeries`, pontos já vêm prontos); `useClientHealthScoreHistory(clientId)` novo em `useManagerPortalData.ts`; `ClientPerformanceMetricsCard.tsx` ganhou `onClick` no card "Health Score" (o único que faltava dos 13) — o tipo do estado local virou `MetricKey | 'healthScore' | null` pra acomodar o novo caso, que usa uma série/config próprias em vez do fluxo padrão de `buildMetricSeries`. Ficou fora do escopo desta rodada, por não ter sido pedido: Health Score no Portal Cliente (`HealthScoreGauge.tsx`, `Reports.tsx`) e os outros lugares do gestor que mostram Health Score sem ser um KpiCard clicável (`ClientCard.tsx`, `AtRiskClientsList.tsx`, o valor solto em `ClientDetail.tsx`). `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

## Fase 32 — Google Ads: tipo de campanha, Grupos de Anúncios, múltiplas campanhas por projeto, alerta de mudança de estado

Pedido em duas rodadas: deixar a leitura de dados do Google Ads mais fiel/rica (tipo de campanha, ad groups, impressão perdida, qualidade), depois ampliado com múltiplas campanhas por projeto e alerta automático de mudança de estado — nessa ordem, por prioridade do próprio usuário. Plano completo arquivado em `docs/interno/` (histórico de conversa).

- [x] **Fase 32.1 — concluída** — tipo de conversão por projeto (Vendas/Leads) + Receita automática. `migration-062`: `projects.conversion_type` (padrão `'leads'`). Projeto vinculado a uma campanha, com o cliente tendo Ticket Médio (e Leads pra Fechar, se for Leads) configurados, passa a calcular a Receita da Visão Geral automaticamente a partir das conversões sincronizadas — link "editar manualmente" sobrescreve, e uma vez editado o manual vence pra sempre.
- [x] **Fase 32.2 — concluída** — aba "Grupos de Anúncios" (`AdGroupsTab.tsx`) + Parcela de impressão perdida + Índice de Qualidade. Endpoint novo `/ad-groups` na Edge Function `integrations` (Google Ads, busca ao vivo sem histórico salvo); `migration-063` estende `campaign_performance_snapshots` com `search_rank_lost_impression_share`, `search_budget_lost_impression_share`, `budget_amount`.
- [x] **Fase 32.3 — concluída** — tipo de campanha, CPC médio, valor de conversão e resumos curados (dispositivo/geográfico/top termos de pesquisa/top palavras-chave/breakdown de conversão/demográfico/melhor dia-horário), pra Google Ads e Meta Ads. `migration-065` estende `campaign_performance_snapshots` com `campaign_type`, `conversion_value`. Endpoint novo `/campaign-insights` (só Google Ads).
- [x] **Fase 32.4 — concluída** — múltiplas campanhas por projeto. `migration-066`: tabela `project_campaign_links` substitui o link único antigo em `projects` (com backfill automático) — um projeto agora pode ter várias campanhas vinculadas (ex: Search + Performance Max juntas), CPA/CTR/Gasto/Receita somando todas.
- [x] **Fase 32.5 — concluída** — alerta automático de mudança de estado da campanha. `migration-067`: `project_campaign_links` ganha `last_known_status`/`last_known_budget`; função `check_campaign_state_changes()` (chamada a cada sync) cria um Alerta quando uma campanha vinculada pausa/é removida, ou o orçamento muda mais de 20%.
- [x] **Fase 32.6 — concluída** — apagar projeto e mudar status direto na lista de Projetos (Central de Informações). `migration-064` corrige a FK de `digital_asset_connections.project_id` (`on delete set null`) pra permitir apagar um projeto sem travar numa integração vinculada.

  `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos em cada etapa.

- [x] **Fase 32.7 (bug real, corrigido)** — usuário reportou que o badge de tipo de campanha não aparecia numa campanha de teste do Google Ads mesmo já vinculada corretamente a um projeto. Causa: tipo/status/orçamento só eram sincronizados como efeito colateral da consulta de métricas por dia, que não grava nenhuma linha pra campanha+dia sem atividade real (comum em conta de teste, gasto zero). Corrigido com uma consulta separada, independente de data, que sincroniza o metadado de toda campanha da conta mesmo sem gasto. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

## Fase 33 — Teste A/B de Campanhas

- [x] **Fase 33 — concluída** — aba "Testes" no projeto, construída a partir de uma spec escrita pelo próprio usuário (`docs/interno/testes ab caampanhaa.md`). `migration-068`: `projects.test_type` (nenhum/segmentação/anúncio/campanha) + `test_min_spend`; tabela `campaign_ad_change_log`. Cada campanha vinculada (Fase 32.4) vira uma variante comparada por CPA/CTR/Taxa de Conversão contra a média do grupo — variante com gasto abaixo do mínimo configurável fica marcada "dados insuficientes" e some do cálculo da média, mas continua na lista; variante que bate a média fica destacada. Modo "Segmentação" mostra o resumo de dispositivo/geográfico/demográfico de cada variante (reaproveita a Fase 32.3); modo "Anúncio" mostra um log manual de troca de criativo por campanha; modo "Campanha" só compara as métricas. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

## Fase 34 — Catálogo de Criativos e Segmentações

- [x] **Fase 34 — concluída** — ferramenta de organização/classificação de anúncios (texto/vídeo) e segmentações, sempre por cliente específico, sem reaproveitamento entre clientes, conectada ao Teste A/B (Fase 33). `migration-069`: tabela `catalog_entries` (`catalog_type` criativo/segmentação, `tipo` texto/vídeo só pra criativo, `conteudo`, `origem` IA/Forms/Manual, `status` Rascunho→Em Teste→Aprovado/Implementado→Descartado, `prioridade` Alta/Média/Baixa, `derivado_de` opcional apontando pra outra entrada do mesmo cliente/catálogo, `campaign_link_id` opcional apontando pro Grupo de Teste de um projeto). Vídeo não guarda o arquivo em si (navegador não abre arquivo local com um clique) — só a referência de texto do caminho esperado, no mesmo campo `conteudo` usado pelo texto do anúncio. **UI** (`CatalogCard.tsx`, 2 cards novos na Central de Informações do Cliente — Criativos e Segmentações): duas abas por catálogo, "Em Triagem" (Rascunho/Em Teste, ordenada por prioridade) e "Confirmados/Implementados", em vez de uma lista única; descartados ficam atrás de um link colapsável no rodapé, sem se perder. Entrada vinculada a um Grupo de Teste herda o resultado (CPA/CTR/Taxa de Conversão acima/abaixo da média do grupo) automaticamente — mesmo cálculo já usado na aba Testes (Fase 33), autocontido a partir só do `campaign_link_id` (`useCampaignLinkTestResult`), sem duplicar o número em lugar nenhum. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.
- [x] **Fase 34b — concluída** — 3 ajustes pedidos pelo usuário depois de usar a Fase 34 ao vivo. **Bug visual corrigido**: o badge de Prioridade ("Prioridade Baixa") quebrava em 2 linhas no formulário de adicionar — texto encurtado pra só o nome da prioridade + largura responsiva (`w-full sm:w-32`), mesmo padrão já usado nos outros campos do formulário. **Tipo de Criativo ganhou subtipos reais**: `migration-071` troca o "Texto" genérico por 3 componentes de anúncio de verdade — Headline, Descrição, Frase de destaque (cada um com placeholder próprio) — mais Vídeo, que continua igual; backfill automático migra qualquer entrada "Texto" já existente pra "Headline". **Avaliação em estrelas** (coluna `rating`, 1 a 5, nova nessa mesma migration): clicável em cada entrada de Criativo (clicar na mesma estrela desmarca), usada pra ordenar junto da prioridade. **Página global nova "Catálogo"** (`/catalog`, menu do Portal Gestor, `Catalog.tsx`): mostra Criativos/Segmentações de todos os clientes juntos (ou filtrado por um só), com busca por texto e o "modo de exclusão" (`DeleteModeToggle`/`DeleteItemButton`) padrão do resto do app — diferente do `CatalogCard` por cliente, que apaga direto sem confirmar. Extraído `CatalogEntryRow.tsx` (`src/components/catalog/`) compartilhado entre o card por cliente e a página global, pra não duplicar a lógica de badges/estrela/resultado herdado do Grupo de Teste. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.
- [x] **Fase 34c — concluída** — sem migration nova, só frontend. **Criação em massa**: extraído `CatalogAddForm.tsx` (`src/components/catalog/`) do formulário que já existia dentro do `CatalogCard`, ganhando um modo "Colar várias em massa" — troca a textarea de conteúdo único por uma grande, onde cada linha colada vira uma entrada separada (`useCreateCatalogEntries`, um único insert em lote pra todas as linhas, mesmo Tipo/Origem/Prioridade compartilhado entre elas) — pedido explícito do usuário pra cadastrar headlines/descrições em lote em vez de uma de cada vez, nunca uma headline partida em duas linhas nem duas headlines juntas na mesma linha. **Criar também na página global**: a página `/catalog` (Fase 34b, que só lia/editava/apagava) ganhou o mesmo `CatalogAddForm` — só aparece quando um cliente específico está selecionado no filtro (criar sem cliente definido não faz sentido); `useCreateCatalogEntry`/`useCreateCatalogEntries` passaram a invalidar também o cache `all-catalog-entries` (antes só invalidavam o cache por cliente, então uma entrada criada no `CatalogCard` de um cliente não aparecia na página global sem recarregar). **Reorganização da Central de Informações do Cliente** (`ClientDetail.tsx`, pedido explícito, "as coisas estão começando a ficar desorganizadas"): nova ordem — Cabeçalho → Contato/Plano/Renovação → Plataforma Escolhida → Tarefas/Metas SMART/Reuniões/Atividades → Métricas de Performance → Cliente em risco (se houver) → Projetos → Catálogo → Público-Alvo (se houver) → Observações internas → Acesso ao Portal → Alertas por limiar de métricas → Cassie IA — puramente reordenação de blocos JSX já existentes, nenhuma lógica mudou. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.
- [x] **Fase 34d — concluída** — 3 pedidos do usuário. **Limite de caracteres em Criativos**: `CATALOG_TIPO_MAX_LENGTH` (`src/lib/catalog.ts`) — Headline 15 caracteres, Descrição 90 (Frase de destaque e Vídeo sem limite); validado só no `CatalogAddForm` (contador + bloqueio de salvar/criar em massa acima do limite), de propósito sem constraint no banco pra não quebrar entradas antigas já maiores que isso. **Plataforma do projeto** (`migration-072`): `projects.platform` (`google_ads`/`meta_ads`) — selecionado na criação (`NewProjectDialog`, pré-selecionado Google Ads) e editável na aba Campanha (`ProjectDetailDialog`), mostrado como badge na Visão Geral do projeto e na lista de Projetos da Central de Informações; projeto anterior a essa fase (sem plataforma salva) simplesmente não mostra o badge, e o Select cai em "Google Ads" como fallback ao editar. **Palavras-chave em campanha de Pesquisa**: `projects.keywords` (mesma migration) — campo de texto livre igual ao Público-alvo (`icp`) já existente, só que só aparece na aba Campanha quando a campanha vinculada e sincronizada é do tipo Pesquisa (`campaignPerformance.data.campaignTypes.includes('SEARCH')`, dado real vindo do Google Ads, Fase 32.3) — documentação manual do gestor, diferente das "Top palavras-chave" sincronizadas que já existem na aba Grupos de Anúncios. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.
- [x] **Fase 34e — concluída** — sem migration, sem deploy de Edge Function. **Bug corrigido**: `ActivityTemplateCard.tsx` (Workflow de Atividades) truncava o título de cada item do checklist numa linha só (`truncate`) — removido, agora quebra em várias linhas igual ao Workflow Operacional (`WorkflowCard.tsx`) e ao Workflows do Cliente (`ClientWorkflowCard.tsx`), que já quebravam certo (nenhuma mudança neles). **Menu do Portal Gestor reordenado** (`nav-items.ts`, pedido explícito do usuário): Metas SMART e Atividades foram pra logo abaixo de Kanban; Ativos Digitais foi pra logo abaixo de Tarefas do Cliente (virando o 7º item); Timeline foi pro final, abaixo de Cassie IA. **Integrações virou sub-aba de Ativos Digitais**: a página própria `Integrations.tsx` foi apagada e seu conteúdo (lista de Conexões, busca, filtro por cliente, sincronizar, ver respostas de Forms) migrou pra dentro de `Assets.tsx` como uma 2ª aba ("Ativos"/"Integrações", cada uma com busca/filtro independentes) — `/integrations` continua existindo só como redirecionamento pra `/assets?tab=integracoes` (App.tsx), lendo o `tab` da query string pra abrir a sub-aba certa; o retorno do OAuth da Edge Function `integrations` continua apontando pra `/assets` (sub-aba padrão "Ativos"), sem precisar mudar nada no backend. `npx tsc -b --noEmit`, `npm run test` (46/46) e `npm run build` limpos.

## Fase 35 — Recorrência genérica em Workflows + Fechamento do Loop de Venda

- [x] **Fase 35 — concluída** — a partir de uma spec de duas partes escrita pelo próprio usuário. `migration-073`. **Parte 1 — recorrência genérica**: `recurrence_interval` (texto: `7`/`15`/`30`/`cadencia_reuniao`/`cadencia_otimizacao`, null = única) + `completed_at` novos em `activity_checklist_items` e `client_tasks`; editável por item/etapa nos formulários de `ActivityTemplateFormDialog`/`ClientWorkflowTemplateFormDialog` (o Workflow Operacional não ganhou o campo — só tarefa interna do Kanban não recorre). Desenho deliberado sem cron nem duplicar linha: a MESMA instância cicla — `completed`/`status` fica batido até `completed_at + intervalo` vencer, aí volta a contar como pendente sozinha, tudo calculado em JS puro (`src/lib/recurrence.ts`, coberto por teste automatizado) a partir do plano atual do cliente (`clients.plan`) — troca de plano já muda o ritmo na hora. Cadência "do plano" tem 2 sabores: "cadência de reunião" (reaproveita o mesmo mapeamento de `meeting_recurrence_interval` já existente — Validação mensal/Escala quinzenal/Dominação semanal) e "cadência de otimização" (mesmo mapeamento, conceito novo, ajustável à parte no futuro). Efetivo aplicado em `Activities.tsx`, no card de Atividades da Central de Informações, em `/tasks` (Portal Cliente) e na aba "Tarefas do projeto" de `/project`. **Bug real achado revisando o código**: a reescrita de `apply_workflow` na Fase 34a (migration-070) tinha derrubado a cópia de `platform_scope` pro `activity_checklist_items` — toda entrada criada por "Aplicar Workflow" (Operacional) desde então nascia com o padrão "as duas plataformas", ignorando item exclusivo de Meta/Google só; corrigido junto nessa migration. **Parte 2 — Fechamento do Loop de Venda**: `form_responses.status` (Novo/Qualificado/Venda/Perdido, RPC `set_form_response_status` — gestor mexe em qualquer um, cliente só nos próprios) marcável em Ativos Digitais → Integrações → "Ver respostas" (gestor) OU na aba nova "Leads" do Portal Cliente (`Leads.tsx`, criada depois de perguntar ao usuário onde o cliente marcaria isso — RLS nova pra `cliente` ler `form_responses`/`form_answers`/`form_questions` das próprias respostas, essa última via função `security definer` pra não abrir `digital_asset_connections`, que guarda token OAuth, pro papel cliente). Metas SMART ganham 2 métricas novas (`leads_qualificados`, `vendas`) alimentadas pela contagem real de status — sem sobrescrever `current_value` sozinho (continua editável manualmente, igual toda outra métrica), só mostra a contagem real ao lado com um botão "usar esse valor" (`SmartGoalCard.tsx`/`UpdateGoalProgressDialog.tsx`, helper `src/lib/lead-metrics.ts`). `npx tsc -b --noEmit`, `npm run test` (60/60) e `npm run build` limpos.
- [x] **Fase 35.1 (bug real, corrigido)** — usuário reportou que mandar uma tarefa via "Workflows do Cliente" aparecia certo no Portal Cliente, mas não aparecia em "Tarefas do Cliente" (`/client-tasks`) do Portal Gestor — os dois "não estavam sincronizados". Causa: desde a Fase 30 (separação `tasks`/`client_tasks`), essa página continuou lendo `public.tasks` (Kanban interno) filtrado por cliente, em vez de `client_tasks` — apesar do próprio propósito da página, segundo o usuário, sempre ter sido "o gestor ver quais tarefas o cliente está completando e criando". Corrigido sem migration nova (RLS `admin_gestor_full_client_tasks` já liberava tudo, só faltava a UI): `useAllClientTasks`/`useUpdateClientTaskStatusAsManager`/`useDeleteManagerClientTask`/`useCreateManagerClientTask`/`useUpdateManagerClientTask` novos (`useManagerPortalData.ts`); `ManagerClientTaskRow.tsx` e `ClientTaskFormDialog.tsx` novos (mesmo molde de `ManagerTaskRow.tsx`/`KanbanTaskFormDialog.tsx`, que continuam intocados e exclusivos do Kanban); `ManagerClientTasks.tsx` reescrita pra usar os novos. Já sai considerando recorrência (Fase 35): badge + status efetivo igual ao resto. `npx tsc -b --noEmit`, `npm run test` (60/60) e `npm run build` limpos.
- [x] **Fase 34d.1 (correção)** — o usuário avisou que o limite de caracteres de Headline (Fase 34d) deveria ser 30, não 15 (engano no pedido original). `CATALOG_TIPO_MAX_LENGTH.headline` corrigido pra 30 em `src/lib/catalog.ts` — Descrição continua 90, sem mudança. `npx tsc -b --noEmit`, `npm run test` e `npm run build` limpos.

## Fase 35.2 — Desconectar integração, nome real do Google Forms conectado, validação do link ao conectar

- [x] **Fase 35.2 — concluída** — 3 pedidos do usuário depois de usar Ativos Digitais/Integrações ao vivo. `migration-074`: `digital_asset_connections.external_account_name` (novo). **Desconectar integração**: o botão "Conectar integração" ficava lado a lado com "Sincronizar agora" mesmo já conectado, contraintuitivo — agora vira "Desconectar integração" (`handleDisconnect` novo na Edge Function `integrations`, rota `POST /disconnect`, mesmo espírito de `handleAgencyDisconnect`: mantém a linha/histórico, só reseta status/external_account_id/external_account_name/login_customer_id e apaga o `oauth_tokens`) enquanto existir uma conexão `connected` daquele Ativo; `useDisconnectIntegration`/`disconnectIntegration` novos, `AssetCard.tsx` troca o botão condicionalmente. **Validação real do link do Google Forms** (bug real: colar o link PÚBLICO de resposta, `.../forms/d/e/<id>/viewform`, fazia `extractFormId` capturar só a letra solta "e" — a conexão completava o OAuth e ficava "Conectada" mesmo sem apontar pra formulário nenhum, porque o login do Google não tem nada a ver com qual formulário foi escolhido): `handleConnect` rejeita na hora um link `.../forms/d/e/` antes de iniciar o OAuth; `handleCallback` (novo bloco pro provider `google_forms`, diferente de Google Ads/Meta — aqui a validação é obrigatória, não best-effort) chama `forms.googleapis.com/v1/forms/{formId}` de verdade depois do OAuth completar e só marca "Conectado" se a API confirmar acesso, senão marca "Erro" com mensagem clara. **Nome real do formulário + link automático**: a mesma chamada de validação já devolve `info.title` (gravado em `external_account_name`, mostrado no card do Ativo, na lista de Integrações e no título de "Ver respostas" — diferente do nome do Ativo Digital, que é só um rótulo escolhido por quem cadastrou) e `responderUri` (gravado sozinho no `url`/"Link de acesso" do Ativo Digital). Sem migration de dado (só a coluna nova, populada a partir da próxima vez que cada conexão existente for reconectada). `npx tsc -b --noEmit`, `npm run test` (60/60) e `npm run build` limpos.

## Fase 36 — Arquivamento automático de tarefas concluídas

- [x] **Fase 36 — concluída** — usuário perguntou (pergunta exploratória, não pedido direto) qual seria a melhor forma de apagar tarefas concluídas, automática ou manual, considerando o sistema de recorrência da Fase 35; recomendei arquivar em vez de apagar de verdade (convenção comum em apps de tarefa — some da lista, mas fica recuperável) e disse que itens com recorrência já não têm o problema (a Fase 35 cicla a mesma linha, nunca cria linha nova); usuário aprovou ("pode implementar o esquema"). `migration-075`: `archived_at` novo em `tasks` (Kanban) e `client_tasks`; função `archive_stale_completed_tasks()` (security definer, só admin/gestor) arquiva `status = 'done'` há mais de 30 dias — em `tasks` usa `updated_at` como proxy de "quando terminou" (Workflow Operacional não tem `completed_at`, de propósito sem recorrência); em `client_tasks` usa `completed_at` e pula qualquer linha com `recurrence_interval` preenchido (essas nunca acumulam, não fazem sentido arquivar). Sem cron: `useAutoArchiveOldTasks()` (`useManagerPortalData.ts`) dispara a função uma vez por carregamento de página (Kanban e "Tarefas do Cliente" chamam o mesmo hook — não importa qual tela o gestor abre primeiro, a função cobre as duas tabelas), toast só quando realmente arquivou algo. `useAllTasks`/`useAllClientTasks` passam a filtrar `archived_at is null`; hooks novos `useArchivedTasks`/`useArchivedClientTasks` (lista arquivada) e `useRestoreManagerTask`/`useRestoreManagerClientTask` (`archived_at = null`, volta pra lista normal). `ArchivedTasksDialog.tsx` novo (`src/components/tasks/`, compartilhado pelas 2 telas) — botão "Arquivadas (N)" ao lado dos filtros, mostra a lista com "Restaurar" e "Apagar definitivamente" (confirmação, reaproveita os hooks de delete que já existiam). `npx tsc -b --noEmit`, `npm run test` (60/60) e `npm run build` limpos.
- [x] **Fase 36.1 (correção de escopo)** — usuário apontou que Atividades do Gestor (`activity_checklist_items`) acumula tanto ou mais que o Kanban (workflow inteiro instanciado por cliente, sem nenhuma separação visual de concluído) e ficou de fora da Fase 36 por engano; também identificou o risco real de arquivar item RECORRENTE do mesmo jeito que um não-recorrente: a mesma linha cicla pra sempre (Fase 35), então arquivar de verdade (sumir da consulta) quebraria a recorrência — o item nunca mais voltaria a aparecer sozinho quando vencesse de novo, sem uma ação manual de "restaurar" que ninguém chamaria. Resolvido separando em duas coisas: (1) **arquivamento de verdade** (`migration-076`, `activity_checklist_items.archived_at` novo, 3º bloco em `archive_stale_completed_tasks()`) só pra item SEM recorrência concluído há mais de 30 dias — mesmíssimo padrão de `client_tasks`; hooks `useArchivedActivityChecklistItems`/`useRestoreActivityChecklistItem` novos, `ArchivedTasksDialog` (Fase 36) reaproveitado direto em `Activities.tsx`. (2) **colapso visual** (`isCompletionStale`, `src/lib/recurrence.ts`, testado) — SEM migration nenhuma: item concluído (recorrente ou não) fica visível só por 1 dia depois de `completed_at`, daí entra num toggle "Mostrar concluídas há mais de 1 dia (N)" colapsado por padrão, tanto em `Activities.tsx` (por cliente) quanto no card de Atividades da Central de Informações (`ClientDetail.tsx`); item recorrente sai do colapso sozinho no instante em que `isRecurrenceDueAgain` vira verdadeiro — mesmo cálculo de sempre, sem nenhuma ação de restaurar envolvida, exatamente o "outro método" que resolve a preocupação do usuário. Contagem de progresso (`X de Y concluídos`) continua somando todo mundo, só a renderização de linha muda. Achado à parte, ainda não resolvido: `apply_workflow`/`apply_client_workflow` inserem `activity_checklist_items` sem checar duplicata — reaplicar o mesmo Workflow no mesmo cliente cria linhas repetidas; arquivamento não resolve isso (duplicata não é "dado antigo"), fica pendente de decisão do usuário. `npx tsc -b --noEmit`, `npm run test` (64/64) e `npm run build` limpos.
- [x] **Fase 36.2 — concluída** — usuário achou que o colapso genérico da Fase 36.1 não bastava pro item RECORRENTE: queria ver quanto tempo falta pra vencer de novo (o badge só dizia "cadência de otimização", sem prazo nenhum), e que o item recorrente concluído nem ficasse 1 dia na lista principal — some na hora, direto pra uma aba própria "Recorrentes", separada de "Arquivadas". `src/lib/recurrence.ts` ganha `recurrenceDueAt`/`daysUntilRecurrenceDue` (testados) — mesma conta de `isRecurrenceDueAgain`, extraída pra não duplicar. `RecurringTasksDialog.tsx` novo (`src/components/tasks/`, reaproveitado pelas 2 telas): lista título/cliente/rótulo de recorrência/"Vence em N dias (data)", ordenado do que vence mais cedo, com "Reabrir agora" por linha (reaproveita o toggle/status normal, sem mutation nova) — nunca é arquivamento de verdade, não mexe em coluna nenhuma no banco, a mesma linha já ia voltar sozinha quando vencesse (Fase 35), "Reabrir agora" só adianta isso. Aplicado em `Activities.tsx` (item recorrente concluído nunca mais aparece na lista principal, nem no toggle "Mostrar concluídas" da Fase 36.1 — esse ficou só pra item sem recorrência) e `ManagerClientTasks.tsx` (mesma ideia, filtro novo em `filteredTasks`); Kanban não ganhou nada aqui — Workflow Operacional continua sem recorrência, de propósito (Fase 35). Também dá mais visibilidade pro teste da cadência de plano (item 27 do TESTES.md): a contagem de dias muda na hora ao trocar o plano do cliente, sem precisar simular `completed_at` às cegas. `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
- [x] **Fase 36.3 (achados ao vivo, corrigidos)** — 3 coisas reportadas numa mesma rodada de teste. **Token de acesso do Google parando de funcionar sozinho**: `refreshGoogleAccessToken`/`refreshMetaAccessToken` (Edge Function `integrations`) falhavam em silêncio (só `return null`, nenhum log) — sem jeito de saber SE era `invalid_grant` (refresh_token revogado/vencido) ou outra coisa. Corrigido: as duas passam a chamar `logServerError` com o corpo real do erro do Google/Meta; o branch do Google em `getValidAccessToken`/`getValidAgencyAccessToken` também passa a marcar a conexão como `status: 'error'` quando o refresh falha (o branch do Meta já fazia isso, o do Google tinha ficado pra trás — conexão ficava "Conectada" pra sempre mesmo morta). Causa mais provável do caso relatado (token que funcionava e parou sozinho): enquanto o app OAuth do Google Cloud estiver em modo "Testing" (não verificado — item 31), o Google expira QUALQUER refresh_token sozinho depois de 7 dias, então toda conexão para de sincronizar sozinha depois de uma semana até a verificação sair — o único jeito de voltar a funcionar antes disso é desconectar e conectar de nova (gera refresh_token novo). **Métrica "Leads" (bruta) sem contagem real**: `leadCountForMetric`/`isLeadCountMetric` (`src/lib/lead-metrics.ts`) só cobriam `leads_qualificados`/`vendas` — metas com o metric_type genérico "Leads" (que já existia antes da Fase 35, mistura fácil com "Leads Qualificados") nunca mostravam a linha de contagem real, mesmo o hook já calculando `total` (contagem de QUALQUER resposta, todo status) havia tempo. Corrigido: "Leads" passa a contar `total`; `leadCountLabel` novo diferencia o texto — "Leads Qualificados"/"Vendas" continuam "Contagem real...", "Leads" vira "Respostas de formulário recebidas (bruto, sem validação)", pedido explícito do usuário pra não deixar o gestor (ou o cliente, se o valor for usado sem essa ressalva) confundir volume bruto de preenchimento com lead validado de verdade. **Investigado, sem bug encontrado**: usuário reportou que uma tarefa de `client_tasks` com recorrência de cadência do plano não mostrava o badge 🔁 — revisado `apply_client_workflow`/`ClientWorkflowTemplateFormDialog.tsx`/`ManagerClientTaskRow.tsx` de ponta a ponta, os 3 pontos gravam/leem a chave `recurrence`/`recurrence_interval` de forma consistente; explicação mais provável é a recorrência ter sido definida no modelo DEPOIS que aquela tarefa específica já tinha sido criada (é uma cópia feita no momento de aplicar o Workflow, não um vínculo vivo com o modelo) — sem reproduzir o bug, não deu pra confirmar nem corrigir, passado pro usuário verificar `select recurrence_interval from client_tasks where id = '...'` direto no SQL Editor. `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
- [x] **Fase 36.4 (achados ao vivo, corrigidos)** — 2 coisas reportadas na mesma rodada da Fase 36.3. **"Tarefas do Cliente" sem risco visual em concluída**: `ManagerClientTaskRow.tsx` (Portal Gestor) mudava só a cor do badge de status ao marcar "Concluída", o título continuava com a cor normal — diferente de `Activities.tsx`/`TaskList.tsx` (Portal Cliente), que já riscavam (`line-through`) o título nesse caso; corrigido pra usar o mesmo padrão (título com `text-muted-foreground line-through` quando `effectiveTaskStatus(...) === 'done'`). **Sem indicação de quais itens de um MODELO de Workflow têm recorrência configurada**: o badge roxo 🔁 já existia nas listas de itens/tarefas JÁ INSTANCIADAS (`Activities.tsx`, `ManagerClientTaskRow.tsx`, Fase 35), mas os CARDS de modelo em Workflows (`ActivityTemplateCard.tsx`, `ClientWorkflowCard.tsx`) nunca mostravam isso — só dava pra saber abrindo "Editar" item por item. Corrigido adicionando o mesmo badge (`Repeat` + `recurrenceShortLabels`) ao lado do título de cada item/step que tiver `recurrence`/`step.recurrence` preenchido, direto no card, sem precisar abrir o formulário. Workflows Operacionais (Kanban, `WorkflowCard.tsx`/`WorkflowTemplateFormDialog.tsx`) ficaram de fora de propósito — nunca tiveram campo de recorrência (só o Workflow de Atividades e o Workflow do Cliente têm, Fase 35). `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.

## Fase 37 — Redesenho da definição do funil de lead

- [x] **Fase 37 — concluída** — usuário apontou um problema real de escala: marcar status de lead resposta por resposta não funciona quando o volume cresce, nem ele nem o cliente têm tempo pra isso. Depois de eu propor 4 caminhos (ações em massa / regras automáticas por resposta / os dois / pergunta aberta) e perguntar via `AskUserQuestion`, o usuário explicou como já usa formulário na prática: cada formulário tem função fixa por cliente (um "cliente-comprador" = fecha venda, outro de "objeções" = registra por que não comprou) e não existe formulário de "leads"/"qualificado" de propósito, porque colocar um formulário na frente do prospect atrapalharia a conversão — e trouxe um modelo novo de funil: Leads (bruto) pelo pixel/tag do próprio Google/Meta Ads, Qualificado por quem responde no WhatsApp, Venda registrado pelo cliente. `migration-077`. **Bloco 1 — propósito do formulário**: `digital_asset_connections.form_purpose` novo (`vendas`/`perdido`/null), RPC `set_form_purpose` (admin/gestor); `syncFormsConnection` (Edge Function `integrations`) ganha um update em lote no fim — toda resposta ainda "novo" (nunca mexida na mão) de um formulário com propósito definido já nasce classificada sozinha, sem sobrescrever nada já classificado manualmente; Select novo dentro de `FormResponsesDialog.tsx` (Ativos Digitais → Integrações → Ver respostas). **Bloco 2 — "Leads" (bruto) ganha 2ª fonte de contagem real**: `useClientAdConversions` (`useManagerPortalData.ts`) soma `conversions` (já sincronizado por campanha pra CPA/ROAS, `campaign_performance_snapshots`) dos últimos 30 dias de TODAS as campanhas vinculadas a qualquer projeto do cliente — mostrado ao lado (nunca substituindo) da contagem de resposta de formulário em `SmartGoalCard.tsx`/`UpdateGoalProgressDialog.tsx`, com rótulo deixando clara a origem (`adConversionsLabel`, `src/lib/lead-metrics.ts`), já que são números diferentes por natureza. **Bloco 3 — leads manuais (Qualificado via WhatsApp)**: como não existe integração de WhatsApp Business API no projeto (escopo grande demais pra essa rodada, usuário optou por resolver sem ela por agora), tabela nova `manual_leads` (mesmo funil de status de `form_responses`, nasce "qualificado") + RPC `set_manual_lead_status`; `ManualLeadsDialog.tsx` novo (`src/components/leads/`, componente único reaproveitado nos dois portais) tem busca dentro das respostas de formulário já sincronizadas (`useSearchFormResponses`, `ilike` em `form_answers.answer_text`) ANTES de liberar o registro manual — pedido explícito do usuário ("um mecanismo de busca, pra não ter bagunça") pra não duplicar quem já tem uma linha de formulário; se achar, marca a resposta existente como Qualificado (fluxo normal); se não achar, registra nome/contato/observação como lead novo. Aparece em Ativos Digitais → Integrações (Portal Gestor, botão "Leads manuais" ao lado de "Ver respostas") e na aba Leads (Portal Cliente, botão "Registrar lead manual") — RLS/RPC cobrem os dois papéis (cliente só nos próprios). `useClientLeadStatusCounts` passa a somar `manual_leads` em "Qualificados"/"Vendas" (nunca no `total` bruto, que continua só resposta de formulário — lead manual não preencheu formulário nenhum). Precisa de `supabase functions deploy integrations` pro Bloco 1 funcionar (Blocos 2/3 são só frontend/RPC). `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
- [x] **Fase 37.1 (achado ao vivo, corrigido)** — usuário pediu a lista de escopos do Meta Ads pra configurar no App Dashboard antes de começar a testar essa integração (ainda não mexida). Revisando o código pra responder, achei um bug real: a conexão de AGÊNCIA (Business Manager, Fase 28 — `handleAgencyConnect`) chama `GET /me/businesses` e `GET /{business_id}/client_ad_accounts`, que exigem o escopo `business_management` — mas a URL de autorização pedia só `ads_read` (`META_SCOPE`, reaproveitado sem diferenciação da conexão por cliente, que não passa por Business Manager e não precisa desse escopo a mais). Corrigido com `META_AGENCY_SCOPE = 'ads_read business_management'`, usado só no fluxo de agência; conexão por cliente continua só `ads_read`. `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos. PRECISA DE DEPLOY MANUAL DA EDGE FUNCTION.
- [x] **Fase 37.2 (achado ao vivo, corrigido)** — usuário testou a conexão de agência do Meta logo depois da Fase 37.1 e "Escolher Business Manager" veio vazio ("Nenhum Business Manager encontrado"), mesmo tendo um Business Manager de verdade na mesma conta/e-mail. Causa mais provável: reconectou antes do deploy da Fase 37.1 chegar a valer (token sem `business_management` ainda) — mas ao investigar achei um bug de diagnóstico real: `discoverMetaBusinesses`/`discoverMetaClientAdAccounts` só logavam a falha com `console.error` (inacessível pro usuário, só nos logs internos do Supabase) e devolviam lista vazia — indistinguível de "a conta realmente não tem Business Manager nenhum". Corrigido pra logar com `logServerError` (aparece em Configurações → Erros) igual ao padrão já usado pro refresh de token (Fase 36.3). `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos. PRECISA DE DEPLOY MANUAL DA EDGE FUNCTION.
- [x] **Fase 37.3 (achado ao vivo, corrigido)** — usuário reportou não conseguir rolar o formulário de "Novo Projeto"/edição de projeto ao tentar vincular uma campanha do Meta Ads (o formulário cresceu ao longo das fases — Objetivo, Descrição, Tipo de conversão, Tipo de teste A/B, Campanha vinculada — e passou a estourar a altura da tela sem scroll nenhum). Causa raiz: o `DialogContent` base (`src/components/ui/dialog.tsx`) nunca teve `max-h`/`overflow-y-auto` próprios — cada diálogo precisava lembrar de passar isso no `className` (só alguns faziam, ex: `FormResponsesDialog.tsx`), e `NewProjectDialog.tsx` nunca tinha. Corrigido na raiz, não só nesse diálogo: `DialogContent` ganhou `max-h-[85vh] overflow-y-auto` como padrão (via `cn()`, então qualquer diálogo que já define seu próprio `max-h`/`overflow` continua podendo sobrescrever) — protege TODOS os diálogos do app contra o mesmo problema, presentes e futuros, não só o de projeto. `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
- [x] **Fase 37.4 (achado ao vivo, corrigido)** — usuário reportou que a aba "Grupos de Anúncios" de um projeto vinculado ao Meta Ads não tinha "conjunto de anúncios" nenhum, e pediu pra confirmar se Visão Geral/Campanha puxam dado real da API do Meta. **Verificação (sem bug encontrado)**: revisei `syncConnection` (Edge Function `integrations`) — a branch do Meta já chama `/insights` (spend/clicks/impressions/conversions, quebrado por campanha+dia) e `/campaigns` (objetivo/status) de verdade, grava em `performance_snapshots` e `campaign_performance_snapshots` exatamente como o Google — Visão Geral e Campanha já mostravam dado real, não fake. **Bug real, corrigido**: `handleListAdGroups` (endpoint `/ad-groups`) e a aba `AdGroupsTab.tsx` eram hard-blocked pra `provider !== 'google_ads'`, com uma mensagem genérica de "só Google Ads por enquanto" cobrindo TUDO (orçamento/impressão perdida, ad groups E os resumos curados juntos) — mesmo a lista de "conjuntos de anúncios" tendo equivalente direto no Meta (hierarquia Campanha > Conjunto de Anúncios > Anúncio). Corrigido: `handleListAdGroups` ganhou uma branch pro Meta (`GET /{campaign_id}/adsets` pra id/nome/status + `GET /{campaign_id}/insights?level=adset` pra spend/clicks/impressions/conversões, mesmo formato de resposta do Google, `avgQualityScore` sempre `null` — esse conceito não existe em ad set do Meta); status normalizado pro mesmo vocabulário ENABLED/PAUSED/REMOVED via `normalizeMetaStatus` novo (extraído do que `syncConnection` já fazia inline pra campanha, reaproveitado nos 2 lugares). `AdGroupsTab.tsx` reestruturada: a seção de grupos/conjuntos de anúncio agora renderiza pros dois provedores (rótulo muda: "grupo de anúncio" no Google, "conjunto de anúncios" no Meta); só orçamento/impressão perdida e os resumos curados (dispositivo/geográfico/top termos/palavras-chave/demográfico) continuam exclusivos do Google Ads, de propósito — são conceitos de Pesquisa sem equivalente direto no Meta — agora com aviso claro em vez da mensagem genérica de antes bloqueando tudo junto. `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos. PRECISA DE DEPLOY MANUAL DA EDGE FUNCTION.

## Fase 38 — Propósito do formulário movido pra criação do Ativo Digital

- [x] **Fase 38 — concluída (aguardando migration/deploy)** — usuário apontou que o "propósito do formulário" (Fase 37) tinha ficado num lugar pouco intuitivo, dentro de "Ver respostas", e pediu pra mover pra criação do Ativo Digital: Tipo ganha a opção "Formulário (Google Forms)" e, ao escolher esse tipo, já aparece o campo "Propósito deste formulário" (Genérico/Vendas/Objeções). `migration-078` — `digital_assets.form_purpose` novo (mesmo check `vendas`/`perdido`/null de `digital_asset_connections.form_purpose`, Fase 37); função `set_asset_form_purpose(asset_id, purpose)` atualiza o Ativo E, se já existir, propaga pra conexão `google_forms` vinculada (RLS de `digital_asset_connections` só libera SELECT pro app, por isso via RPC); backfill copia o valor de quem já tinha propósito configurado só na conexão (fluxo antigo). `AssetFormDialog.tsx` ganha o campo condicional (`isGoogleForms`); `useUpdateDigitalAsset` passa a chamar a RPC nova depois do update normal, sempre que `form_purpose` está no payload (inclusive pra limpar/`null` quando o Tipo muda pra outro valor). `FormResponsesDialog.tsx` perdeu o Select (virou texto informativo, lendo o mesmo `connection.form_purpose` de sempre — sem mudança na leitura). `handleConnect` (Edge Function `integrations`) passa a copiar `digital_assets.form_purpose` pro `form_purpose` da conexão nova, no momento em que ela é criada — sem isso, conectar um Ativo já configurado ficaria com o propósito "esquecido" até editar de novo. `src/lib/form-purpose.ts` novo — extrai as 3 opções pra um lugar só, evitando duplicar entre os dois componentes. `npx tsc -b --noEmit` limpo (`npm run build` chegou a compilar e empacotar os módulos, só travou depois numa falha de binário nativo do rollup pré-existente neste ambiente — `@rollup/rollup-linux-x64-gnu` ausente, bug conhecido do npm com dependências opcionais, sem relação com esta mudança). **Pendente**: rodar `migration-078-proposito-formulario-no-ativo.sql` e reimplantar a Edge Function `integrations` (`supabase functions deploy integrations`) — sem isso, o campo aparece na tela mas salvar dá erro (RPC/coluna não existem ainda) e conectar um Ativo novo não herda o propósito automaticamente. `docs/interno/TESTES.md` — item 37 (Bloco 1) atualizado pra apontar pro novo lugar, e item 39 novo criado com o roteiro de teste completo desta fase.

## Fase 39 — Rodada de testes ao vivo do TESTES.md (via `docs/interno/Prompt - fase testes pendentes.md`)

- [x] **Migration-078 já estava aplicada** — confirmado direto no banco (cliente Supabase autenticado como a conta de teste admin, sem service role): `digital_assets.form_purpose` e a RPC `set_asset_form_purpose` já existiam e funcionavam antes de eu começar a testar (alguém — usuário ou a sessão anterior — já tinha rodado). Commitei o trabalho da Fase 38 (estava só no working tree, não commitado ainda) depois de revisar o diff inteiro.
- [x] **Item 29 resolvido — não era bug, era relato desatualizado**: consultei `campaign_performance_snapshots.campaign_status` direto (cliente autenticado como admin, `select` na coluna) — ela existe e já tem dado real (`"ENABLED"`), confirmando que `migration-067` rodou de verdade e está sincronizando. O erro "Could not find the 'campaign_status' column" que o usuário viu deve ter sido de ANTES da migration rodar; não reproduz mais. Item removido do TESTES.md.
- [x] **Fase 38 (item 39 do TESTES.md) testada ao vivo com Playwright** (`tests/e2e/fase38-asset-form-purpose.spec.ts`, novo) — confirmado: Tipo "Formulário (Google Forms)" aparece na lista, revela o campo "Propósito deste formulário" só quando esse Tipo está selecionado, o valor persiste ao reabrir pra editar, e trocar o Tipo pra outro valor limpa `form_purpose` pra `null` de verdade (checado direto no banco, não só na tela). As 2 partes que dependem de login real do Google (conectar um Ativo do zero, sincronizar com a Edge Function reimplantada) ficam pendentes de confirmação manual — fora do alcance de automação sem credencial de terceiro.
- [x] **2 bugs reais achados nos TESTES E2E existentes (não no app)**: rodando a suíte inteira (`npx playwright test`), 2 specs pré-existentes falhavam por estarem desatualizadas em relação a mudanças reais do app em fases anteriores — não eram regressões desta sessão. (1) `auth.spec.ts` clicava em "Sair" uma vez só e esperava ir direto pro `/login` — mas o app tem uma confirmação em 2 passos ("Sair da sua conta?") desde antes, então o logout nunca completava de verdade; corrigido pra clicar no "Sair" do menu E no "Sair" de confirmação do diálogo. (2) `workflow-tasks.spec.ts` esperava um combobox único "Cliente — Projeto" pra aplicar um Workflow Operacional — mas esse fluxo foi reestruturado nas Fases 21/26 pra 3 selects separados (Cliente, "Aplicar em..." Projeto/Kanban, e só então o Projeto) e o texto do toast de sucesso também mudou; corrigido pra seguir o fluxo novo e o texto novo ("N tarefas criadas no projeto escolhido."). `approval-revision-task.spec.ts` passa normal isolado — não precisou de correção. **Achado à parte (ambiente, não é bug de app nem de teste)**: rodar a suíte e2e INTEIRA de uma vez nesta máquina falha de forma consistente e imprevisível (client diferente falha a cada rodada) mesmo com os 2 bugs acima corrigidos — todo teste passa normalmente quando rodado sozinho; suspeita é limite de taxa do Supabase Auth por causa de vários logins em sequência rápida entre arquivos de teste diferentes. Recomendação prática: rodar specs e2e individualmente (ou com `--workers=1` e algum espaçamento) nesta máquina, não a suíte inteira de uma vez. `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
- [x] **Item 7 — tentativa de reprodução, sem sucesso (não é "sem bug", é "não reproduzido")**: testei ao vivo com Playwright — cliente de teste descartável, trocar Plano de Validação pra Dominação em Central de Informações → Salvar → confirma sucesso, sem erro, valor persiste depois de recarregar (`tests/e2e/item7-mudanca-plano.spec.ts`, mantido como regressão). Não force conclusão nenhuma além disso — o relato original do usuário continua sem detalhe suficiente pra saber se o cenário de teste bateu com o que ele viveu (pode ser um cliente com dado específico já existente, uma mensagem de erro que só aparece em outra combinação, etc.). Registrado no TESTES.md pedindo mais detalhe.
- [x] **Itens 27/28/32/33/34/36 confirmados ao vivo com Playwright** (`tests/e2e/recorrencia-arquivamento.spec.ts`, novo, 4 testes) — cobrindo o núcleo do sistema de recorrência/arquivamento das Fases 35/36/36.1/36.2/36.4 de ponta a ponta: item recorrente concluído some da lista principal na hora e aparece em "Recorrentes" com contagem certa; forçar `completed_at` além do prazo faz o item voltar PENDENTE sozinho (mesma linha, activity_checklist_items e client_tasks); recorrência NUNCA é arquivada mesmo aos 31 dias; item sem recorrência aos 31 dias arquiva de verdade; trocar o plano do cliente muda o prazo de recorrência na hora, sem editar o item; Kanban não ganha o botão "Recorrentes". **Bug real achado e corrigido nesse processo**: o React acusava `<p> cannot contain a nested <div>` — o Badge de recorrência (`<Badge>`, que renderiza um `<div>`) estava aninhado dentro de uma tag `<p>` em 4 arquivos (`ManagerClientTaskRow.tsx`, `Activities.tsx`, `RecurringTasksDialog.tsx`, `ClientDetail.tsx`) — HTML inválido que o navegador corrige sozinho quebrando a árvore DOM na hora de renderizar (o motivo real de várias tentativas de locator falharem ao escrever o teste, antes de eu perceber o warning). Corrigido trocando o `<p>` por `<div>` nos 4 lugares (mesmas classes, sem mudança visual). `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
- [x] **Item 21 confirmado ao vivo com Playwright** (`tests/e2e/item21-workflow-cliente.spec.ts`, novo) — aplicar um Workflow do Cliente cria a tarefa em `client_tasks` (status "backlog", nunca em `tasks`/Kanban) e ela aparece de verdade em `/tasks` do Portal Cliente, ponta a ponta (gestor aplica → cliente vê).
- [x] **Item 37 Bloco 3 confirmado parcialmente ao vivo com Playwright** (`tests/e2e/item37-leads-manuais.spec.ts`, novo, via Portal Cliente → Leads → "Registrar lead manual") — caminho "busca não acha nada → registra lead novo" completo: aviso de busca vazia certo, lead nasce "Qualificado", troca de status pelo badge persiste no banco. Caminho "busca encontra uma resposta de formulário existente e promove ela" não testado — precisa de dado real de formulário sincronizado (fora do alcance sem OAuth do Google), que a Fase 39 já vinha sinalizando como limite estrutural desta rodada de testes (sem service role/CLI do Supabase, só as 3 contas fixas de teste). `npx tsc -b --noEmit`, `npm run test` (68/68) e `npm run build` limpos.
